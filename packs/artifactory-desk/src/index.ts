// Desk is a LAYOUT plugin: its whole contribution is DATA in
// `dimension.plugin.json` — its slots, ONE of which carries an `app` pin
// (doc 45 §8: the host calls `get-system-info` on the System Monitor pack's
// server at boot and seats the View), and the Desk space that assembles on it.
// No runtime: a layout is topology, and a pin is a declaration.
export {};
