// Bots is a LAYOUT plugin: its whole contribution is DATA - the Build parts
// plus a space that seats the general-chat pack TWICE, each placement naming
// its own General Agent (doc 72 §4). One agent, one chat; the pack is generic.
export {};
