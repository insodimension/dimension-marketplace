{
	"name": "@dimension/classic-switcher",
	"author": "Dimension",
	"version": "0.1.0",
	"description": "Classic Switcher — the space switcher rail — the far-left column of space marks, as a marketplace pack.",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"scripts": {
		"build": "vite build"
	},
	"peerDependencies": {
		"react": ">=19"
	},
	"devDependencies": {
		"vite": "8.1.5"
	},
	"dimension": {
		"name": "classic-switcher",
		"description": "The shipped switcher rail as a pack — the far-left column of space marks.",
		"defaultEnabled": true,
		"category": "component",
		"keywords": [
			"component",
			"switcher",
			"classic"
		]
	}
}
