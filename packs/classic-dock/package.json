{
	"name": "@dimension/classic-dock",
	"author": "Dimension",
	"version": "0.1.0",
	"description": "Classic Dock — the dock COLUMN — the tab strip, the resize handle, the panel body, as a marketplace pack.",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"scripts": {
		"build": "vite build"
	},
	"peerDependencies": {
		"react": ">=19"
	},
	"devDependencies": {
		"vite": "8.1.5"
	},
	"dimension": {
		"name": "classic-dock",
		"description": "The shipped dock COLUMN as a pack — tab strip, resize handle, panel body. Fills the `dock-column` contract (a `dock` component is an instrument, i.e. one tab).",
		"defaultEnabled": true,
		"category": "component",
		"keywords": [
			"component",
			"dock",
			"classic"
		]
	}
}
