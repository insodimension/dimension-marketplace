/**
 * @fraym/ui — SLOT CONTRACT declarations, flattened and self-contained.
 *
 * GENERATED by `bun scripts/emit-contract-types.ts` in @fraym/ui. Do not edit.
 * Vendor this file into a pack and point the pack's `@fraym/ui` path at it;
 * the pack then types against the real contracts with no monorepo on disk.
 */
import * as react from 'react';
import { ReactNode, ComponentType, RefObject, ComponentProps, DragEvent, PointerEvent, MouseEvent } from 'react';
import * as react_jsx_runtime from 'react/jsx-runtime';

type AvatarId = "blob" | "static" | "rorschach" | "inkblot" | "aurora" | "nebula" | "siri" | "quasar" | "matrix" | "lattice" | "liquid" | "koi" | "duel" | "smiley" | "cat" | "octo" | "cham" | "cube" | "none";
type AvatarState = "idle" | "thinking" | "typing";
type AvatarMode = "" | "search" | "read" | "run" | "edit" | "skill" | "mcp" | "think";
interface AvatarProps {
    readonly state?: AvatarState;
    readonly mode?: AvatarMode;
    readonly energy?: number;
    readonly className?: string;
}

interface PresenceProps extends AvatarProps {
    readonly avatar?: AvatarId;
}
declare function Presence({ avatar, ...props }: PresenceProps): react_jsx_runtime.JSX.Element | null;

declare const iconPaths: {
    readonly chat: "M21 11.5a8.4 8.4 0 0 1-11.9 7.6L3 21l1.9-6.1A8.4 8.4 0 1 1 21 11.5Z";
    readonly book: "M2 4h6a4 4 0 0 1 4 4v12a3 3 0 0 0-3-3H2z|M22 4h-6a4 4 0 0 0-4 4v12a3 3 0 0 1 3-3h7z";
    readonly bot: "M7 7a2 2 0 0 1 2-2h6a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H9a2 2 0 0 1-2-2V7zM10 9a1 1 0 1 0 0 2 1 1 0 0 0 0-2zM14 9a1 1 0 1 0 0 2 1 1 0 0 0 0-2zM12 5V3";
    readonly code: "M7 8l-4 4 4 4M17 8l4 4-4 4M14 4l-4 16";
    readonly chart: "M4 20V11M10 20V4M16 20v-6M22 20H2";
    readonly aether: "M11 3l1.4 5.6L18 10l-5.6 1.4L11 17l-1.4-5.6L4 10l5.6-1.4L11 3z|M18 14l.7 2.3 2.3.7-2.3.7-.7 2.3-.7-2.3-2.3-.7 2.3-.7.7-2.3z";
    readonly layers: "M12 2 2 7l10 5 10-5-10-5z|M2 17l10 5 10-5M2 12l10 5 10-5";
    readonly plus: "M12 5v14M5 12h14";
    readonly bolt: "M13 2 4 14h6l-1 8 9-12h-6l1-8Z";
    readonly loop: "M18.18 8c5.09 0 5.09 8 0 8-5.1 0-7.14-8-12.74-8-4.59 0-4.59 8 0 8 5.6 0 7.64-8 12.74-8Z";
    readonly orbit: "M9 12a3 3 0 1 0 6 0a3 3 0 1 0-6 0|M17 5a2 2 0 1 0 4 0a2 2 0 1 0-4 0|M3 19a2 2 0 1 0 4 0a2 2 0 1 0-4 0|M10.4 21.9a10 10 0 0 0 9.941-15.416|M13.5 2.1a10 10 0 0 0-9.841 15.416";
    readonly shield: "M12 2 4 6v6c0 5 3.5 8 8 10 4.5-2 8-5 8-10V6l-8-4Z";
    readonly gear: "M12 2v3M12 19v3M2 12h3M19 12h3M5 5l2 2M17 17l2 2M19 5l-2 2M7 17l-2 2";
    readonly sliders: "M4 6h11M19 6h1M4 12h5M13 12h7M4 18h9M17 18h3M15 4v4M9 10v4M13 16v4";
    readonly branch: "M7 6.5v11|M17 8.5a10 8 0 0 1-10 8|M18.75 6.75a1.75 1.75 0 1 1-3.5 0a1.75 1.75 0 1 1 3.5 0";
    readonly "git-branch": "M7 8v8|M9 6a2 2 0 1 1-4 0a2 2 0 1 1 4 0|M9 18a2 2 0 1 1-4 0a2 2 0 1 1 4 0|M19 6a2 2 0 1 1-4 0a2 2 0 1 1 4 0|M17 8a8 10 0 0 1-8 10";
    /** THE branch you are on — one stem growing into one node.
     *
     *  Deliberately not `git-branch`: that mark's three nodes describe branching
     *  TOPOLOGY (several checkouts, a fork, a merge), which is what the
     *  environment card's worktree rows mean. A singular stem and a singular tip
     *  says "this one, the one you are standing on", and the two marks sitting
     *  one line apart on that card have to be told apart at 14px. */
    readonly "branch-current": "M7 20.5v-8|M7 12.5c0-4.3 2.6-5.7 4.7-6.1|M16.3 6.4a2.3 2.3 0 1 1-4.6 0a2.3 2.3 0 1 1 4.6 0";
    readonly history: "M3 12a9 9 0 1 0 3-6.7L3 8m0-5v5h5";
    readonly panel: "M3 5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2Z|M15 3v18";
    readonly canvas: "M3 5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2Z|M3 8h18";
    readonly caretU: "m6 15 6-6 6 6";
    readonly caretD: "m6 9 6 6 6-6";
    readonly caretR: "m9 6 6 6-6 6";
    readonly send: "M12 19V5M5 12l7-7 7 7";
    readonly file: "M14 3v4a1 1 0 0 0 1 1h4M5 3h9l5 5v12a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1Z";
    readonly image: "M3 5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2Z|M8.5 11a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3Z|m21 15-4.5-4.5L7 20";
    readonly mic: "M12 2a3 3 0 0 1 3 3v6a3 3 0 0 1-6 0V5a3 3 0 0 1 3-3Z|M19 10v1a7 7 0 0 1-14 0v-1|M12 18v4";
    readonly micOff: "M9 5a3 3 0 0 1 6 0v6c0 .4-.08.79-.22 1.14|M9 9v2a3 3 0 0 0 4.6 2.53|M19 10v1a7 7 0 0 1-.64 2.93M12 18a7 7 0 0 1-7-7v-1|M12 18v4|M3 3l18 18";
    readonly waveform: "M4 12h.01M8 8v8M12 4v16M16 7v10M20 11v2";
    readonly filePlus: "M14 3v4a1 1 0 0 0 1 1h4M5 3h9l5 5v12a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1Z|M9 15h6M12 12v6";
    readonly edit: "M12 20h9M16.5 3.5a2.1 2.1 0 0 1 3 3L8 18l-4 1 1-4Z";
    readonly copy: "M8 8h11v11H8zM5 15H4a1 1 0 0 1-1-1V4h10a1 1 0 0 1 1 1v1";
    readonly folder: "M3 7a2 2 0 0 1 2-2h4l2 2h8a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2Z";
    readonly terminal: "m8 9 3 3-3 3M13 15h4";
    readonly termBox: "M3 4h18v16H3zM8 9l3 3-3 3M13 15h4";
    readonly check: "M20 6 9 17l-5-5";
    readonly x: "M18 6 6 18M6 6l12 12";
    readonly minus: "M5 12h14";
    readonly pause: "M8 5v14M16 5v14";
    readonly maximize: "M8 3H5a2 2 0 0 0-2 2v3M16 3h3a2 2 0 0 1 2 2v3M16 21h3a2 2 0 0 0 2-2v-3M8 21H5a2 2 0 0 1-2-2v-3";
    readonly square: "M5 4h14a1 1 0 0 1 1 1v14a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V5a1 1 0 0 1 1-1z";
    readonly refresh: "M21 12a9 9 0 1 1-2.64-6.36M21 3v6h-6";
    readonly external: "M15 3h6v6M10 14 21 3M19 14v5a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h5";
    readonly search: "M11 19a8 8 0 1 0 0-16 8 8 0 0 0 0 16ZM21 21l-4.3-4.3";
    readonly globe: "M12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18ZM3 12h18M12 3a14 14 0 0 1 0 18 14 14 0 0 1 0-18Z";
    readonly spark: "M12 3v4M12 17v4M3 12h4M17 12h4M6 6l2.5 2.5M15.5 15.5 18 18M18 6l-2.5 2.5M8.5 15.5 6 18";
    readonly arrowR: "M5 12h14M13 5l7 7-7 7";
    readonly user: "M12 8a4 4 0 1 0 0-8 4 4 0 0 0 0 8ZM4 21c0-4 4-6 8-6s8 2 8 6";
    readonly logout: "M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4M16 17l5-5-5-5M21 12H9";
    readonly back: "M19 12H5M12 19l-7-7 7-7";
    readonly arrowUpRight: "M7 17 17 7M8 7h9v9";
    readonly cursor: "M5 3l6.5 16 2.3-6.8 6.8-2.3L5 3z";
    readonly download: "M4 17v2a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-2M12 15V3M8 11l4 4 4-4";
    readonly upload: "M4 17v2a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-2M12 3v12M8 7l4-4 4 4";
    readonly tablet: "M5 3h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z|M12 18h.01";
    readonly phone: "M7 3h10a1 1 0 0 1 1 1v16a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1z|M12 18h.01";
    readonly clock: "M12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18ZM12 7v5l3 3";
    readonly db: "M12 3c4.4 0 8 1.3 8 3s-3.6 3-8 3-8-1.3-8-3 3.6-3 8-3ZM4 6v12c0 1.7 3.6 3 8 3s8-1.3 8-3V6";
    readonly list: "M8 6h13M8 12h13M8 18h13M3 6h.01M3 12h.01M3 18h.01";
    readonly eye: "M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7-10-7-10-7Z|M12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z";
    readonly play: "M6 4l14 8-14 8V4Z";
    readonly diff: "M12 3v18M5 8H2m3 8H2m17-8h3m-3 8h3";
    readonly grid: "M3 3h7v7H3zM14 3h7v7h-7zM14 14h7v7h-7zM3 14h7v7H3z";
    readonly sun: "M12 3v2M12 19v2M3 12h2M19 12h2M5 5l1.5 1.5M17.5 17.5 19 19M19 5l-1.5 1.5M6.5 17.5 5 19|M12 16a4 4 0 1 0 0-8 4 4 0 0 0 0 8Z";
    readonly moon: "M21 12.8A8.5 8.5 0 1 1 11.2 3a6.6 6.6 0 0 0 9.8 9.8Z";
    readonly type: "M4 6V4h16v2M9 20h6M12 4v16";
    readonly keyboard: "M3 6h18v12H3zM7 10h.01M11 10h.01M15 10h.01M7 14h10";
    readonly hook: "M18 6a4 4 0 0 0-8 0v8a3 3 0 1 1-6 0M14 8l4-2 2 4";
    readonly link: "M9 15l6-6M10 6l1-1a4 4 0 0 1 6 6l-1 1M14 18l-1 1a4 4 0 0 1-6-6l1-1";
    readonly computer: "M3 4h18v12H3zM8 20h8M12 16v4";
    readonly monitor: "M4 4h16a1 1 0 0 1 1 1v9a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V5a1 1 0 0 1 1-1z|M9 20h6M12 15v5";
    readonly archive: "M3 4h18v4H3zM5 8v12h14V8M9 12h6";
    readonly trash: "M3 6h18M8 6V4h8v2M6 6l1 15h10l1-15M10 10v7M14 10v7";
    readonly card: "M2 6h20v12H2zM2 10h20";
    readonly envbox: "M3 4h18v16H3zM15 4v16";
    readonly pin: "M12 21s-7-6-7-11a7 7 0 0 1 14 0c0 5-7 11-7 11Z";
    readonly hand: "M8 12V6a1.4 1.4 0 0 1 2.8 0v5M10.8 11V4.5a1.4 1.4 0 0 1 2.8 0V11M13.6 11V6a1.4 1.4 0 0 1 2.8 0v7c0 3.6-2.4 6-6 6-2.6 0-4.4-1.3-5.6-3.4l-1.6-2.8a1.4 1.4 0 0 1 2.4-1.5L8 13";
    readonly markGithub: "M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27s1.36.09 2 .27c1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0 0 16 8c0-4.42-3.58-8-8-8z";
    readonly sword: "M9.5 17.5 21 6V3h-3L6.5 14.5|m11 19-6-6|m8 16-4 4|m5 21-2-2";
    readonly workflow: "M5 3h4a2 2 0 0 1 2 2v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z|M7 11v4a2 2 0 0 0 2 2h4|M15 13h4a2 2 0 0 1 2 2v4a2 2 0 0 1-2 2h-4a2 2 0 0 1-2-2v-4a2 2 0 0 1 2-2z";
    readonly box: "M21 8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16Z|m3.3 7 8.7 5 8.7-5|M12 22V12";
    readonly table: "M12 3v18|M5 3h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z|M3 9h18|M3 15h18";
    readonly braces: "M8 3H7a2 2 0 0 0-2 2v5a2 2 0 0 1-2 2 2 2 0 0 1 2 2v5c0 1.1.9 2 2 2h1|M16 21h1a2 2 0 0 0 2-2v-5c0-1.1.9-2 2-2a2 2 0 0 1-2-2V5a2 2 0 0 0-2-2h-1";
    readonly unreal: "M12 0a12 12 0 1012 12A12 12 0 0012 0zm0 23.52A11.52 11.52 0 1123.52 12 11.52 11.52 0 0112 23.52zm7.13-9.791c-.206.997-1.126 3.557-4.06 4.942l-1.179-1.325-1.988 2a7.338 7.338 0 01-5.804-2.978 2.859 2.859 0 00.65.123c.326.006.678-.114.678-.66v-5.394a.89.89 0 00-1.116-.89c-.92.212-1.656 2.509-1.656 2.509a7.304 7.304 0 012.528-5.597 7.408 7.408 0 013.73-1.721c-1.006.573-1.57 1.507-1.57 2.29 0 1.262.76 1.109.984.923v7.28a1.157 1.157 0 00.148.256 1.075 1.075 0 00.88.445c.76 0 1.747-.868 1.747-.868V9.172c0-.6-.452-1.324-.905-1.572 0 0 .838-.149 1.484.346a5.537 5.537 0 01.387-.425c1.508-1.48 2.929-1.902 4.112-2.112 0 0-2.151 1.69-2.151 3.96 0 1.687.043 5.801.043 5.801.799.771 1.986-.342 3.059-1.441Z";
    readonly dots: "M5 12h.01M12 12h.01M19 12h.01";
    readonly dotsV: "M12 5h.01M12 12h.01M12 19h.01";
    readonly funnel: "M22 3H2l8 9.46V19l4 2v-8.54L22 3z";
    readonly "git-commit": "M12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6z|M3 12h6M15 12h6";
    readonly "git-merge": "M7 8v8|M9 6a2 2 0 1 1-4 0a2 2 0 1 1 4 0|M9 18a2 2 0 1 1-4 0a2 2 0 1 1 4 0|M19 12a2 2 0 1 1-4 0a2 2 0 1 1 4 0|M7 8a8 8 0 0 0 8 4";
    readonly "git-pr": "M7 8v8|M9 6a2 2 0 1 1-4 0a2 2 0 1 1 4 0|M9 18a2 2 0 1 1-4 0a2 2 0 1 1 4 0|M19 8a2 2 0 1 1-4 0a2 2 0 1 1 4 0|M17 10v6a2 2 0 0 1-2 2h-3M14 15l-2 3 2 3";
    readonly cloudUp: "M12 13v8M8.5 16.5 12 13l3.5 3.5|M20 16.6A5 5 0 0 0 18 7h-1.3A8 8 0 1 0 5 15.3";
    readonly warnTri: "M10.3 3.9 1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0z|M12 9v4M12 17h.01";
};
type IconName = keyof typeof iconPaths;

interface IconProps extends React.SVGAttributes<SVGSVGElement> {
    readonly name: IconName;
    readonly size?: number;
    readonly strokeWidth?: number;
    readonly filled?: boolean;
}
declare const Icon: react.MemoExoticComponent<({ name, size, strokeWidth, filled, viewBox, className, ...props }: IconProps) => react_jsx_runtime.JSX.Element>;

/** The session rail's three layout states: full (264px) · icon-only (56px) · gone (0). */
type RailMode = "expanded" | "compact" | "hidden";

/**
 * The compiled shape of a pipeline, as a client draws it.
 *
 * This is a MIRROR, not a model: the engine compiles a `.pipeline.md` into a
 * graph and ships the result on `EnginePipelineRecord`. A surface that rebuilt
 * the graph from `stages[]` would be a second compiler, and two compilers for
 * one contract drift until the picture shows a run the engine would not execute.
 *
 * There is deliberately NO run-driver contract here. A pipeline run is an
 * ordinary agent session — the product seeds one from the pipeline's expansion
 * and the person watches it in the thread (doc 38 §14). The `start + subscribe`
 * driver that used to live in this file described a second executor that nothing
 * ever called; it was deleted rather than left asserting a capability the
 * transport did not have.
 *
 * Mirror discipline: these shapes mirror the engine's `packages/engine/src/pipeline`
 * types. The engine is authoritative; this is the wire's view of it.
 */
type PipelineNodeRole = "turn" | "judge" | "distribution";
/** What a node IS, so a client can draw a run it has never seen before. */
interface PipelineNodeFace {
    readonly id: string;
    readonly role: PipelineNodeRole;
    readonly title: string;
    readonly stageId?: string;
}
/** The compiled shape of a pipeline, mirrored from the engine's graph-core.
 *
 *  Mirrored rather than re-derived: a surface that rebuilt the graph from
 *  `stages[]` would be a second compiler, and two compilers for one contract
 *  drift until the picture shows a run the engine would not execute. */
interface PipelineGraphNode {
    readonly id: string;
    /** Registry id of the effect that runs this node (`node.turn`, `node.judge`…). */
    readonly effect: string;
    readonly params?: Readonly<Record<string, unknown>>;
    readonly needs?: readonly string[];
}
interface PipelineGraphEdge {
    readonly from: string;
    readonly to: string;
    /** Named exit on `from` — `out`, `pass`, `revise`. */
    readonly handle?: string;
    /** Registry id of the guard gating this edge; absent = always passable. */
    readonly guard?: string;
    readonly guardParams?: Readonly<Record<string, unknown>>;
}
interface PipelineGraph {
    readonly id: string;
    readonly nodes: readonly PipelineGraphNode[];
    readonly edges: readonly PipelineGraphEdge[];
    readonly entry?: string;
}

type EngineResourceAuthType = "oauth" | "api_key" | "none";
type EngineProviderAuthSource = "none" | "oauth" | "auth_file" | "env" | "external";
type EngineResourceSourceScope = "user" | "project" | "native" | "temporary";
type EngineResourceSourceOrigin = "package" | "top-level";
type EngineCommandSource = "builtin" | "extension" | "prompt" | "skill";
interface EngineResourceSourceInfo {
    readonly path: string;
    readonly source: string;
    readonly scope: EngineResourceSourceScope;
    readonly origin: EngineResourceSourceOrigin;
    readonly baseDir?: string;
}
type EngineProviderAccountSelectionPolicy = "weighted" | "priority-fallback";
type EngineProviderAccountHealthState = "healthy" | "rate_limited" | "overloaded" | "auth_failed" | "unavailable" | "unknown";
interface EngineProviderAccountLimitRecord {
    readonly id: string;
    readonly label: string;
    readonly status?: "ok" | "warning" | "exhausted" | "unknown";
    readonly usedFraction?: number;
    readonly remainingFraction?: number;
    readonly resetsAt?: number;
}
interface EngineProviderAccountDiagnostics {
    readonly state: EngineProviderAccountHealthState;
    readonly checkedAt: number;
    readonly reason?: string;
    readonly accountId?: string;
    readonly limits?: readonly EngineProviderAccountLimitRecord[];
}
interface EngineProviderAccountRecord {
    readonly key: string;
    readonly label: string;
    readonly active: boolean;
    /** Explicitly pinned: ONLY this account is ever used, even past its rate
     *  limit — distinct from `active` (currently in effect, e.g. the P0 of a
     *  priority-fallback chain that can still fail over). */
    readonly pinned: boolean;
    readonly priority?: number;
    readonly diagnostics?: EngineProviderAccountDiagnostics;
}
interface EngineProviderRecord {
    readonly id: string;
    readonly name: string;
    readonly hasAuth: boolean;
    readonly authType: EngineResourceAuthType;
    readonly authSource: EngineProviderAuthSource;
    readonly oauthSupported: boolean;
    readonly apiKeySetupSupported: boolean;
    /** Provider-level auth failure: a present credential was rejected (e.g. 401/403).
     *  Set only when the effective credential failed, so multi-account OAuth with one
     *  dead account among healthy ones stays unflagged. */
    readonly authFailed?: boolean;
    readonly accountSelectionPolicy?: EngineProviderAccountSelectionPolicy;
    readonly accounts?: readonly EngineProviderAccountRecord[];
}
interface EngineModelCost {
    readonly input: number;
    readonly output: number;
    readonly cacheRead: number;
    readonly cacheWrite: number;
}
interface EngineModelRecord {
    readonly providerId: string;
    readonly providerName: string;
    readonly modelId: string;
    readonly label: string;
    readonly available: boolean;
    readonly authType: EngineResourceAuthType;
    readonly reasoning: boolean;
    readonly supportsImages: boolean;
    /** Max input context in tokens. Absent when the catalog does not know it. */
    readonly contextWindow?: number;
    /** Max output tokens per response. Absent when unknown. */
    readonly maxOutputTokens?: number;
    /** Accepted input modalities (e.g. "text", "image"). */
    readonly inputModalities?: readonly string[];
    /** Native tool-call support. Absent ⇒ supported; only `false` is meaningful. */
    readonly supportsTools?: boolean;
    /** USD per million tokens. Absent when the catalog does not price the model. */
    readonly cost?: EngineModelCost;
}
interface EngineModelBenchmarkCategory {
    readonly id: string;
    readonly label: string;
    readonly elo: number;
    readonly winRate: number;
    /** Rank within the feed's listed models for this category (lower = better). */
    readonly rank: number;
}
interface EngineModelInsightIndices {
    readonly intelligence?: number;
    readonly coding?: number;
    readonly agentic?: number;
}
/** One model's DeepSWE result (Datacurve's long-horizon SWE benchmark —
 *  113 original tasks over live OSS repos, fixed mini-swe-agent harness). */
interface EngineModelDeepSwe {
    /** pass@1, percent 0-100 (attempt pass rate over scored rollouts). */
    readonly passAt1: number;
    /** pass@4, percent 0-100 (tasks with ≥1 passing rollout). */
    readonly passAt4?: number;
    /** Reasoning effort of the best-scoring config (e.g. "max"). */
    readonly effort?: string;
    /** Median cost per task, USD. */
    readonly medianCostPerTaskUsd?: number;
    /** Median agent steps per task. */
    readonly medianAgentSteps?: number;
    /** Snapshot timestamp from the producer's artifact. */
    readonly generatedAt?: string;
}
/** Per-model benchmark/usage enrichment served by the engine's insight feed. */
interface EngineModelInsight {
    /** OpenRouter's coding RANKING: position by Artificial Analysis coding index
     *  across the feed's benchmarked models (lower = better). */
    readonly rank?: number;
    /** Design Arena `codecategories` rank (crowd pairwise votes; lower = better). */
    readonly arenaRank?: number;
    /** Design Arena coding ELO. */
    readonly elo?: number;
    /** Design Arena coding win rate (0-100). */
    readonly winRate?: number;
    /** Derived "good at" tags, e.g. `#1 coding`. */
    readonly strengths?: readonly string[];
    /** Artificial Analysis indices (0-100). */
    readonly indices?: EngineModelInsightIndices;
    /** Real weekly-usage rank across the catalog (lower = more used). */
    readonly usageRank?: number;
    /** Full per-category benchmark table. */
    readonly categories?: readonly EngineModelBenchmarkCategory[];
    /** DeepSWE long-horizon agentic result (fixed-harness, first-party feed). */
    readonly deepSwe?: EngineModelDeepSwe;
    /** LMArena (arena.ai) human-preference leaderboard standing. */
    readonly arenaTextElo?: number;
    readonly arenaTextRank?: number;
    readonly arenaTextVotes?: number;
    readonly arenaCodeElo?: number;
    readonly arenaCodeRank?: number;
    readonly arenaCodeVotes?: number;
}
/**
 * Semantic affordance hint for a command. The UI resolver maps this to an
 * existing Fraym surface (model picker, MCP modal, dock tab, ...) so a picked
 * command can open the right affordance instead of being sent as plain text.
 */
declare const ENGINE_COMMAND_UI_HINTS: readonly ["model-picker", "mcp", "context", "settings", "session-tree", "tasks", "plan-toggle", "tools", "plugins", "usage", "new-session", "compact", "diff"];
type EngineCommandUiHint = (typeof ENGINE_COMMAND_UI_HINTS)[number];
/** One declarative argument-completion row advertised for a command (mirrors Engine's
 *  TUI subcommand dropdown); carried in `_meta["inso/command"].subcommands`. */
interface EngineCommandSubcommand {
    readonly name: string;
    readonly description?: string;
    /** Usage hint, e.g. `<name> [--scope project|user]`. */
    readonly usage?: string;
}
interface EngineCommandRecord {
    readonly name: string;
    readonly description?: string;
    readonly source: EngineCommandSource;
    readonly sourceInfo: EngineResourceSourceInfo;
    readonly uiHint?: EngineCommandUiHint;
    /**
     * Engine-advertised semantic kind of the command's output; clients may
     * override; unknown = generic rendering.
     */
    readonly renderKind?: string;
    /** Declarative argument completions offered once this command is committed. */
    readonly subcommands?: readonly EngineCommandSubcommand[];
    /** Free-text argument hint from the ACP command's `input.hint` (e.g. `<what to work on>`). */
    readonly inputHint?: string;
}
/** A convergence condition on a looping stage — open named-signal vocabulary. */
interface EnginePipelineStageUntil {
    readonly signal: string;
    readonly threshold?: number;
    readonly maxIterations: number;
}
/** A Critic gate: the team that gates this stage plus optional deterministic
 *  evaluators. The team (a data plugin, e.g. "design-jury") owns the rubric. */
interface EnginePipelineCritic {
    readonly team?: string;
    readonly evaluators?: readonly string[];
}
/** One node of the Run graph a Pipeline unrolls into. */
interface EnginePipelineStage {
    readonly id: string;
    readonly title: string;
    readonly artifactory: string;
    readonly hosted?: boolean;
    readonly artifactClass: "deliverable" | "intermediate" | "conditioning";
    readonly needs?: readonly string[];
    readonly until?: EnginePipelineStageUntil;
    readonly branch?: {
        readonly directions: number;
    };
    /** What this stage collects from the human before producing anything — a plain
     *  instruction the agent turns into ONE `ask` tool call, in the session the
     *  person is watching. Replaced an `EnginePipelineGate` mapping and its
     *  suspending node (deleted 2026-07-29): the pipeline says WHAT to collect and
     *  the `ask` tool owns HOW to render it, so there is exactly one asking path. */
    readonly ask?: string;
    readonly critic?: EnginePipelineCritic;
    /** Skills this stage loads when active (agent knowledge). */
    readonly skills?: readonly string[];
    /** Tool ids this stage may use on top of the Kind pack's tools. */
    readonly tools?: readonly string[];
}
/** A terminal pipeline stage: how the Run ships. */
interface EnginePipelineDistribution {
    readonly verb: "publish" | "deploy" | "export" | "share" | "submit-for-review" | "set-active";
    readonly target?: string;
}
/** A context chip the pick brings to the composer. */
interface EnginePipelineContextChip {
    readonly kind: "design-system" | "brand-kit" | "assets" | "connector" | "tool" | "command" | "mcp" | "plugin";
    readonly ref: string;
    readonly sections?: readonly string[];
}
/** One loaded Pipeline from the engine catalog (`*.pipeline.md` files shipped by
 *  plugins or dropped in the workspace; project wins on id collision). */
interface EnginePipelineRecord {
    readonly specVersion: number;
    readonly id: string;
    readonly title: string;
    readonly description: string;
    readonly icon: string;
    /** Card art as a data URI, when the pipeline ships one. */
    readonly artUrl?: string;
    readonly seed: string;
    readonly brings?: readonly EnginePipelineContextChip[];
    /** Skills the run applies (agent knowledge; loaded into run conditioning). */
    readonly skills?: readonly string[];
    readonly stages: readonly EnginePipelineStage[];
    readonly distribution: readonly EnginePipelineDistribution[];
    /** The compiled shape — the same nodes and edges a run would traverse.
     *  Absent on an engine that predates it; a surface must degrade to the
     *  stage list rather than inventing a graph. */
    readonly graph?: PipelineGraph;
    readonly faces?: readonly PipelineNodeFace[];
    /** Agent-facing run instructions (the file's markdown body). */
    readonly body: string;
    readonly source: "project" | "plugin";
    readonly pluginId?: string;
}
/** A pipeline file that failed to parse/validate — it fails alone. */
interface EnginePipelineIssue {
    readonly file: string;
    readonly pluginId?: string;
    readonly errors: readonly string[];
}
/** Canonical design dimensions a DESIGN.md may cover — mirrors the engine's
 *  `DesignDimension` (design-md.ts), reproduced like every other closed union
 *  in this file so consumers keep autocomplete/exhaustiveness. */
type EngineDesignDimension = "theme" | "color" | "typography" | "components" | "layout" | "depth" | "responsive" | "anti-patterns" | "guide";
/** One design system from the engine catalog (`design-systems/<id>/DESIGN.md`
 *  shipped by plugins or dropped in the project `.fraym/`; project wins on id
 *  collision). Mirrors the engine's `DesignSystemRecord`. */
interface EngineDesignSystemRecord {
    readonly id: string;
    readonly title: string;
    /** Raw `##` headings the DESIGN.md declares (numbering stripped). */
    readonly sections: readonly string[];
    /** Canonical dimensions covered — a pipeline's `brings` chip prunes to these. */
    readonly dimensions: readonly EngineDesignDimension[];
    readonly source: "project" | "plugin";
    readonly pluginId?: string;
    /** Absolute path to the DESIGN.md on the engine's machine. */
    readonly path: string;
}
/** One member of a team's roster: an LLM juror (backed by an agent) or a
 *  deterministic code check (backed by an evaluator). */
interface EngineTeamMemberRecord {
    readonly name: string;
    readonly kind: "llm" | "deterministic";
    readonly weight: number;
    /** The juror agent name (llm) or evaluator id (deterministic) this member runs. */
    readonly ref: string;
    readonly label: string;
    readonly description: string;
}
/** One bundled team (jury) — a named roster + fold strategy + gate threshold.
 *  Static catalog data, distinct from {@link EngineTeamToolDetails} (the runtime
 *  verdict a `team` call returns). */
interface EngineTeamRecord {
    readonly id: string;
    readonly strategy: string;
    readonly strategyLabel: string;
    readonly strategyDescription: string;
    readonly threshold: number;
    readonly members: readonly EngineTeamMemberRecord[];
}
/** A deterministic evaluator a pipeline stage can thread onto a team's roster. */
interface EngineEvaluatorRecord {
    readonly id: string;
    readonly label: string;
    readonly description: string;
}
/** An artifactory PACK a plugin contributes via `dimension.plugin.json`
 *  `artifactories[]` (doc 44 §15, 2026-07-17 transport ruling: the renderer
 *  bundle runs in a sandboxed iframe, served by the engine's `/plugin-assets`
 *  route; `@fraym/ui`'s bridged host adapts it into the stage registry).
 *  Mirrors the engine's `EngineArtifactoryRecord`. */
interface EngineArtifactoryRecord {
    readonly id: string;
    readonly pluginId: string;
    readonly contractVersion: number;
    readonly label: string;
    readonly icon?: string;
    readonly extensions: readonly string[];
    /** Plugin-root-relative bundle path; the client composes
     *  `GET /plugin-assets/<pluginId>/<bundle>` with the engine token. */
    readonly bundle: string;
    readonly signals?: readonly string[];
}

/** Attribution confidence for one session's relationship to a checkout. A shared
 * checkout is never claimed as exclusively owned by this session. */
type SessionScmConfidence = "exclusive" | "shared" | "inferred";
interface SessionScmCheckout {
    readonly dirtyCount: number;
    readonly ahead: number;
    readonly behind: number;
}
interface SessionScmCommit {
    readonly sha: string;
    readonly subject?: string;
    readonly pushed: boolean;
    readonly confidence: SessionScmConfidence;
}
/** Bounded first-paint projection of a canonical session SCM ledger. */
interface SessionScmLedgerSummary {
    readonly available: boolean;
    readonly checkoutRoot?: string;
    readonly baselineHead?: string;
    readonly branch?: string;
    readonly exclusive: boolean;
    readonly confidence: SessionScmConfidence;
    readonly touchedCount: number;
    readonly recentTouchedPaths: readonly string[];
    readonly recentCommits: readonly SessionScmCommit[];
    readonly committedCount: number;
    readonly pushedCount: number;
    readonly unpushedCount: number;
    readonly checkout: SessionScmCheckout;
    readonly finished?: boolean;
    readonly updatedAt: Timestamp;
}

type WorkspaceId = string;
type SessionId = string;
type RunId = string;
type Timestamp = string;
/** Per-project rail appearance, surfaced by the engine from
 *  `<project>/.inso/config.json` (project) over `~/.inso/config.json` (user)
 *  under the `appearance` key. Render precedence: image > emoji > icon > folder. */
interface WorkspaceAppearance {
    /** A built-in Fraym icon glyph name. */
    readonly icon?: string;
    /** A renderable image src (`data:`/`http(s):` URL; local paths are inlined engine-side). */
    readonly image?: string;
    /** A short emoji marker (rendered as text when no image is set). */
    readonly emoji?: string;
    /** CSS color for the rail status dot / accent. */
    readonly color?: string;
}
interface WorkspaceRef {
    readonly workspaceId: WorkspaceId;
    readonly path: string;
    readonly displayName?: string;
    /** Per-project rail appearance (icon/image/emoji/color) from project config. */
    readonly appearance?: WorkspaceAppearance;
    readonly git?: {
        readonly currentBranch?: string;
        readonly branches?: readonly string[];
        readonly dirty?: boolean;
        /** True when this checkout is a linked git worktree (not the main one). */
        readonly worktree?: boolean;
    };
}
interface SessionRef {
    readonly workspaceId: WorkspaceId;
    readonly sessionId: SessionId;
    /** Body (harness) that owns this session — e.g. `"claude-code"`. The ROUTING
     *  identity: a dispatcher needs to know which body a call belongs to *before*
     *  it can pick a driver, so it cannot live on `SessionSnapshot` (which is a
     *  result of having already picked one). See `docs/design/48-pluggable-harness.md`
     *  §5.D.
     *
     *  Absent = the host default body, which is every session today — the field is
     *  additive and changes no behaviour on its own.
     *
     *  NOT part of session identity: `sessionKey()` stays `workspaceId:sessionId`.
     *  A session has exactly one body for life, so keying on the harness would let
     *  a ref that omits the hint fork the cache for a session that already exists. */
    readonly harness?: string;
}
type SessionStatus = "idle" | "running" | "failed";
type SessionMessageDeliveryMode = "steer" | "followUp";
interface SessionQueuedMessage {
    /** Stable key for this queue row. It is the composer's `clientMessageId` when the
     *  host reported one; otherwise the adapter invents a POSITIONAL label and sets
     *  {@link synthesizedId}. Always safe as a React key — see `synthesizedId` before
     *  using it as a durable identity. */
    readonly id: string;
    /** True when {@link id} is an adapter-invented positional label (`${mode}-${index}`)
     *  rather than a real composer identity, because the host sent no `clientMessageId`
     *  for this entry. Such a label is re-derived on every `queue_changed`, so two
     *  DIFFERENT messages occupying the same queue index over a session share it — it
     *  must never be sent as a `clientMessageId`, which the journal treats as a durable
     *  identity and REPLACES rows by. */
    readonly synthesizedId?: boolean;
    readonly mode: SessionMessageDeliveryMode;
    readonly text: string;
    readonly attachments?: readonly SessionAttachment[];
    readonly createdAt: Timestamp;
    readonly updatedAt: Timestamp;
}
interface SessionSnapshot {
    readonly ref: SessionRef;
    readonly workspace: WorkspaceRef;
    readonly title: string;
    readonly status: SessionStatus;
    readonly updatedAt: Timestamp;
    readonly archivedAt?: Timestamp;
    readonly pinnedAt?: Timestamp;
    /** Agent profile id the session was created under (e.g. "designer") — the durable
     *  session kind, persisted on the engine's session header and surfaced on listings
     *  as `_meta["inso/profile"]`. Hosts scope bench catalogs on it. */
    readonly profile?: string;
    /** Session origin: `"autonomy"` marks a self-running Loop tick (scheduler-fired),
     *  absent/`"user"` marks a human-driven session. Threaded from the engine
     *  (`EngineSessionSnapshot.source`) and, over ACP, from the listing's
     *  `_meta["inso/source"]` beside `inso/profile`. The session rail + Loops group
     *  classify loop runs by this; the `loop: ` title prefix is display + fallback. */
    readonly source?: "user" | "autonomy";
    readonly preview?: string;
    readonly config?: SessionConfig;
    readonly runningRunId?: RunId;
    readonly queuedMessages?: readonly SessionQueuedMessage[];
    readonly contextUsage?: ContextUsage;
    readonly tasks?: readonly TaskPhase[];
    readonly workingStatus?: WorkingStatus;
    readonly planMode?: PlanModeState;
    readonly goal?: Goal | null;
    readonly modes?: readonly ModeState[];
    readonly subagentBatches?: readonly SubagentBatch[];
    readonly isCompacting?: boolean;
    readonly hasBackgroundWork?: boolean;
    readonly loaded?: boolean;
    readonly reconnecting?: boolean;
    readonly blockedOnInput?: boolean;
    readonly recap?: {
        readonly text: string;
        readonly generatedAt: string;
    };
    /** Durable SCM facts attributed from this session's custom journal entries.
     * This is explicitly session-scoped and never substitutes for checkout-wide
     * source-control state. */
    readonly scmLedger?: SessionScmLedgerSummary;
}
interface SessionImageAttachment {
    readonly kind: "image";
    readonly mimeType: string;
    readonly data: string;
    readonly name?: string;
}
interface SessionFileAttachment {
    readonly kind: "file";
    readonly name: string;
    readonly mimeType: string;
    readonly fsPath: string;
    readonly sizeBytes?: number;
}
type SessionAttachment = SessionImageAttachment | SessionFileAttachment;
interface SessionConfig {
    readonly provider?: string;
    readonly modelId?: string;
    readonly thinkingLevel?: string;
    /** Selectable thinking-level option values offered by the engine (e.g. off, auto, minimal…xhigh). */
    readonly thinkingLevels?: readonly string[];
    readonly approvalMode?: string;
    /** Selectable approval-mode option values offered by the engine (always-ask, write, yolo). */
    readonly approvalModes?: readonly string[];
    readonly ephemeral?: boolean;
    /** Session-scoped model profile applied via `setSessionProfile`; null/absent = config defaults. */
    readonly activeProfile?: string | null;
    /**
     * The models THIS session's body offers, when the body owns its own catalog
     * (doc 48 §2.4). Present only for a body that advertised `models` on
     * `session/new` — a third-party ACP agent whose ids are its own (`default`,
     * `sonnet`, `haiku`) and which would reject one of ours. Absent on our own
     * engine, where the global model registry is authoritative.
     *
     * A UI offering a model picker MUST prefer this when present: the catalog is
     * scoped to the BODY rather than shown as one global list, because a body's ids
     * are its own and it will reject a cross-family id outright.
     */
    readonly bodyModels?: readonly {
        readonly id: string;
        readonly label: string;
        readonly description?: string;
    }[];
}
interface SessionTranscriptToolCall {
    readonly callId: string;
    readonly toolName: string;
    readonly input?: unknown;
    readonly status: "running" | "success" | "error";
    readonly text?: string;
    readonly progress?: number;
    readonly output?: unknown;
    readonly metadata?: ToolCallMetadata;
}
/** A transcript block.
 *
 *  `id` is STABLE IDENTITY (contract C4): the journal builder mints it once, where
 *  the block is created, and it survives the durable re-derivation at settle and the
 *  `tokenUsage` splice. Consumers key React on it and MUST NOT key on the array
 *  index — the block array is rebuilt on every settle and a splice renumbers every
 *  position after it, which remounts live content and restarts its reveal.
 *
 *  It is OPTIONAL on the wire, and today absence is the RULE rather than the
 *  exception: no driver stamps it yet. `driver-acp`'s `journal-core.ts` is where it
 *  will be minted; `driver-codex` and `driver-test` are independent implementors that
 *  would each need their own. So the positional fallback is not a rare migration path
 *  for pre-field IndexedDB snapshots — it is currently the only path taken, and the
 *  "MUST NOT key on the array index" rule above is the target state, not a followable
 *  instruction: a consumer obeying it literally today keys every block on `undefined`.
 */
type SessionTranscriptBlock = 
/** `interruptedBy` names the rule(s) whose TTSR match aborted this text mid-stream
 *  (engine `stopReason: "aborted"` + `errorMessage: "TTSR matched rule…"`). The engine
 *  discards the partial from model context and regenerates, so the draft is dead prose:
 *  present for the record, but the UI MUST NOT render it as ordinary output beside the
 *  regenerated answer — that is the near-duplicate-summary defect. */
{
    readonly type: "text";
    readonly text: string;
    readonly id?: string;
    readonly interruptedBy?: readonly string[];
} | {
    readonly type: "reasoning";
    readonly text: string;
    readonly id?: string;
} | {
    readonly type: "tool";
    readonly call: SessionTranscriptToolCall;
    readonly id?: string;
}
/** Slash-command output. `renderKind` is the engine-neutral routing key the
 *  adapter stamps (LAW 2) — the UI binds chips/panels to it, never to
 *  engine command names. */
 | {
    readonly type: "command";
    readonly command: string;
    readonly text: string;
    readonly renderKind: string;
    readonly invocation?: string;
    readonly id?: string;
}
/** One assistant response's token usage, placed after its text and above its tool calls — mirrors the TUI's per-message footer. */
 | {
    readonly type: "tokenUsage";
    readonly usage: TurnUsage;
    readonly id?: string;
}
/** An inline image (pasted/attached by the user, or an agent capture). `src` is a URL or a `data:` URL.
 *  `unavailable` marks an image whose pixels never reached the client — an unresolved blob ref, a
 *  blob missing at replay, or an oversized inline payload — so the UI shows a placeholder instead of
 *  silently dropping it (which stranded a dead `[Image #N]` text marker). Then `src` is empty. */
 | {
    readonly type: "image";
    readonly src: string;
    readonly alt?: string;
    readonly caption?: string;
    readonly unavailable?: boolean;
    readonly id?: string;
}
/** A run-ending error with no other renderable content — the transcript builder synthesizes this so an errored-empty turn surfaces via the UI notice card instead of a silent empty bubble. */
 | {
    readonly type: "notice";
    readonly level: "info" | "warning" | "error";
    readonly message: string;
    readonly source?: string;
    readonly id?: string;
};
interface SessionTranscriptMessage {
    /** The row's PERMANENT React/reconciliation identity. It is assigned when the row
     *  is first published and NEVER reassigned — an agent turn opened under a
     *  provisional `live:*` id keeps that id for the whole session, because the view
     *  keys on it and a rename unmounts and remounts the entire turn mid-stream
     *  (defect A4: `driver-codex` sends no `messageId`, so every Codex turn rekeyed).
     *  The engine's durable name for the same row rides on {@link durableId}. */
    readonly id: string;
    readonly role: "user" | "agent" | "divider";
    readonly blocks: readonly SessionTranscriptBlock[];
    /** The journal's durable entry id for this row, when the row was opened under a
     *  provisional id and later reconciled to a durable assistant message. Absent when
     *  `id` is already the durable name (every Law 2 body). Anything that correlates a
     *  row to the journal — branch/leaf ancestry, durable ordering — MUST read
     *  `durableId ?? id`; anything that reconciles React identity MUST read `id`.
     *
     *  NOT YET PRODUCED. No driver stamps this today — `driver-acp`'s `journal-core.ts`
     *  will mint it, and `driver-codex` / `driver-test` are independent implementors
     *  that would each need their own. The declaration landed ahead of its producer, so
     *  until then `durableId ?? id` always takes the `id` branch. Read that as "absent
     *  means always", not "absent means legacy". */
    readonly durableId?: string;
    /** Explicit turn phase (contract C5). `"streaming"` while the builder's turn is
     *  open, `"settled"` once `flushAgentTurn`/`materializeAgentTurn` stamped it —
     *  a STATED fact, never inferred from array position, trailing-row-ness, or the
     *  session-global `isStreaming`. Agent turn rows only; absent on user rows,
     *  dividers, command rows, custom-message rows, and pre-field persisted snapshots.
     *
     *  NOT YET PRODUCED — same caveat as {@link durableId}. A consumer switching on it
     *  today sees `undefined` for every row, so it must not be used as the sole signal
     *  for whether a turn is still streaming. */
    readonly turnPhase?: "streaming" | "settled";
    /** Builder-owned open projection awaiting its settled journal entry. */
    readonly live?: boolean;
    /** Opaque id minted by the ACP driver for prompt-recovery idempotency. */
    readonly clientMessageId?: string;
    /** Divider rendering (role "divider" only) — e.g. the compaction marker the
     *  adapter builds from the journaled `compaction` entry, or the session
     *  continuation marker built from the journaled `session_continuation`
     *  custom entry. Mirrors the UI's divider fields so journal-built dividers
     *  survive refresh and reopen. */
    readonly variant?: "compacting" | "done" | "continuation";
    readonly auto?: boolean;
    readonly freed?: number;
    /** Pre-compaction token count ("Compacted from N tokens"). */
    readonly tokens?: number;
    /** One-line summary of what was compacted. */
    readonly summary?: string;
    /** Continuation divider (variant "continuation"): the successor session. */
    readonly toSessionId?: string;
    /** Continuation provenance: "handoff" | "new" | "plan" | "sidequest" | … */
    readonly reason?: string;
    readonly timestamp?: Timestamp;
    readonly meta?: string;
    /** Engine custom-message type; the UI keys a `msg:<customType>` renderer on it. */
    readonly customType?: string;
    /** Structured payload for the `msg:<customType>` renderer (Engine custom message `details`, render-safe). */
    readonly payload?: unknown;
    readonly durationMs?: number;
    /** A persisted assistant response reached a terminal stop reason. */
    readonly settled?: boolean;
    readonly final?: boolean;
}
/** One assistant response's token usage (NOT summed across the turn). Mirrors the TUI's per-message readout. */
interface TurnUsage {
    /** Fresh prompt tokens (the TUI shows `input + cacheWrite`). */
    readonly input: number;
    /** Completion tokens. */
    readonly output: number;
    /** Prompt-prefix tokens served from cache. */
    readonly cacheRead: number;
    /** Tokens written to cache (billed as input). */
    readonly cacheWrite: number;
    /** Time to first token (ms). Omitted when the engine didn't report one (e.g. a cached/instant response). */
    readonly ttftMs?: number;
    /** Whole-response wall-clock duration (ms), including TTFT. Used with `ttftMs` to derive a throughput readout. */
    readonly durationMs?: number;
}
interface SessionJournalCustomMessage {
    readonly id: string;
    readonly text: string;
    readonly payload?: unknown;
}
type SessionTreeNodeKind = "message" | "thinking_level_change" | "model_change" | "compaction" | "branch_summary" | "custom" | "custom_message" | "label" | "session_info";
interface SessionTreeNodeSnapshot {
    readonly id: string;
    readonly parentId: string | null;
    readonly kind: SessionTreeNodeKind;
    readonly timestamp: Timestamp;
    readonly label?: string;
    readonly role?: string;
    readonly customType?: string;
    readonly title: string;
    readonly preview?: string;
    readonly children: readonly SessionTreeNodeSnapshot[];
}
interface SessionTreeSnapshot {
    readonly roots: readonly SessionTreeNodeSnapshot[];
    readonly leafId: string | null;
}
interface NavigateSessionTreeOptions {
    readonly summarize?: boolean;
    readonly customInstructions?: string;
}
interface NavigateSessionTreeResult {
    readonly cancelled: boolean;
    readonly aborted?: boolean;
    readonly editorText?: string;
    readonly editorImages?: readonly {
        readonly data: string;
        readonly mimeType: string;
    }[];
    readonly summaryCreated?: boolean;
}
/** Result of forking a session (see {@link SessionDriver.forkSession}). */
interface ForkSessionResult {
    /** The newly-created forked session — the caller opens/activates it. */
    readonly snapshot: SessionSnapshot;
    /** Composer prefill text: set when forking FROM a user message (its text, as the fork
     *  lands just before it), absent when forking from an agent message (kept in place). */
    readonly editorText?: string;
    /** Composer prefill image attachments (tray thumbnails), restored alongside `editorText`
     *  when forking FROM a user message that carried images. */
    readonly editorImages?: readonly {
        readonly data: string;
        readonly mimeType: string;
    }[];
}
interface SessionModelSelection {
    readonly provider: string;
    readonly modelId: string;
}
/** Result of a TUI-parity role-model cycle (Ctrl+P). */
interface SessionRoleCycleResult {
    /** The role the session landed on (e.g. "slow"). */
    readonly role: string;
    /** The full cycle order, for rendering the segment track. */
    readonly order: readonly string[];
    readonly provider: string;
    readonly modelId: string;
    readonly thinkingLevel?: string | null;
}
/** Result of a session-scoped model-profile switch. */
interface SessionProfileSwitchResult {
    /** The now-active profile name; null = config defaults. */
    readonly profile: string | null;
    readonly provider?: string | null;
    readonly modelId?: string | null;
}
/** Engine on-device STT: the configured model + whether it is on disk yet. */
interface SpeechToTextStatus {
    readonly modelKey: string;
    /** False = first use will download the model (the stream reports progress). */
    readonly cached: boolean;
    /** Human label for the tier, e.g. "Parakeet (NVIDIA TDT 0.6B v3)". Absent on
     *  engines that predate the setup surface. */
    readonly label?: string;
    /** Approximate on-disk size, e.g. "~680 MB" — what a first download costs.
     *  Absent on engines that predate the setup surface. */
    readonly sizeHint?: string;
}
/** Push events for one engine STT capture stream. */
interface SpeechToTextEvents {
    /** The worker is live — audio fed from here on is transcribed. */
    readonly onReady?: () => void;
    /** Model download progress while the stream prepares (first use). `percent`
     *  is 0-100 across the whole tier, absent until byte counts arrive;
     *  `loaded`/`total` are bytes, so a 680 MB fetch can show real magnitude. */
    readonly onProgress?: (progress: {
        readonly label?: string;
        readonly percent?: number;
        readonly loaded?: number;
        readonly total?: number;
    }) => void;
    /** Volatile transcript of the in-progress segment (refreshed in place). */
    readonly onPartial?: (text: string) => void;
    /** A finalized segment committed by the engine's endpointer. */
    readonly onSegment?: (text: string, index: number) => void;
    /** The stream failed engine-side; it is torn down. */
    readonly onError?: (message: string) => void;
}
/** A live engine STT capture stream. */
interface SpeechToTextHandle {
    /** Feed one chunk of 16 kHz mono s16le PCM. */
    readonly sendAudio: (pcm: Int16Array) => void;
    /** Flush the trailing segment; resolves the full joined transcript. */
    readonly stop: () => Promise<string>;
    /** Tear the stream down without a final flush. */
    readonly cancel: () => void;
}
interface SessionMessageInput {
    readonly text: string;
    /** Model-facing prompt override (ACP `_meta["inso/expansion"]`): when present,
     *  the engine sends THIS to the model and journals `text` as the message's
     *  typed face (`displayText`) — the slash-command expansion convention. Used
     *  by pipeline runs so the transcript shows only what the user said. */
    readonly expansion?: string;
    readonly attachments?: readonly SessionAttachment[];
    readonly deliverAs?: SessionMessageDeliveryMode;
    /** Stable id shared with a caller's optimistic user echo, when it has one. */
    readonly clientMessageId?: string;
}
interface CreateSessionOptions {
    readonly title?: string;
    readonly initialModel?: SessionModelSelection;
    readonly initialThinkingLevel?: string;
    /** Agent profile id for this session (e.g. "aether"). Forwarded to the host so it can
     *  activate per-agent faculties; carried over ACP as `_meta["inso/profile"]`. */
    readonly profile?: string;
    /** Body (harness) to create this session ON — e.g. `"claude-code"`. Carried over
     *  ACP as `_meta["inso/harness"]` and stamped WRITE-ONCE on the host's session
     *  header, because the body process is spawned on the first turn and a later
     *  swap would orphan it (doc 48 §2.4). Absent = the host default body. */
    readonly harness?: string;
}
/** A selectable General Agent, for the creation-time agent picker. `name` is the
 *  value `createSession` takes as `profile` (→ ACP `_meta["inso/profile"]`). */
interface AgentChoice {
    readonly name: string;
    readonly description: string;
    readonly source: "bundled" | "user" | "project";
    readonly hasManifest: boolean;
}
/** A selectable BODY, for the creation-time harness picker (doc 48 §6). `id` is
 *  the value `createSession` takes as `harness` (→ ACP `_meta["inso/harness"]`).
 *  A host offers only bodies it can actually spawn, and omits its own reference
 *  body — running on the default is what creating a session already does. */
interface HarnessChoice {
    readonly id: string;
    readonly label: string;
    /** One-line hint under the label, e.g. the vendor or the tier. */
    readonly detail?: string;
}
interface SessionEventBase {
    readonly type: string;
    readonly sessionRef: SessionRef;
    readonly timestamp: Timestamp;
    readonly runId?: RunId;
}
interface SessionOpenedEvent extends SessionEventBase {
    readonly type: "sessionOpened";
    readonly snapshot: SessionSnapshot;
}
interface SessionJournalUpdatedEvent extends SessionEventBase {
    readonly type: "sessionJournalUpdated";
    readonly seq: number;
    readonly transcript: readonly SessionTranscriptMessage[];
    /** The sender rebuilt the complete transcript snapshot; consumers replace rather than merge it. */
    readonly replace?: boolean;
    /** The same durable ledger carried on the session snapshot, emitted with a
     * journal update so adapters can patch the snapshot live. `null` is an
     * explicit current-branch absence; omitted means this sender had no ledger lane. */
    readonly scmLedger?: SessionScmLedgerSummary | null;
    readonly customMessages: Readonly<Record<string, SessionJournalCustomMessage>>;
    /** Present only for a negotiated bounded first paint; absence retains legacy
     * complete-replay semantics. */
    readonly history?: SessionHistoryWindow;
}
interface SessionUpdatedEvent extends SessionEventBase {
    readonly type: "sessionUpdated";
    readonly snapshot: SessionSnapshot;
}
interface AssistantDeltaEvent extends SessionEventBase {
    readonly type: "assistantDelta";
    readonly text: string;
    /** When set, `text` is the FULL accumulated text of the live assistant
     *  segment (engine-authoritative), not an increment: consumers replace the
     *  live row's trailing text instead of appending. Emitted once per
     *  hydration when a client opens/reconnects to a mid-turn session — the
     *  journal only holds settled messages, so this is how the already-streamed
     *  head of the reply reaches a late subscriber. */
    readonly replace?: boolean;
    /** Durable journal row id this chunk belongs to (ACP `ContentChunk.messageId`).
     *  Absent from engines that predate server-named rows. */
    readonly messageId?: string;
    /** Position in the session's ONE monotonic order, drawn from the same journal
     *  counter durable rows use and carried as `_meta["inso/seq"]` (doc 49 Law 1's
     *  ephemeral tier, sub-step 3.2c). Numbered and broadcast, NEVER written — so
     *  it is not a journal position and nothing may derive one from it. Absent from
     *  engines that predate the two-tier log. */
    readonly seq?: number;
}
interface ThinkingDeltaEvent extends SessionEventBase {
    readonly type: "thinkingDelta";
    readonly text: string;
    /** Durable journal row id this chunk belongs to (ACP `ContentChunk.messageId`).
     *  Absent from engines that predate server-named rows. */
    readonly messageId?: string;
    /** Position in the session's ONE monotonic order — see {@link AssistantDeltaEvent.seq}. */
    readonly seq?: number;
}
interface TurnEndedEvent extends SessionEventBase {
    readonly type: "turnEnded";
    /** Whole-run wall-clock so far (ms), for the "Worked for Xs" label. */
    readonly durationMs?: number;
    /** True on the final turn — the assistant message that ran no tool calls and ended the run. */
    readonly final?: boolean;
}
interface QueuedMessageStartedEvent extends SessionEventBase {
    readonly type: "queuedMessageStarted";
    readonly message: SessionQueuedMessage;
}
interface ToolStartedEvent extends SessionEventBase {
    readonly type: "toolStarted";
    readonly toolName: string;
    readonly callId: string;
    readonly input?: unknown;
    readonly displayName?: string;
    /** Durable journal row id this tool call belongs to, carried over ACP via
     *  `_meta["inso/messageId"]` (ACP's `ToolCall` is not a `ContentChunk`, so it
     *  has no `messageId` field of its own).
     *
     *  A tool call is not its own journal row — it is a content part inside the
     *  assistant `message`, so this is that row's id, and it matches the
     *  `messageId` on the same turn's `assistantDelta`s. Absent from engines that
     *  predate server-named rows, which is the only case a client still has to
     *  name a turn itself. */
    readonly messageId?: string;
}
interface ToolUpdatedEvent extends SessionEventBase {
    readonly type: "toolUpdated";
    readonly callId: string;
    readonly text?: string;
    readonly progress?: number;
    readonly partialResult?: unknown;
    readonly partialInput?: unknown;
    readonly metadata?: ToolCallMetadata;
}
interface ToolCallMetadata {
    readonly durationMs?: number;
    readonly tokens?: number;
    readonly cachedTokens?: number;
    /** Extra engine-provided facts shown inline (exit code, match count, …). */
    readonly extra?: Readonly<Record<string, string | number>>;
}
interface ToolFinishedEvent extends SessionEventBase {
    readonly type: "toolFinished";
    readonly callId: string;
    readonly success: boolean;
    readonly output?: unknown;
    readonly metadata?: ToolCallMetadata;
}
interface RunCompletedEvent extends SessionEventBase {
    readonly type: "runCompleted";
    readonly snapshot: SessionSnapshot;
}
interface SessionErrorInfo {
    readonly message: string;
    readonly code?: string;
    readonly details?: unknown;
}
interface ExtensionCompatibilityIssue {
    readonly capability: string;
    readonly classification: "terminal-only";
    readonly message: string;
    readonly extensionPath?: string;
    readonly eventName?: string;
}
interface RunFailedEvent extends SessionEventBase {
    readonly type: "runFailed";
    readonly error: SessionErrorInfo;
}
type HostUiResponse = {
    readonly requestId: string;
    readonly value: string;
} | {
    readonly requestId: string;
    readonly values: readonly string[];
} | {
    readonly requestId: string;
    readonly confirmed: boolean;
} | {
    readonly requestId: string;
    readonly optionId: string;
} | {
    readonly requestId: string;
    readonly navigate: "back" | "forward";
} | {
    readonly requestId: string;
    readonly cancelled: true;
};
/**
 * A rich select option. `ui.select` callers may pass plain strings (back-compat)
 * or `{ label, description }` objects; the host UI renders the description as
 * dim sub-text below the label. The Engine `ask` tool bakes a `" (Recommended)"`
 * suffix into the label of the recommended option — the picker strips it for
 * display and shows a badge.
 */
interface HostUiSelectOption {
    readonly label: string;
    readonly description?: string;
}
type HostUiRequest = {
    readonly kind: "confirm";
    readonly requestId: string;
    readonly title: string;
    readonly message: string;
    readonly defaultValue?: boolean;
    readonly timeoutMs?: number;
    readonly meta?: Readonly<Record<string, unknown>>;
} | {
    readonly kind: "input";
    readonly requestId: string;
    readonly title: string;
    readonly placeholder?: string;
    readonly initialValue?: string;
    readonly timeoutMs?: number;
    readonly meta?: Readonly<Record<string, unknown>>;
} | {
    readonly kind: "select";
    readonly requestId: string;
    readonly title: string;
    readonly options: readonly (string | HostUiSelectOption)[];
    readonly meta?: Readonly<Record<string, unknown>>;
    readonly allowMultiple?: boolean;
    readonly selectionMarker?: "radio" | "checkbox";
    readonly markableCount?: number;
    readonly checkedIndices?: readonly number[];
    readonly initialIndex?: number;
    readonly canNavigateBack?: boolean;
    readonly canNavigateForward?: boolean;
    readonly timeoutMs?: number;
} | {
    readonly kind: "editor";
    readonly requestId: string;
    readonly title: string;
    readonly initialValue?: string;
} | {
    readonly kind: "notify";
    readonly requestId: string;
    readonly message: string;
    readonly level?: "info" | "warning" | "error";
} | {
    readonly kind: "status";
    readonly requestId: string;
    readonly key: string;
    readonly text?: string;
} | {
    readonly kind: "widget";
    readonly requestId: string;
    readonly key: string;
    readonly lines?: readonly string[];
    readonly placement?: "aboveComposer" | "belowComposer";
} | {
    readonly kind: "title";
    readonly requestId: string;
    readonly title: string;
} | {
    readonly kind: "editorText";
    readonly requestId: string;
    readonly text: string;
} | {
    readonly kind: "reset";
    readonly requestId: string;
} | {
    readonly kind: "permission";
    readonly requestId: string;
    readonly toolCallId: string;
    readonly toolName: string;
    readonly title: string;
    readonly paths?: readonly string[];
    readonly locations?: readonly PermissionLocation[];
    readonly options: readonly PermissionOption[];
};
interface HostUiRequestEvent extends SessionEventBase {
    readonly type: "hostUiRequest";
    readonly request: HostUiRequest;
}
interface ExtensionCompatibilityIssueEvent extends SessionEventBase {
    readonly type: "extensionCompatibilityIssue";
    readonly issue: ExtensionCompatibilityIssue;
}
interface SessionClosedEvent extends SessionEventBase {
    readonly type: "sessionClosed";
    readonly reason: "manual" | "ended" | "failed" | "deleted";
}
type SessionDriverEvent = SessionOpenedEvent | SessionUpdatedEvent | SessionJournalUpdatedEvent | AssistantDeltaEvent | ThinkingDeltaEvent | TurnEndedEvent | QueuedMessageStartedEvent | ToolStartedEvent | ToolUpdatedEvent | ToolFinishedEvent | RunCompletedEvent | RunFailedEvent | HostUiRequestEvent | ExtensionCompatibilityIssueEvent | SessionClosedEvent | TasksUpdatedEvent | ContextUsageEvent | QueuedMessagesChangedEvent | BackgroundWorkChangedEvent | RunningStatusChangedEvent | SessionLoadedChangedEvent | WorkingStatusEvent | PlanModeChangedEvent | GoalChangedEvent | ThinkingLevelChangedEvent | CompactionStartedEvent | CompactionFinishedEvent | NoticeEvent | CustomMessageEvent | CommandResultEvent | CommandsUpdatedEvent | RetryEvent | TtsrTriggeredEvent | SessionSwitchedEvent | SubagentBatchStartedEvent | SubagentRunUpdatedEvent | SubagentBatchFinishedEvent | ModeStateChangedEvent;
type SessionEventListener = (event: SessionDriverEvent) => void | Promise<void>;
type Unsubscribe = () => void;
interface SessionBoundedDetailPreview {
    readonly items: readonly unknown[];
    readonly maxItems: number;
    readonly maxBytes: number;
    readonly hasMore: boolean;
    readonly detailCursor?: string;
}
/** Versioned, explicitly bounded first-paint data. It is a preview, never a
 * complete SCM/tree fold; use independent detail cursors for exhaustive views. */
interface SessionBoundedFirstPaintState {
    readonly version: 1;
    readonly scm: SessionBoundedDetailPreview;
    readonly recap: SessionBoundedDetailPreview;
    readonly branchNeighborhood: SessionBoundedDetailPreview;
}
/** Authoritative bounded durable transcript coverage. `durableHead` is durable
 * ordering only; live event watermarks deliberately remain outside this shape. */
interface SessionHistoryWindow {
    readonly durableHead: number;
    readonly interval: {
        readonly startInclusive: number;
        readonly endInclusive: number;
    };
    readonly leafId: string | null;
    readonly branchGeneration: number;
    readonly projectionGeneration: number;
    /** Schema version of the durable reducers that produced this projection. */
    readonly reducerVersion: number;
    readonly scmLedger?: SessionScmLedgerSummary | null;
    readonly scmLedgerVersion?: 1;
    readonly hasMoreBefore: boolean;
    readonly beforeCursor?: string;
    readonly oversize?: boolean;
    /** The oldest durable ordinal the TRANSCRIPT published beside this window actually
     * covers, stated only when the publisher is carrying rows BELOW
     * `interval.startInclusive` — a paged span a same-lattice window slide walked past.
     * Absent when the two coincide, which is every first paint and every rebase.
     *
     * Published because `beforeCursor` and `interval` answer different questions: the
     * cursor is minted for the publisher's paging FLOOR, never for the window start.
     * A consumer that re-derives the floor from `interval` therefore disagrees with the
     * cursor by exactly the retained span and refuses every page the engine serves —
     * and a consumer with NO prior state (first paint, a remount, an app restart) has
     * nothing to re-derive from at all, which is when that disagreement is permanent
     * (the 2026-07-31 restart loop: window 1003-1057, cursor paging below 1001, the
     * engine serving 977-1000 correctly on all ten clicks). */
    readonly retainedFloor?: number;
    /** Required for every negotiated replay-window response. */
    readonly firstPaintState: SessionBoundedFirstPaintState;
}
/** Explicit budgets for a separately pageable projection detail lane. */
interface SessionHistoryDetailOptions {
    readonly maxItems?: number;
    readonly maxBytes?: number;
}
/** Independently pageable bounded SCM/tree detail. Never infer completeness
 * from the transcript's first-paint window. */
interface SessionHistoryDetailPage {
    readonly items: readonly unknown[];
    readonly branchGeneration: number;
    readonly projectionGeneration: number;
    readonly reducerVersion: number;
    readonly durableHead: number;
    readonly leafId: string | null;
    readonly maxItems: number;
    readonly maxBytes: number;
    readonly oversize: boolean;
    readonly complete: boolean;
    readonly hasMore: boolean;
    readonly detailCursor?: string;
}
/** One exact older durable transcript page. Consumers prepend `journal` without
 * replacing current runtime, branch detail, or first-paint reducer state. */
interface SessionHistoryPage {
    readonly journal: {
        readonly seq: number;
        readonly transcript: readonly SessionTranscriptMessage[];
        readonly customMessages: Readonly<Record<string, SessionJournalCustomMessage>>;
    };
    readonly window: SessionHistoryWindow;
    /** Canonical dedupe identities for this page. A transcript row may be
     * materialized from several journal entries, so row identity alone is not a
     * durable overlap proof. */
    readonly durableEntries: readonly {
        readonly seq: number;
        readonly id?: string;
    }[];
    /** Stable boundary ids for preserving a rendered scroll anchor across prepend. */
    readonly anchor: {
        readonly previousFirstMessageId?: string;
        readonly firstPrependedMessageId?: string;
    };
}
/** Request parameters for a negotiated bounded initial transcript window. */
interface SessionOpenOptions {
    /** User-configured first-paint turn budget. The driver never substitutes a smaller cap. */
    readonly maxVisibleTurns?: number;
    /** Requested wire budget. The negotiated server remains authoritative. */
    readonly maxBytes?: number;
    readonly includeRuntime?: boolean;
}
interface SessionDriver {
    listSessions(workspace: WorkspaceRef): Promise<readonly SessionSnapshot[]>;
    createSession(workspace: WorkspaceRef, options?: CreateSessionOptions): Promise<SessionSnapshot>;
    openSession(sessionRef: SessionRef, initialSnapshot?: SessionSnapshot | null, options?: SessionOpenOptions): Promise<SessionSnapshot>;
    archiveSession(sessionRef: SessionRef): Promise<void>;
    unarchiveSession(sessionRef: SessionRef): Promise<void>;
    pinSession(sessionRef: SessionRef): Promise<void>;
    unpinSession(sessionRef: SessionRef): Promise<void>;
    deleteSession(sessionRef: SessionRef): Promise<void>;
    sendUserMessage(sessionRef: SessionRef, input: SessionMessageInput): Promise<void>;
    interruptWithQueuedMessage(sessionRef: SessionRef, next: SessionMessageInput, remaining: readonly SessionQueuedMessage[]): Promise<void>;
    replaceQueuedMessages(sessionRef: SessionRef, messages: readonly SessionQueuedMessage[]): Promise<void>;
    cancelCurrentRun(sessionRef: SessionRef): Promise<void>;
    setSessionModel(sessionRef: SessionRef, selection: SessionModelSelection): Promise<void>;
    setSessionThinkingLevel(sessionRef: SessionRef, thinkingLevel: string): Promise<void>;
    setSessionApprovalMode(sessionRef: SessionRef, approvalMode: string): Promise<void>;
    setSessionEphemeral(sessionRef: SessionRef, ephemeral: boolean): Promise<void>;
    /** TUI-parity role-model cycling (Ctrl+P): hop the session model across the
     *  engine's `cycleOrder` roles. Null when fewer than two role models resolve.
     *  Optional capability. */
    cycleSessionRoleModel?(sessionRef: SessionRef, direction: "forward" | "backward"): Promise<SessionRoleCycleResult | null>;
    /** Session-scoped model-profile switch: apply `modelProfiles.<profile>` to this
     *  session's role map (null reverts to config defaults). Never persisted.
     *  Optional capability. */
    setSessionProfile?(sessionRef: SessionRef, profile: string | null): Promise<SessionProfileSwitchResult>;
    /** Engine on-device speech-to-text availability (`_inso/stt/status`). Null on
     *  engines that predate the stt lane. Optional capability. */
    speechToTextStatus?(): Promise<SpeechToTextStatus | null>;
    /** Provision the on-device speech model AHEAD of first use, with no capture
     *  stream and no microphone permission — what a Settings setup surface
     *  calls so a first download (the default tier is ~680 MB) finishes before
     *  the user's first sentence stalls behind it. Resolves the resulting
     *  status, or null on engines that predate the lane. Optional capability. */
    downloadSpeechModel?(events?: {
        readonly onProgress?: (progress: {
            readonly label?: string;
            readonly percent?: number;
            readonly loaded?: number;
            readonly total?: number;
        }) => void;
    }): Promise<SpeechToTextStatus | null>;
    /** Open a live engine STT capture stream: the client streams 16 kHz mono
     *  s16le PCM in; partial/segment transcripts push back over the event lane.
     *  Optional capability. */
    startSpeechToText?(events: SpeechToTextEvents, lang?: string): Promise<SpeechToTextHandle>;
    /** Resolves the live Pulse endpoint advertised by the engine.
     *
     *  OPTIONAL — doc 48 P0.3. Pulse is a Dimension concept; a foreign body has no
     *  endpoint to advertise. Absent ⇒ the caller shows nothing, never `url: null`
     *  dressed up as an answer. */
    pulseGetEndpoint?(): Promise<{
        url: string | null;
    }>;
    renameSession(sessionRef: SessionRef, title: string): Promise<void>;
    /** OPTIONAL — doc 48 P0.3. Compaction is a property of *our* loop; a body that
     *  manages its own context cannot honestly implement it. Absent ⇒ the affordance
     *  is hidden. (`driver-codex` throws here — loud, but still the wrong answer:
     *  the UI should never have offered the button.) */
    compactSession?(sessionRef: SessionRef, customInstructions?: string): Promise<void>;
    reloadSession(sessionRef: SessionRef): Promise<void>;
    /** Fetch the exact durable transcript page preceding `beforeCursor`. The
     * cursor is opaque and branch/projection bound; callers must retain it from
     * the authoritative preceding window, never synthesize one. */
    loadEarlierSession?(sessionRef: SessionRef, beforeCursor: string): Promise<SessionHistoryPage | null>;
    /** Fetch bounded, branch/projection-bound SCM detail separately from the
     * transcript window. */
    getSessionScmDetail?(sessionRef: SessionRef, detailCursor: string, options?: SessionHistoryDetailOptions): Promise<SessionHistoryDetailPage | null>;
    /** Fetch bounded, branch/projection-bound tree detail separately from the
     * transcript window. */
    getSessionTreeDetail?(sessionRef: SessionRef, detailCursor: string, options?: SessionHistoryDetailOptions): Promise<SessionHistoryDetailPage | null>;
    /** Probe the active ACP transport after the host returns from the background.
     *  A silent half-open wire re-dials and rehydrates this session without
     *  replaying a user prompt. Optional for non-ACP drivers. */
    recoverTransport?(sessionRef: SessionRef): Promise<void>;
    /** OPTIONAL — doc 48 P0.3. The branch/leaf tree is our journal's shape. Absent ⇒
     *  no tree UI, which is honest; returning `{ roots: [], leafId: null }` is not —
     *  it renders an empty tree and a user cannot tell "no branches" from "cannot
     *  answer". That exact bug is live in `driver-codex` today. */
    getSessionTree?(sessionRef: SessionRef): Promise<SessionTreeSnapshot>;
    /** On-demand per-category context breakdown (parity with Engine `/context`). Optional capability. */
    getContextBreakdown?(sessionRef: SessionRef): Promise<ContextBreakdown | null>;
    /** Live tools visible to the agent (parity with Engine `/tools`). Optional capability. */
    getTools?(sessionRef: SessionRef): Promise<readonly ToolDescriptor[] | null>;
    /** Bytes for ONE image a REPLAYED tool result carried as a `blob:sha256:` ref instead of
     *  inline base64. The engine externalizes tool-result images and its replay payload guard
     *  drops any inline `data` over 256 KB, so a reopened session used to lose every image
     *  above that outright. Replay now folds the pixels back into the ref that addresses them
     *  and a card fetches through here when it is actually about to paint — so a session with
     *  fifty screenshots costs one fetch per visible card, not fifty inlined megabytes on open.
     *
     *  Resolves `null` when the blob is gone (pruned/garbage-collected) — a legitimate answer
     *  the card renders as its "unavailable" tile, not an error. Optional capability: a driver
     *  that omits it keeps showing that tile, which is exactly the pre-fix behavior. */
    readSessionImageBlob?(sessionRef: SessionRef, ref: string, mimeType?: string): Promise<{
        data: string;
        mimeType?: string;
    } | null>;
    /** Conversation-wide background job inventory (parity with Engine `/jobs`), for
     *  the "working in background" badge popover. Optional capability. */
    getJobs?(sessionRef: SessionRef, options?: {
        recentLimit?: number;
    }): Promise<JobsSnapshot | null>;
    /** Stop one background job by id. Returns false when it was already terminal
     *  or unknown. Optional capability. */
    cancelJob?(sessionRef: SessionRef, jobId: string): Promise<boolean>;
    /** Hot-mount an enabled plugin's tools into the LIVE session so the current
     *  conversation can use them immediately (rides Engine's mid-session tool
     *  activation). Optional capability — older engines (no `_inso/session/mountPlugin`
     *  RPC) resolve `{ mounted: false }` and the caller degrades to "new
     *  conversations only". Never rejects. */
    mountPlugin?(sessionRef: SessionRef, plugin: string): Promise<MountPluginResult>;
    /** Discovered General Agents for a workspace — the creation-time picker's catalog.
     *  Optional capability: hosts without agent discovery (or the desktop in-process
     *  driver) omit it and the app falls back to its known product profiles. */
    listAgents?(workspace: WorkspaceRef): Promise<readonly AgentChoice[]>;
    /** OPTIONAL — doc 48 P0.3. Repositioning within the journal presumes our journal.
     *  Absent ⇒ navigation affordances are hidden. Worst offender in `driver-codex`,
     *  which returns `{ cancelled: false }` — a *claim of success* for work that
     *  never happened. */
    navigateSessionTree?(sessionRef: SessionRef, targetId: string, options?: NavigateSessionTreeOptions): Promise<NavigateSessionTreeResult>;
    /** Fork a session into a NEW independent session (all entries + artifacts deep-copied,
     *  entry ids preserved). With `fromEntryId`, the fork is then repositioned at that entry —
     *  forking from a user message lands just before it (its text returned as `editorText` for
     *  composer prefill); from an agent message keeps up to and including it. Optional capability.
     *  The original session is untouched. */
    forkSession?(sessionRef: SessionRef, fromEntryId?: string): Promise<ForkSessionResult>;
    /** OPTIONAL — doc 48 P0.3. Slash commands come from our registry. Absent ⇒ no
     *  command palette entries from the body; `[]` would claim it has none. */
    getSessionCommands?(sessionRef: SessionRef): Promise<readonly EngineCommandRecord[]>;
    /** OPTIONAL — doc 48 P0.3. Absent ⇒ no completions offered, rather than an empty
     *  list that reads as "nothing matched". */
    queryCompletions?(sessionRef: SessionRef, query: CompletionQuery): Promise<readonly CompletionItem[]>;
    /** Resolves the pending host-UI request. Returns false when no pending request
     *  matched (already resolved elsewhere, or the picker outlived its request) -
     *  the caller should surface that as a notice rather than silently hanging. */
    respondToHostUiRequest(sessionRef: SessionRef, response: HostUiResponse): Promise<boolean>;
    subscribe(sessionRef: SessionRef, listener: SessionEventListener): Unsubscribe;
    closeSession(sessionRef: SessionRef): Promise<void>;
}
type PermissionOptionKind = "allow_once" | "allow_always" | "reject_once" | "reject_always";
/** One choice in a permission prompt (Engine `ClientBridgePermissionOption`). */
interface PermissionOption {
    readonly optionId: string;
    readonly name: string;
    readonly kind: PermissionOptionKind;
}
/** A file the gated tool would touch (Engine permission-intent paths/locations). */
interface PermissionLocation {
    readonly path: string;
    readonly line?: number;
}
type TaskStatus = "pending" | "in_progress" | "completed" | "abandoned";
/** A single task (Engine `TodoItem`). */
interface TaskItem {
    readonly content: string;
    readonly status: TaskStatus;
    readonly notes?: readonly string[];
}
/** A named group of tasks (Engine `TodoPhase`). */
interface TaskPhase {
    readonly name: string;
    readonly tasks: readonly TaskItem[];
}
interface TasksUpdatedEvent extends SessionEventBase {
    readonly type: "tasksUpdated";
    readonly phases: readonly TaskPhase[];
}
interface QueuedMessagesChangedEvent extends SessionEventBase {
    readonly type: "queuedMessagesChanged";
    readonly messages: readonly SessionQueuedMessage[];
}
interface BackgroundWorkChangedEvent extends SessionEventBase {
    readonly type: "backgroundWorkChanged";
    readonly hasBackgroundWork: boolean;
}
interface RunningStatusChangedEvent extends SessionEventBase {
    readonly type: "runningStatusChanged";
    readonly status: "running" | "idle";
}
interface SessionLoadedChangedEvent extends SessionEventBase {
    readonly type: "loadedChanged";
    readonly loaded: boolean;
}
interface ContextUsage {
    /** Estimated context tokens, or null if unknown (e.g. just after compaction). */
    readonly tokens: number | null;
    readonly contextWindow: number;
    /** Usage as a fraction of the window, or null if tokens is unknown. */
    readonly percent: number | null;
}
interface ContextUsageEvent extends SessionEventBase {
    readonly type: "contextUsage";
    readonly usage: ContextUsage;
}
/** One category bucket in the context window (system prompt, tools, messages, …). */
interface ContextBreakdownCategory {
    readonly id: string;
    readonly label: string;
    readonly tokens: number;
}
/** On-demand per-category context breakdown — mirrors Engine `computeContextBreakdown`. */
interface ContextBreakdown {
    readonly contextWindow: number;
    readonly usedTokens: number;
    readonly autoCompactBufferTokens: number;
    readonly freeTokens: number;
    readonly categories: readonly ContextBreakdownCategory[];
}
/** One tool exposed to the agent — mirrors Engine's tool registry entry. */
interface ToolDescriptor {
    readonly name: string;
    /** Whether the tool is currently active (in `getActiveToolNames`). */
    readonly active: boolean;
    /** Origin bucket, derived from the name (`mcp__` prefix ⇒ MCP, else builtin). */
    readonly source: "builtin" | "mcp";
    /** Human-readable label for UI (Engine `AgentTool.label`), if known. */
    readonly label?: string;
    /** One-line summary (Engine `AgentTool.summary`/`description`), if known. */
    readonly summary?: string;
}
/** Result of hot-mounting an enabled plugin's tools into the LIVE session, so
 *  the current conversation can use them immediately (parity with Engine's
 *  mid-session tool activation rail). `mounted:false` means nothing was loadable
 *  into this session — the caller degrades to "takes effect in new conversations".
 *  `tools` lists the tool names newly mounted (for a "mounted N tools" notice). */
interface MountPluginResult {
    readonly mounted: boolean;
    readonly tools?: readonly string[];
}
type JobStatus = "running" | "completed" | "failed" | "cancelled";
/** One background job (Engine `AsyncJob`) — conversation-wide, not owner-scoped:
 *  matches the badge signal that drives `hasBackgroundWork`. */
interface JobDescriptor {
    readonly id: string;
    readonly type: "bash" | "task";
    readonly status: JobStatus;
    readonly label: string;
    readonly startTime: number;
    /** Registry id of the agent that registered the job (e.g. "Main", a subagent name). */
    readonly ownerId?: string;
}
interface JobsDeliveryState {
    readonly queued: number;
    readonly delivering: boolean;
    readonly nextRetryAt?: number;
    readonly pendingJobIds: readonly string[];
}
interface JobsSnapshot {
    readonly running: readonly JobDescriptor[];
    readonly recent: readonly JobDescriptor[];
    readonly delivery: JobsDeliveryState;
}
/** The streaming status line — the "verb" (Thinking…/Searching…) + visibility. */
interface WorkingStatus {
    readonly message: string | null;
    readonly visible: boolean;
}
interface WorkingStatusEvent extends SessionEventBase {
    readonly type: "workingStatus";
    readonly status: WorkingStatus;
}
interface PlanModeState {
    readonly enabled: boolean;
    readonly planFilePath: string;
    readonly workflow?: "parallel" | "iterative";
    /** Live plan-file markdown, included in the engine snapshot when plan mode is active. */
    readonly content?: string;
}
interface PlanModeChangedEvent extends SessionEventBase {
    readonly type: "planModeChanged";
    readonly state: PlanModeState;
}
type GoalStatus = "active" | "paused" | "budget-limited" | "complete" | "dropped";
interface Goal {
    readonly id: string;
    readonly objective: string;
    readonly status: GoalStatus;
    readonly tokenBudget?: number;
    readonly tokensUsed: number;
    readonly timeUsedSeconds: number;
}
interface GoalChangedEvent extends SessionEventBase {
    readonly type: "goalChanged";
    readonly goal: Goal | null;
}
/** Tone hint for a mode pill; mirrors `ModePillTone` in `@fraym/ui`. */
declare const MODE_STATE_TONES: readonly ["accent", "add", "blue", "warn", "del", "mute"];
type ModeStateTone = (typeof MODE_STATE_TONES)[number];
/** Generic snapshot of one active session mode, surface-agnostic. */
interface ModeState {
    /** Stable mode id, e.g. "loop", "advisor", "ephemeral". */
    readonly id: string;
    /** Pill label shown to the user. */
    readonly label: string;
    /** Optional tonal color hint; the renderer defaults to "accent". */
    readonly tone?: ModeStateTone;
    /** Optional short status/detail text. */
    readonly status?: string;
    /** Whether the client should render a close (×) affordance. */
    readonly closeable?: boolean;
    /** Slash-command text to run when the user closes the pill (e.g. "/loop"). */
    readonly closeCommand?: string;
}
interface ModeStateChangedEvent extends SessionEventBase {
    readonly type: "modeStateChanged";
    readonly modes: readonly ModeState[];
}
interface ThinkingLevelChangedEvent extends SessionEventBase {
    readonly type: "thinkingLevelChanged";
    readonly thinkingLevel: string | null;
}
interface CompactionStartedEvent extends SessionEventBase {
    readonly type: "compactionStarted";
    readonly reason: "threshold" | "overflow" | "idle" | "manual";
}
interface CompactionFinishedEvent extends SessionEventBase {
    readonly type: "compactionFinished";
    readonly aborted: boolean;
    readonly tokensBefore?: number;
    readonly shortSummary?: string;
    /** Tokens freed by the compaction, in thousands (for the UI divider). */
    readonly freed?: number;
    /** The underlying failure reason when `aborted` (provider/model error) — absent for a
     *  benign/cancelled abort with nothing more specific to report. */
    readonly errorMessage?: string;
}
interface NoticeEvent extends SessionEventBase {
    readonly type: "notice";
    readonly level: "info" | "warning" | "error";
    readonly message: string;
    readonly source?: string;
}
/**
 * A displayed Engine custom message appended live mid-session (extension
 * `sendMessage` with `display: true`). The thread keys a `msg:<customType>`
 * surface renderer on `customType`; `text` is the renderer's (and fallback's)
 * content; `payload` carries the sender's structured `details` when the
 * transport forwards them. Snapshot reloads rebuild the same message from the
 * persisted `custom_message` entry.
 */
interface CustomMessageEvent extends SessionEventBase {
    readonly type: "customMessage";
    readonly customType: string;
    readonly text: string;
    /** Stable engine custom-message id, when the extension supplied one. */
    readonly id?: string;
    readonly payload?: unknown;
}
/**
 * Result of a builtin slash command run at the engine boundary (e.g. `/usage`).
 * The engine emits the command name + its text output; the driver adapter
 * stamps a neutral `renderKind` (LAW 2) that the UI maps to a transcript block
 * renderer (rich panel, or a terminal-styled fallback). `data` carries an
 * optional structured payload for commands that emit one.
 */
interface CommandResultEvent extends SessionEventBase {
    readonly type: "commandResult";
    readonly command: string;
    readonly text: string;
    readonly renderKind?: string;
    /** Stable command-result journal entry id, when the engine supplied one. */
    readonly id?: string;
    readonly data?: unknown;
}
/**
 * The session's slash-command catalog changed.
 *
 * The catalog arrives as a DEFERRED push (`available_commands_update`) and can
 * also be re-read on demand, so a consumer that only re-snapshots on its own
 * schedule can miss it entirely: type `/revie`, stop typing, and a catalog that
 * lands one tick later never reaches the menu. This is the edge that says
 * "re-read now" — the payload is the catalog itself so a listener needs no
 * follow-up call.
 */
interface CommandsUpdatedEvent extends SessionEventBase {
    readonly type: "commandsUpdated";
    readonly commands: readonly EngineCommandRecord[];
}
/** Provider retry / model-fallback progress (Engine `auto_retry_*`, `retry_fallback_*`). */
interface RetryEvent extends SessionEventBase {
    readonly type: "retry";
    readonly phase: "started" | "succeeded" | "failed" | "fallback";
    readonly attempt?: number;
    readonly maxAttempts?: number;
    readonly message?: string;
    readonly fromModel?: string;
    readonly toModel?: string;
}
/** A TTSR rule whose condition matched and was injected into context. */
interface TtsrRule {
    readonly name: string;
    readonly description?: string;
}
/** One or more TTSR rules were injected (Engine `ttsr_triggered`). */
interface TtsrTriggeredEvent extends SessionEventBase {
    readonly type: "ttsr";
    readonly rules: readonly TtsrRule[];
}
/**
 * [fraym] The agent replaced the underlying session behind this session ref —
 * `/handoff`, `/new`, auto-handoff, plan-approval "fresh context", an
 * extension `ctx.switchSession`, or a client-side spawn (`/sidequest`).
 * Emitted on the SOURCE session's subscription. `follow: true` means the
 * conversation itself moved (the source session is finished) and consumers
 * should switch to `toSessionId`; `follow: false` means a side session was
 * spawned and an open affordance is enough.
 */
interface SessionSwitchedEvent extends SessionEventBase {
    readonly type: "sessionSwitched";
    /** Session id of the continuation/side session (same workspace). */
    readonly toSessionId: string;
    /** Best-effort provenance: "handoff" | "new" | "switch" | "sidequest" | … */
    readonly reason?: string;
    /** Whether the conversation moved (auto-switch) vs. spawned a side session. */
    readonly follow: boolean;
    /** Title of the target session, when known. */
    readonly title?: string;
}
type SubagentStatus = "pending" | "running" | "completed" | "failed" | "aborted";
/** Auto-retry state for a provider-throttled subagent (Engine `AgentProgress.retryState`). */
interface SubagentRetry {
    readonly attempt: number;
    readonly maxAttempts: number;
    readonly message?: string;
}
/** One code-review finding (Engine `report_finding` / `ReportFindingDetails`). */
interface SubagentReviewFinding {
    /** Severity, P0 (most severe) to P3. */
    readonly priority: "P0" | "P1" | "P2" | "P3";
    readonly title: string;
    readonly filePath?: string;
    readonly lineStart?: number;
    readonly lineEnd?: number;
    /** Full finding detail, shown when the run row is expanded. */
    readonly body?: string;
    /** Reviewer confidence 0..1. */
    readonly confidence?: number;
}
/**
 * Reviewer-agent outcome: an overall verdict plus the reported findings (Engine
 * `yield`/`SubmitReviewDetails` + `report_finding`). Present only on runs whose
 * agent performed a code review.
 */
interface SubagentReview {
    /** Overall verdict; absent when the reviewer reported findings but never yielded a verdict. */
    readonly verdict?: "correct" | "incorrect";
    /** Verdict confidence 0..1, when a verdict was given. */
    readonly confidence?: number;
    readonly explanation?: string;
    readonly findings: readonly SubagentReviewFinding[];
}
/**
 * One parallel subagent. Unifies Engine's live `AgentProgress` and terminal
 * `SingleResult` into a single shape the UI renders the same way at every status.
 */
interface SubagentRun {
    readonly index: number;
    readonly id: string;
    /** Agent persona/type, e.g. `"explore"`, `"reviewer"` (Engine `AgentProgress.agent`). */
    readonly agent: string;
    readonly status: SubagentStatus;
    /** Short UI label, distinct from the task text (Engine `AgentProgress.description`). */
    readonly description?: string;
    /** The assignment text driving this agent. */
    readonly task: string;
    /** Live one-liner of what the agent is doing now, e.g. `"find: Locating X"` (Engine `lastIntent`). */
    readonly lastIntent?: string;
    /** Tool the agent is mid-call on (Engine `AgentProgress.currentTool`). */
    readonly currentTool?: string;
    readonly toolCount: number;
    /** Cumulative billed tokens across turns (Engine `AgentProgress.tokens`). */
    readonly tokens: number;
    /** Current per-turn context size — numerator of the `<ctx>/<window>` gauge. */
    readonly contextTokens?: number;
    /** Model context window — denominator of the gauge. */
    readonly contextWindow?: number;
    /** Cumulative cost in USD. */
    readonly cost: number;
    readonly durationMs: number;
    /** Resolved `<provider>/<id>` model string, when known. */
    readonly resolvedModel?: string;
    /** Present while the run is blocked on a provider retry (renders a "rate-limited" badge). */
    readonly retry?: SubagentRetry;
    /** Terminal exit code (Engine `SingleResult.exitCode`); set once `status` is terminal. */
    readonly exitCode?: number;
    /** Terminal error message, when the run failed. */
    readonly error?: string;
    /** Full self-contained assignment text for this run (Engine `SingleResult.assignment ?? task`). */
    readonly assignment?: string;
    /** Terminal output text (the agent's final message); rendered as a capped preview (Engine `renderOutputSection`). */
    readonly output?: string;
    /** The run's output was capped at the byte/line limit (Engine `SingleResult.truncated`). */
    readonly truncated?: boolean;
    /** Why the run was aborted (Engine `SingleResult.abortReason`); set when `status` is `"aborted"`. */
    readonly abortReason?: string;
    /** Reviewer-agent verdict + findings, when this run performed a code review (Engine yield/report_finding). */
    readonly review?: SubagentReview;
    /**
     * Subagents this run spawned through its own nested `task` dispatch — the agent
     * lineage beneath it. Rendered as an indented sub-tree; recursion is unbounded.
     * A child commonly runs on a different model than its parent (e.g. a cheap
     * explorer under an expensive planner), which the UI surfaces as a lineage marker.
     */
    readonly children?: readonly SubagentRun[];
}
type SubagentBatchStatus = "dispatching" | "running" | "completed" | "failed" | "aborted";
/**
 * A parallel subagent fan-out: the shared context + N `SubagentRun`s. Two
 * producers, one shape — a `task`-tool dispatch (Engine `TaskParams` +
 * `TaskToolDetails`), or one `phase()` of an `eval` cell's `agent()` fan-out
 * (Engine `eval/agent-bridge.ts` progress events).
 *
 * Keyed by `callId`: for a `task` dispatch that IS the parent tool call's id, so
 * the consumer renders the batch in place of that call. An eval call can hold
 * several phases, so those ids are synthesized per phase from the call id and are
 * NOT a tool-call id — never thread one back to the engine.
 */
interface SubagentBatch {
    /** Batch id. A `task` call's `callId`, or a synthesized `<callId>#c<cell>:<phase>` / `<callId>#agents:<phase>` for an eval fan-out. */
    readonly callId: string;
    /** Agent persona the runs share, e.g. `"explore"` (Engine `TaskParams.agent`); outliers render their own chip. */
    readonly agent: string;
    /** Shared background prepended to each assignment — the Goal/Context/Constraints block (Engine `TaskParams.context`). */
    readonly context?: string;
    /** Display label for the fan-out's subject — the eval `phase()`/cell title. Unset on the `task` path, which titles from `context`. */
    readonly title?: string;
    /**
     * Which producer this fan-out came from. `"task"` (default) is a `task`-tool
     * dispatch; `"eval"` is one `phase()` of an `eval` cell's `agent()` fan-out —
     * the code-mode workflow path, whose batch id is synthesized rather than a
     * tool-call id.
     */
    readonly source?: "task" | "eval";
    readonly status: SubagentBatchStatus;
    /** Runs in isolated worktrees, returning patches (Engine `TaskParams.isolated`). */
    readonly isolated?: boolean;
    readonly runs: readonly SubagentRun[];
    readonly totalDurationMs?: number;
    /** Aggregate billed tokens across all runs (Engine `TaskToolDetails.usage`). */
    readonly tokens?: number;
    /** Aggregate cost in USD across all runs. */
    readonly cost?: number;
}
interface SubagentBatchStartedEvent extends SessionEventBase {
    readonly type: "subagentBatchStarted";
    readonly batch: SubagentBatch;
}
/** One run's progress changed (Engine `task:subagent:progress`). */
interface SubagentRunUpdatedEvent extends SessionEventBase {
    readonly type: "subagentRunUpdated";
    readonly callId: string;
    readonly run: SubagentRun;
}
interface SubagentBatchFinishedEvent extends SessionEventBase {
    readonly type: "subagentBatchFinished";
    readonly callId: string;
    readonly status: SubagentBatchStatus;
    readonly totalDurationMs?: number;
    readonly tokens?: number;
    readonly cost?: number;
}
type CompletionKind = "command" | "command-arg" | "file" | "mention" | "skill" | "prompt";
interface CompletionSubcommand {
    readonly name: string;
    readonly description?: string;
    /** Usage hint, e.g. `<name> [--scope project|user]`. */
    readonly usage?: string;
}
interface CompletionItem {
    readonly kind: CompletionKind;
    readonly label: string;
    /** Text inserted when the item is accepted. */
    readonly value: string;
    readonly detail?: string;
    /** Usage hint shown alongside the item (e.g. an argument's `<name> [--scope …]`),
     *  distinct from `detail`. Populated for `command-arg` rows. */
    readonly hint?: string;
    /** Declarative subcommands offered as argument completions once this command is committed
     *  (e.g. `/fast` → on | off | status). Mirrors Engine's TUI subcommand dropdown. */
    readonly subcommands?: readonly CompletionSubcommand[];
    /** Engine-advertised icon (skill frontmatter `icon`) — the slash-entry policy's
     *  wire layer; a bare glyph name or a data-URI asset. */
    readonly icon?: string;
}
interface CompletionQuery {
    readonly text: string;
    /** Caret offset within `text`. */
    readonly cursor: number;
}

type AnalyticsRange = "1d" | "3d" | "7d" | "30d" | "all";
type AnalyticsFreshnessStatus = "ready" | "syncing" | "stale" | "unavailable";
interface AnalyticsFreshness {
    readonly status: AnalyticsFreshnessStatus;
    readonly source: string;
    readonly syncedAt?: string;
    readonly processedFiles?: number;
    readonly processedRows?: number;
    readonly error?: string;
}
interface AnalyticsOverview {
    readonly sessions: number;
    readonly messages: number;
    readonly requests: number;
    readonly errors: number;
    readonly errorRate: number;
    readonly inputTokens: number;
    readonly outputTokens: number;
    readonly cacheReadTokens: number;
    readonly cacheWriteTokens: number;
    readonly totalTokens: number;
    readonly cacheRate: number;
    readonly totalCost: number;
    readonly premiumRequests: number;
    readonly activeDays: number;
    readonly currentStreak: number;
    readonly peakHour?: number | null;
    readonly latestActivityAt?: string;
    readonly averageDurationMs?: number | null;
    readonly averageTtftMs?: number | null;
    readonly averageTokensPerSecond?: number | null;
}
interface AnalyticsActivityBucket {
    readonly timestamp: number;
    readonly date: string;
    readonly sessions?: number;
    readonly messages: number;
    readonly requests: number;
    readonly errors: number;
    readonly tokens: number;
    readonly cost: number;
    readonly level: number;
}
/**
 * External-benchmark "world" standing for a model, joined from public leaderboards.
 * OpenRouter aggregates **Design Arena** (coding ELO/rank/win-rate) and
 * **Artificial Analysis** indices; pricing is the model's published list price.
 *
 * Latency/throughput here are OPTIONAL and default to null: OpenRouter does NOT
 * expose world TTFT/tok-s, so they stay absent until a latency source
 * (Artificial Analysis API / llm-stats) is wired. Never fabricate them.
 */
interface AnalyticsModelWorld {
    /** Matched external model slug, e.g. "anthropic/claude-opus-4.6". */
    readonly slug: string;
    /** Design Arena coding ELO (arena "models" / category "codecategories"). */
    readonly codingElo?: number | null;
    /** Design Arena coding rank (1 = best). */
    readonly codingRank?: number | null;
    /** Total models ranked in that arena — denominator for "rank N of M". */
    readonly rankPool?: number | null;
    /** Design Arena coding win-rate, 0-100. */
    readonly codingWinRate?: number | null;
    /** Artificial Analysis indices (0-100), when published for this model. */
    readonly intelligenceIndex?: number | null;
    readonly codingIndex?: number | null;
    readonly agenticIndex?: number | null;
    /** Published list price, USD per million tokens. */
    readonly priceInputPerMTok?: number | null;
    readonly priceOutputPerMTok?: number | null;
    /** World-measured latency/throughput — only when a non-OpenRouter source supplies it. */
    readonly ttftMs?: number | null;
    readonly tokensPerSecond?: number | null;
}
/** Provenance for the joined external-benchmark layer (snapshot-level). */
interface AnalyticsBenchmarkMeta {
    /** Human label for the world-data source, e.g. "Design Arena + Artificial Analysis". */
    readonly source: string;
    /** ISO timestamp the world data was last fetched/cached. */
    readonly syncedAt?: string;
    /** Models in the ranked pool (for global rank denominators). */
    readonly rankPool?: number | null;
}
interface AnalyticsModelStats {
    readonly model: string;
    readonly provider: string;
    readonly requests: number;
    readonly errors: number;
    readonly inputTokens: number;
    readonly outputTokens: number;
    readonly totalTokens: number;
    readonly cacheRate: number;
    readonly totalCost: number;
    readonly averageTtftMs?: number | null;
    readonly averageTokensPerSecond?: number | null;
    readonly lastActivityAt?: string;
    /** TTFT percentiles (ms), when the aggregator retains per-request samples. */
    readonly ttftP50Ms?: number | null;
    readonly ttftP95Ms?: number | null;
    /** External-benchmark standing for this model, joined from public leaderboards. */
    readonly world?: AnalyticsModelWorld | null;
}
interface AnalyticsCostBucket {
    readonly timestamp: number;
    readonly date: string;
    readonly model: string;
    readonly provider: string;
    readonly cost: number;
    readonly costInput: number;
    readonly costOutput: number;
    readonly costCacheRead: number;
    readonly costCacheWrite: number;
    readonly requests: number;
}
interface AnalyticsBehaviorStats {
    readonly messages: number;
    readonly yelling: number;
    readonly profanity: number;
    readonly anguish: number;
    readonly negation: number;
    readonly repetition: number;
    readonly blame: number;
    readonly characters: number;
}
interface AnalyticsBehaviorModelStats extends AnalyticsBehaviorStats {
    readonly model: string;
    readonly provider: string;
    readonly lastActivityAt?: string;
}
interface AnalyticsRecentRequest {
    readonly id?: number;
    readonly timestamp: number;
    readonly model: string;
    readonly provider: string;
    readonly stopReason: string;
    readonly errorMessage?: string | null;
    readonly totalTokens: number;
    readonly cost: number;
}
/** Per-tool latency/error rollup — Phase 2 (harness performance). */
interface AnalyticsToolStats {
    readonly name: string;
    readonly calls: number;
    readonly errors: number;
    readonly avgLatencyMs: number | null;
    readonly p50LatencyMs: number | null;
    readonly p95LatencyMs: number | null;
}
/** Tool-calls/messages per turn (turn = bounded by consecutive user messages). */
interface AnalyticsTurnShape {
    readonly turns: number;
    readonly avgToolCallsPerTurn: number;
    readonly avgAssistantMessagesPerTurn: number;
    readonly maxToolCallsInTurn: number;
}
/** Requests/tokens/cost attributed to the main agent vs task subagents vs the advisor. */
interface AnalyticsAgentTypeStats {
    readonly agentType: "main" | "subagent" | "advisor";
    readonly requests: number;
    readonly inputTokens: number;
    readonly outputTokens: number;
    readonly cacheReadTokens: number;
    readonly cacheWriteTokens: number;
    readonly totalCost: number;
}
interface AnalyticsHarnessStats {
    readonly tools: readonly AnalyticsToolStats[];
    readonly turnShape: AnalyticsTurnShape;
    readonly byAgentType: readonly AnalyticsAgentTypeStats[];
}
/** Window-over-window comparison for one model ("opus vs last week"). */
interface AnalyticsModelDegradation {
    readonly model: string;
    readonly provider: string;
    readonly currentAvgTtftMs: number | null;
    readonly previousAvgTtftMs: number | null;
    readonly ttftDeltaPct: number | null;
    readonly currentTokensPerSecond: number | null;
    readonly previousTokensPerSecond: number | null;
    readonly throughputDeltaPct: number | null;
    readonly currentRequests: number;
    readonly previousRequests: number;
    readonly trend: "slower" | "faster" | "flat";
}
interface AnalyticsDegradation {
    readonly windowDays: number;
    readonly currentWindowStart: string;
    readonly previousWindowStart: string;
    readonly models: readonly AnalyticsModelDegradation[];
}
interface AnalyticsSnapshot {
    readonly workspace: WorkspaceRef;
    readonly range: AnalyticsRange;
    readonly freshness: AnalyticsFreshness;
    readonly overview: AnalyticsOverview;
    readonly activity: readonly AnalyticsActivityBucket[];
    readonly models: readonly AnalyticsModelStats[];
    readonly costs: readonly AnalyticsCostBucket[];
    readonly behavior?: {
        readonly overall: AnalyticsBehaviorStats;
        readonly byModel: readonly AnalyticsBehaviorModelStats[];
    };
    readonly harness?: AnalyticsHarnessStats;
    readonly degradation?: AnalyticsDegradation;
    readonly recentRequests: readonly AnalyticsRecentRequest[];
    readonly recentErrors: readonly AnalyticsRecentRequest[];
    readonly benchmarks?: AnalyticsBenchmarkMeta;
}
/** Result of a live "Test my models now" speed test — real, billed API calls, never automatic. */
interface AnalyticsBenchResult {
    readonly selector: string;
    readonly model: string;
    readonly ttftMs: number | null;
    readonly tokensPerSecond: number | null;
    readonly outputTokens: number | null;
    readonly runs: number;
    readonly failures: number;
    readonly error?: string;
}

/**
 * A note's kind — a free-form identifier from the vault's own frontmatter,
 * NOT a closed union. The vault declares its taxonomy as data in
 * `MemoryVaultOverview.types`; a host memory backend seeds new vaults with
 * `self | user | person | episode | reference`, but agents grow the schema
 * simply by writing notes with new types. The UI renders whatever arrives.
 */
type MemoryNoteType = string;
/** Where a note came from — doubles as the write-hygiene taint tag (D13). */
type MemoryNoteSource = "chat" | "dream" | "user-edit" | "import";
/** Trust class derived from the note source; quarantine renders with a provenance disclaimer. */
type MemoryTaint = "trusted" | "quarantine";
/** Task-kind fields lifted from a `type: task` note's frontmatter. */
interface MemoryTaskFields {
    readonly status: string;
    readonly priority?: number;
    readonly assignee?: string;
    readonly section?: string;
    readonly order?: number;
    readonly milestone?: string;
    readonly due?: string;
    readonly depends?: readonly string[];
    /** Parent task id — set on a subtask to nest it under an epic (an epic is just
     *  a task that is some other task's parent). Absent on top-level tasks. */
    readonly parent?: string;
    /** Id of the session actively working this task — stamped when an agent runs the
     *  board `start` from inside a session; the UI resolves it to a name + "go to
     *  session". Cleared when the card returns to backlog. */
    readonly session?: string;
}
/** Where a note belongs. A scope can contain any kind: memories, docs, skills, sessions. */
interface MemoryNoteScope {
    readonly type: "global" | "project" | "person" | "place" | string;
    readonly id: string;
    readonly label?: string;
}
/** One scope/room/lens known to the vault. */
interface MemoryScopeView extends MemoryNoteScope {
    readonly count: number;
}
/** A note row ready for list rendering — no body, just card-level metadata. */
interface MemoryNoteSummary {
    /** ULID — stable across renames/moves (D15). */
    readonly id: string;
    /** Vault-relative path, e.g. `episodes/2026-06-10-memory-graft.md`. */
    readonly path: string;
    readonly title: string;
    readonly type: MemoryNoteType;
    readonly source: MemoryNoteSource;
    readonly taint: MemoryTaint;
    /** Scope/room this note belongs to; absent in legacy drivers, inferred by modern ones. */
    readonly scope?: MemoryNoteScope;
    /** First lines of body text, frontmatter stripped, for the list card. */
    readonly snippet: string;
    /** Author-assigned priority, 1-5; feeds wake L1 ranking (D6/D17). */
    readonly importance: number;
    readonly tags: readonly string[];
    /** Milliseconds since epoch. */
    readonly createdAt: number;
    /** Milliseconds since epoch. */
    readonly updatedAt: number;
    /** Present only when `type === "task"`. */
    readonly task?: MemoryTaskFields;
}
/** An open fact anchored to a note — metadata beside the page, never the answer (D5). */
interface MemoryFactView {
    readonly subject: string;
    readonly predicate: string;
    readonly object: string;
    /** ≤0.7 regex hot path, >0.7 dream-refined (D4). */
    readonly confidence: number;
    /** When the fact became true in the world (event time), ms since epoch. */
    readonly validAt?: number;
}
/** A fully resolved note for the reader pane. */
interface MemoryNoteView extends MemoryNoteSummary {
    /** Body markdown with frontmatter stripped. */
    readonly markdown: string;
    /** Optional read-only HTML rendition for non-markdown artifacts such as imported sessions. */
    readonly html?: string;
    /** Open facts whose provenance pointer lands in this note. */
    readonly facts: readonly MemoryFactView[];
    /** Originating session, when the note's provenance recorded one. */
    readonly sourceSessionId?: string;
}
/** A directory node in the vault tree (directories only, no files). */
interface MemoryFolderNode {
    /** Vault-relative path; `""` is the vault root. */
    readonly path: string;
    readonly name: string;
    /** Notes directly inside this folder (not recursive). */
    readonly noteCount: number;
    readonly children: readonly MemoryFolderNode[];
}
interface MemoryTagView {
    readonly tag: string;
    readonly count: number;
}
/**
 * A project the companion works on with the user. Adapter-derived: the demo
 * driver curates from tags; a host memory adapter derives from session/workspace
 * provenance. First-class in the nav — "what do you know about my project?".
 */
interface MemoryProjectView {
    readonly name: string;
    readonly count: number;
}
/**
 * One entry of the vault's self-declared taxonomy. Derived from frontmatter
 * usage (plus optional in-vault schema notes): label/icon/order travel WITH
 * the vault, so any vault — a host backend, Obsidian, a future chip — renders in the
 * same palace without UI changes.
 */
interface MemoryTypeView {
    /** Frontmatter type identifier, e.g. `episode`. */
    readonly id: string;
    /** Display label, e.g. `Episodes`. */
    readonly label: string;
    /** Optional icon hint (palace icon name); UI falls back when absent/unknown. */
    readonly icon?: string;
    readonly count: number;
}
/** The three permanent wings of the palace — the locked hierarchy level
 *  (Palace → Wings → Rooms → Notes). Wings are fixed; what lives UNDER them
 *  is vault-declared data. */
type MemoryWingId = "memories" | "projects" | "personal";
/**
 * The vault's self-declared presentation of one wing — which top-level
 * folders present into it, its label/icon. Same taxonomy-as-data law as
 * {@link MemoryTypeView}: instances travel WITH the vault (schema notes /
 * vault config), never hardcoded in the UI. A vault that declares nothing
 * gets the seed defaults.
 */
interface MemoryWingView {
    readonly id: MemoryWingId;
    /** Display label, e.g. `Memories`. */
    readonly label?: string;
    /** Optional icon hint (palace icon name); UI falls back when absent/unknown. */
    readonly icon?: string;
    /** Canonical disk root for the wing, e.g. `memories`. */
    readonly root?: string;
    /** Additional top-level dirs presented into this wing (legacy layouts). */
    readonly aliases?: readonly string[];
    /** Honest zero-state line when the wing has nothing on disk yet. */
    readonly emptyHint?: string;
}
/** One snapshot powering the nav pane: tree, type counts, tags, vault stats. */
interface MemoryVaultOverview {
    /** Display-only vault location, e.g. `~/.agent/vault`. */
    readonly vaultPath: string;
    /** Host memory backend identifier. */
    readonly backend: string;
    readonly noteCount: number;
    /** Open (non-invalidated) facts in the reflex. */
    readonly factCount: number;
    /** Notes whose source taints them quarantine (imports awaiting review). */
    readonly quarantineCount: number;
    readonly folders: readonly MemoryFolderNode[];
    readonly types: readonly MemoryTypeView[];
    readonly tags: readonly MemoryTagView[];
    readonly projects: readonly MemoryProjectView[];
    /** Scope-first model: global/project/person/place rooms, each containing any note kind. */
    readonly scopes?: readonly MemoryScopeView[];
    /** Vault-declared wing presentation; absent → the palace's seed defaults. */
    readonly wings?: readonly MemoryWingView[];
}
type MemoryNoteSort = "updated" | "created" | "importance" | "title";
/** Cheap list filtering — title/snippet/tag matching, NOT semantic recall. */
interface MemoryNoteQuery {
    /** Restrict to a folder subtree (vault-relative path). */
    readonly folder?: string;
    readonly type?: MemoryNoteType;
    readonly tag?: string;
    /** Restrict to notes attributed to a project (see MemoryProjectView). */
    readonly project?: string;
    /** Restrict to one scope/room: global/project/person/place/etc. */
    readonly scope?: MemoryNoteScope;
    /** Restrict to tasks OWNED by (actively worked in) this session id — the "This session" board view. */
    readonly session?: string;
    readonly taint?: MemoryTaint;
    /** Only notes updated at/after this epoch-ms instant (Recent view). */
    readonly updatedAfter?: number;
    /** Plain text filter over titles and snippets. */
    readonly filter?: string;
    readonly sort?: MemoryNoteSort;
    readonly limit?: number;
    readonly offset?: number;
}
interface MemoryNotePage {
    readonly notes: readonly MemoryNoteSummary[];
    /** Total matches before limit/offset, for pagination. */
    readonly total: number;
}
/**
 * One recall hit. Per the read invariant (D5) the excerpt is ALWAYS journal
 * text resolved from the pointer — fact rows ride along as metadata only.
 */
interface MemoryRecallHit {
    readonly noteId: string;
    readonly notePath: string;
    readonly title: string;
    /** The journal text the pointer resolved to. */
    readonly excerpt: string;
    readonly lineStart: number;
    readonly lineEnd: number;
    /** Composite score (relevance 0.6 + recency 0.2 + salience 0.2). */
    readonly score: number;
    readonly relevance: number;
    readonly recency: number;
    readonly salience: number;
    readonly taint: MemoryTaint;
    readonly facts: readonly MemoryFactView[];
}
interface MemoryRecallQuery {
    readonly query: string;
    /** Max results after dispose. Adapter default applies when omitted. */
    readonly limit?: number;
}
interface MemoryWakeEssential {
    readonly path: string;
    readonly digest: string;
    readonly importance: number;
}
/** Preview of the session-start wake bundle (D6) — what the agent reads first. */
interface MemoryWakePreview {
    /** L0 — identity, ≈100 tokens. */
    readonly identity: string;
    /** L1 — essential story, importance-ranked note digests. */
    readonly essentials: readonly MemoryWakeEssential[];
    readonly estimatedTokens: number;
    /** The chip's wake budget ceiling (spec: 1000). */
    readonly budgetTokens: number;
    readonly rendered: string;
}
/**
 * Input for the human door's create path. The human authors a page directly;
 * per write hygiene (D13) it lands as source `user-edit`, trusted. Kinds,
 * tags, and folders need no pre-registration — writing the first note OF a
 * kind creates the kind (taxonomy derives from usage).
 */
interface MemoryNoteCreate {
    readonly title: string;
    /** Free-form kind id; a new id grows the vault taxonomy. */
    readonly type: string;
    readonly markdown: string;
    readonly tags?: readonly string[];
    /** Vault folder; defaults to the kind's conventional folder. */
    readonly folder?: string;
    /** 1-5; defaults to 3. */
    readonly importance?: number;
    /** Extra frontmatter keys merged on create (e.g. task status/priority). */
    readonly fields?: Record<string, string | number | boolean | readonly string[]>;
}
/** Patch an existing note (capability). `fields` merge into frontmatter (null
 *  deletes a key); `markdown` replaces the body; omitted parts are kept. */
interface MemoryNoteUpdate {
    readonly noteId: string;
    readonly title?: string;
    readonly tags?: readonly string[];
    readonly markdown?: string;
    readonly fields?: Record<string, string | number | boolean | readonly string[] | null>;
}
/**
 * One vault a driver can open. A `MemoryDriver` IS a vault — multi-vault
 * drivers expose siblings through `listVaults`/`openVault` and each opened
 * vault carries its own honest capability set (a project docs tree has no
 * wake bundle; the memory chip does).
 */
interface MemoryVaultRef {
    /** Driver-scoped stable identifier. */
    readonly id: string;
    readonly label: string;
    /** Display-only location hint, e.g. `~/.agent/vault` or `~/workspaces/project/docs`. */
    readonly path?: string;
    /** Coarse flavor for icon/affordance hints, e.g. `memory`, `docs`. */
    readonly kind?: string;
}
type MemorySyncAction = "created" | "updated" | "skipped" | "pruned" | "missing";
/** One project-docs sync progress beat, streamed through {@link MemoryDriver.sync}. */
interface MemorySyncEvent {
    readonly phase: "discover" | "scan" | "doc" | "finalize" | "done";
    readonly project?: string;
    readonly projectName?: string;
    readonly doc?: string;
    readonly index?: number;
    readonly total?: number;
    readonly action?: MemorySyncAction;
    readonly totals?: {
        created: number;
        updated: number;
        skipped: number;
        pruned: number;
    };
}
/** Final tally of a project-docs sync across every registered project. */
interface MemorySyncSummary {
    readonly projects: number;
    readonly created: number;
    readonly updated: number;
    readonly skipped: number;
    readonly pruned: number;
    readonly missing: number;
}
/** A vault's git state (capability — present only when the vault is git-versioned). */
interface MemoryGitInfo {
    /** The `git` CLI is available on the engine host. */
    readonly available: boolean;
    /** The vault directory is itself a git repo. */
    readonly repo: boolean;
    /** Current branch, or `null` (detached / no commits / not a repo). */
    readonly branch: string | null;
    /** HEAD commit sha, or `null` when the repo has no commits yet. */
    readonly head: string | null;
    /** Uncommitted working-tree changes are present. */
    readonly dirty: boolean;
    /** Configured `origin` backup remote URL, or `null` when none is set. */
    readonly remoteUrl: string | null;
    /** Local commits not yet pushed to the upstream (0 when no upstream). */
    readonly ahead: number;
    /** Upstream commits not yet pulled (0 when no upstream). */
    readonly behind: number;
}
/** One commit in a note's history (newest first). */
interface MemoryGitCommit {
    readonly sha: string;
    /** ISO-8601 author date. */
    readonly date: string;
    readonly author: string;
    readonly subject: string;
}
/** Outcome of a manual vault commit. */
interface MemoryGitCommitResult {
    readonly committed: boolean;
    readonly sha: string | null;
    /** Why nothing committed (`nothing-to-commit` | `git-unavailable`). */
    readonly reason?: string;
}
/** Outcome of setting/removing the vault's `origin` backup remote. */
interface MemoryGitSetRemoteResult {
    readonly ok: boolean;
    /** Configured origin URL after the call, or `null`. */
    readonly url: string | null;
    readonly reason?: string;
}
/** Outcome of a push/pull against the backup remote. */
interface MemoryGitSyncResult {
    readonly ok: boolean;
    /** Short human-readable detail (git output summary). */
    readonly detail: string;
    /** Why it failed (`git-unavailable` | `not-a-repo` | `no-remote` | git error). */
    readonly reason?: string;
}
/** Where a locker lives: the shared vault-level locker, or one project room's. */
type LockerScope = "vault" | "project";
interface LockerLocation {
    readonly scope?: LockerScope;
    readonly project?: string;
}
/** One locker entry's metadata — id/kind/tags/timestamps. NEVER a value; the
 *  driver never decrypts (that stays agent-tool-only). */
interface LockerEntrySummary {
    readonly id: string;
    readonly kind: string;
    readonly tags: readonly string[];
    readonly createdAt: number;
    readonly updatedAt: number;
}
/** A principal authorized to put/get/delete in a locker. */
interface LockerGrant {
    readonly principal: string;
    readonly note?: string;
    readonly grantedAt: number;
}
interface LockerPolicy {
    readonly defaultDeny: boolean;
    readonly grants: readonly LockerGrant[];
}
type LockerAuditOp = "put" | "get" | "delete" | "grant" | "revoke" | "denied";
/** One audit line — op/principal/timestamp/entry id. NEVER a secret value. */
interface LockerAuditEntry {
    readonly ts: number;
    readonly op: LockerAuditOp;
    readonly principal: string;
    readonly entryId?: string;
    readonly detail?: string;
}
/** Metadata-only overview of a locker: exists?, entry count, grants, recent
 *  audit tail. NEVER a decrypted value. */
interface LockerStatus {
    readonly root: string;
    readonly exists: boolean;
    readonly entryCount: number;
    readonly policy: LockerPolicy;
    readonly recentAudit: readonly LockerAuditEntry[];
}
/**
 * Memory driver — browse, search, and wake-preview ONE vault. Methods beyond
 * the four reads are optional capabilities: UI surfaces an affordance only
 * when the adapter implements it (write door, wake, sibling vaults).
 */
interface MemoryDriver {
    getVaultOverview(): Promise<MemoryVaultOverview>;
    listNotes(query?: MemoryNoteQuery): Promise<MemoryNotePage>;
    /** Resolve one note by ULID; `null` when it does not exist. */
    getNote(noteId: string): Promise<MemoryNoteView | null>;
    /** Semantic recall through the chip — propose → dispose → open the pages. */
    recall(query: MemoryRecallQuery): Promise<readonly MemoryRecallHit[]>;
    /** Wake-bundle preview (capability — memory chips have wake; doc vaults don't). */
    getWakePreview?(): Promise<MemoryWakePreview>;
    /** Human door: author a page directly (capability — may be absent). */
    createNote?(input: MemoryNoteCreate): Promise<MemoryNoteView>;
    /** Human/agent door: patch an existing page (capability — may be absent). */
    updateNote?(input: MemoryNoteUpdate): Promise<MemoryNoteView>;
    /** Project Memory door (capability): lens every registered project's repo
     *  docs into the vault, streaming progress through `onEvent`. */
    sync?(onEvent: (event: MemorySyncEvent) => void): Promise<MemorySyncSummary>;
    /** Git capability (optional): the vault is git-versioned (OKF delegates
     *  history to git). Reads degrade gracefully when the vault is not a repo. */
    gitInfo?(): Promise<MemoryGitInfo>;
    /** Commit history for one note (vault-relative path), newest first. */
    fileHistory?(notePath: string, limit?: number): Promise<readonly MemoryGitCommit[]>;
    /** Unified diff for one note: a commit's patch (`rev`) or uncommitted changes vs HEAD. */
    fileDiff?(notePath: string, rev?: string): Promise<string>;
    /** Stage + commit the whole vault with `message` (manual checkpoint). */
    commit?(message: string): Promise<MemoryGitCommitResult>;
    /** Set/replace/remove the vault's `origin` backup remote (empty url removes). */
    setRemote?(url: string): Promise<MemoryGitSetRemoteResult>;
    /** Push the vault to its backup remote. */
    push?(): Promise<MemoryGitSyncResult>;
    /** Pull the backup remote into the vault. */
    pull?(): Promise<MemoryGitSyncResult>;
    /** Multi-vault capability: every vault this driver can open, incl. itself. */
    listVaults?(): Promise<readonly MemoryVaultRef[]>;
    /** Open a sibling vault as its own scoped driver; ids come from listVaults. */
    openVault?(vaultId: string): MemoryDriver;
    /** Locker capability (optional): metadata-only overview of the encrypted
     *  secrets vault beside this room (the "locked lens") — exists?, entry
     *  count, grants, recent audit tail. NEVER a decrypted value; the Locker's
     *  put/get/grant/revoke stay agent-tool-only and are never exposed here. */
    lockerStatus?(loc?: LockerLocation): Promise<LockerStatus>;
    /** Locker capability (optional): entry names + metadata (kind/tags/
     *  timestamps). NEVER a value. */
    lockerListEntries?(loc?: LockerLocation): Promise<readonly LockerEntrySummary[]>;
}

type TerminalSessionStatus = "starting" | "running" | "exited" | "error";
/** `output` carries only `data` (the new PTY bytes) — never a snapshot. The full
 *  `history` window rides the `updated` snapshot a subscriber receives on attach,
 *  so a live stream costs one delta per chunk instead of the whole buffer.
 *  `closed` means the session is GONE (killed and dropped from the list);
 *  `exited` means the shell ended but the session is still listed. */
type TerminalEventType = "started" | "output" | "updated" | "exited" | "closed" | "error";
interface TerminalSessionRef {
    readonly workspaceId: string;
    readonly terminalId: string;
}
interface TerminalOpenOptions {
    readonly cwd?: string;
    readonly cols?: number;
    readonly rows?: number;
    readonly env?: Readonly<Record<string, string>>;
    readonly shell?: string;
    readonly command?: string;
}
interface TerminalSessionSnapshot {
    readonly ref: TerminalSessionRef;
    readonly workspace: WorkspaceRef;
    readonly cwd: string;
    readonly shell?: string;
    readonly command?: string;
    readonly title: string;
    readonly status: TerminalSessionStatus;
    readonly cols: number;
    readonly rows: number;
    readonly history: string;
    readonly hasVisibleContent: boolean;
    readonly exitCode?: number;
    readonly error?: string;
    readonly createdAt: string;
    readonly updatedAt: string;
}
interface TerminalEvent {
    readonly type: TerminalEventType;
    readonly ref: TerminalSessionRef;
    readonly timestamp: string;
    readonly data?: string;
    readonly snapshot?: TerminalSessionSnapshot;
    readonly exitCode?: number;
    readonly error?: string;
}
type TerminalEventListener = (event: TerminalEvent) => void | Promise<void>;
interface TerminalDriver {
    listTerminals(workspace: WorkspaceRef): Promise<readonly TerminalSessionSnapshot[]>;
    openTerminal(workspace: WorkspaceRef, options?: TerminalOpenOptions): Promise<TerminalSessionSnapshot>;
    writeTerminal(ref: TerminalSessionRef, data: string): Promise<void>;
    resizeTerminal(ref: TerminalSessionRef, cols: number, rows: number): Promise<TerminalSessionSnapshot>;
    closeTerminal(ref: TerminalSessionRef): Promise<void>;
    subscribeTerminal(ref: TerminalSessionRef, listener: TerminalEventListener): Unsubscribe;
}

type WorkspaceFileStatus = "modified" | "added" | "deleted" | "renamed" | "untracked";
type WorkspaceFileContentKind = "auto" | "markdown" | "code";
interface WorkspaceFileStatusEntry {
    readonly path: string;
    readonly status: WorkspaceFileStatus;
}
interface WorkspaceFileTreeOptions {
    readonly maxFiles?: number;
}
interface WorkspaceFileTreeSnapshot {
    readonly workspace: WorkspaceRef;
    readonly paths: readonly string[];
    readonly gitStatus: readonly WorkspaceFileStatusEntry[];
    readonly generatedAt: string;
    readonly truncated?: boolean;
}
interface WorkspaceFileReadOptions {
    readonly maxBytes?: number;
}
interface WorkspaceFileContent {
    readonly workspace: WorkspaceRef;
    readonly path: string;
    readonly content: string;
    readonly kind?: WorkspaceFileContentKind;
    readonly language?: string;
    readonly sizeBytes?: number;
    readonly truncated?: boolean;
    /** True when `path` resolves to a directory — the client reveals/opens it in the tree
     *  instead of rendering code (a directory is not a file-read error). */
    readonly isDirectory?: boolean;
    /** True when the requested path does not exist / is unreadable — the viewer shows a
     *  clean "not found" state instead of falling back to another file. */
    readonly notFound?: boolean;
    /** When a bare/partial path (e.g. `SKILL.md`) matched MANY workspace files, the list
     *  of matching paths — nothing was read (`notFound` is still true), but the viewer
     *  shows an honest "N files match — pick one" instead of a misleading "not found". */
    readonly ambiguousMatches?: readonly string[];
    /** True when `path` is absolute and resolved OUTSIDE the workspace root; the viewer
     *  loads it but the tree does not try to reveal it (`path` is absolute here). */
    readonly outsideWorkspace?: boolean;
    readonly binary?: boolean;
}
interface WorkspaceSeedFile {
    readonly path: string;
    readonly content: string;
    /** Also backfill into an EXISTING vault when missing (never overwrites). Default: fresh-vault-only. */
    readonly writeIfAbsent?: boolean;
    /**
     * Do not seed this file when any listed workspace-relative compatibility path
     * already exists. Prevents a new primary path from shadowing an existing
     * legacy file.
     */
    readonly skipIfExists?: readonly string[];
}
interface EnsureManagedWorkspaceInput {
    readonly workspaceId: string;
    readonly displayName?: string;
    /** Explicit folder; when omitted the engine resolves the host's default vault path. */
    readonly path?: string;
    /** Files written only when the target directory is newly created / still empty. */
    readonly seed?: readonly WorkspaceSeedFile[];
}
type WorktreeMergeMode = "merge" | "pr" | "leave";
type WorktreePromoteMode = "copy" | "move";
interface WorktreeMergeResult {
    readonly mode: WorktreeMergeMode;
    readonly branch: string;
    readonly merged: boolean;
    readonly conflict?: boolean;
    readonly message?: string;
}
/** Outcome of finishing a worktree session (land + move home + remove). A merge
 *  conflict is a legitimate outcome: `ok: false, conflict: true`, worktree kept. */
interface WorktreeFinishResult {
    readonly ok: boolean;
    readonly text: string;
    readonly branch?: string;
    /** The main checkout the session moved back to (set on success). */
    readonly mainRoot?: string;
    readonly merged?: boolean;
    readonly conflict?: boolean;
    /** False when checkout removal was deferred to the engine's prune sweep. */
    readonly removed?: boolean;
}
/** Outcome of moving a session into a worktree. `reused` separates the two
 *  genuinely different things it can do: CREATE a worktree and carry this
 *  checkout's uncommitted work into it, or JOIN one that already holds the
 *  branch — creating nothing, carrying nothing, and possibly shared with another
 *  live session. A surface that reports both with one sentence lies about one. */
interface WorktreeRelocateResult {
    /** The worktree checkout the session now runs in. */
    readonly path: string;
    readonly branch: string;
    /** True when an existing worktree was joined rather than a new one created. */
    readonly reused: boolean;
}
/** Outcome of syncing a worktree (pulling the main checkout's branch into it).
 *  A conflict is a legitimate outcome: `conflict: true`, merge aborted, worktree
 *  intact. `merged: false` without conflict = already up to date. */
interface WorktreeSyncResult {
    readonly branch: string;
    readonly baseBranch?: string;
    readonly merged: boolean;
    readonly conflict?: boolean;
    readonly message?: string;
}
/** One checkout of a repo: the main checkout, one of the engine's session
 *  worktrees, or a FOREIGN worktree made by hand (listable, never auto-pruned). */
interface RepoWorktree {
    readonly path: string;
    /** Checked-out branch; empty for a detached worktree. */
    readonly branch: string;
    readonly session: boolean;
    readonly main: boolean;
}
/** Which existing checkout to attach a session to: its `path` (what
 *  `listAllWorktrees` returns) or the `branch` it holds. At least one is
 *  required — `{}` is a round trip the type system can refuse — and `path` wins
 *  when both are given. */
type WorktreeAttachTarget = {
    readonly path: string;
    readonly branch?: string;
} | {
    readonly path?: string;
    readonly branch: string;
};
interface WorktreePruneResult {
    readonly pruned: readonly string[];
}
type WorkspaceScmChangeKind = "modified" | "added" | "deleted" | "renamed" | "untracked" | "conflicted";
type WorkspaceScmArea = "staged" | "unstaged";
interface WorkspaceScmChangeEntry {
    readonly path: string;
    readonly kind: WorkspaceScmChangeKind;
    readonly additions: number;
    readonly deletions: number;
    readonly oldPath?: string;
}
interface WorkspaceScmBranchInfo {
    readonly current: string | null;
    readonly upstream: string | null;
    readonly ahead: number;
    readonly behind: number;
}
interface WorkspaceScmSnapshot {
    readonly workspace: WorkspaceRef;
    /** git present + the workspace is a repo. */
    readonly available: boolean;
    readonly branch: WorkspaceScmBranchInfo;
    readonly staged: readonly WorkspaceScmChangeEntry[];
    readonly unstaged: readonly WorkspaceScmChangeEntry[];
    readonly conflicts: readonly WorkspaceScmChangeEntry[];
    readonly totals: {
        readonly files: number;
        readonly additions: number;
        readonly deletions: number;
    };
    readonly generatedAt: string;
}
interface WorkspaceScmBranch {
    readonly name: string;
    readonly current: boolean;
    readonly upstream?: string;
}
interface WorkspaceScmCommitResult {
    readonly committed: boolean;
    readonly sha: string | null;
    readonly reason?: string;
}
interface WorkspaceScmActionResult {
    readonly ok: boolean;
    readonly message?: string;
}
interface WorkspaceScmPullRequestResult {
    readonly ok: boolean;
    readonly url?: string;
    readonly message?: string;
}
/** One of a session's commits that `git push` would send. */
interface WorkspaceScmPushableCommit {
    readonly sha: string;
    readonly subject?: string;
}
/** Answer to the only question the environment card asks about a session's
 *  commits: *which of them would `git push` actually send?* Computed live from
 *  git — `git rev-list <push-destination>..HEAD` intersected with the shas the
 *  session recorded — and never remembered, because a remembered answer goes
 *  stale under a rebase, an amend, or a squash-merge and then either offers a
 *  push that can send nothing or hides work the user can still lose. */
interface WorkspaceScmSessionPushable {
    /** A git repo with a resolvable push destination and a session to attribute. */
    readonly available: boolean;
    /** What a push would send to, e.g. "origin/main". Absent when unresolvable. */
    readonly destination?: string;
    /** Newest first, capped for display — `pushableCount` is the real number. */
    readonly pushable: readonly WorkspaceScmPushableCommit[];
    /** Authoritative count; `pushable` may be capped. */
    readonly pushableCount: number;
    /** Recorded but NOT sendable and NOT landed: rebased, amended, discarded.
     *  Reported without an action — no push can send them. */
    readonly unreachableCount: number;
    /** How many commits this session recorded in total. */
    readonly recordedCount: number;
}
/** Pin status of a submodule checkout vs the gitlink recorded in the parent.
 *  `sync` = equal · `ahead` = checkout descends (pointer bump pending) ·
 *  `behind` = recorded pin descends (checkout update needed) ·
 *  `drift` = histories diverged · `unknown` = relationship unavailable ·
 *  `missing` = submodule not initialized. */
type WorkspaceRepoPinState = "sync" | "ahead" | "behind" | "drift" | "unknown" | "missing";
interface WorkspaceRepoTreeEntry {
    /** Directory name (superproject = basename of the repo root). */
    readonly name: string;
    /** Normalized https remote URL of `origin` (superproject only). */
    readonly remote?: string;
    readonly path: string;
    readonly branch: string | null;
    readonly head: string | null;
    readonly ahead: number;
    readonly behind: number;
    /** Changed-file count. */
    readonly dirty: number;
    /** Submodule-only pin facts; absent on the superproject. */
    readonly pin?: {
        readonly recorded: string;
        readonly state: WorkspaceRepoPinState;
        readonly aheadBy?: number;
        readonly behindBy?: number;
    };
}
/** Superproject + every submodule, one entry each ([0] = superproject). */
interface WorkspaceRepoTree {
    readonly workspace: WorkspaceRef;
    readonly available: boolean;
    readonly repos: readonly WorkspaceRepoTreeEntry[];
    readonly generatedAt: string;
}
interface WorkspaceScmCheckpoint {
    readonly ref: string;
    readonly seq: number;
    readonly label: string;
    readonly createdAt: string;
    readonly totals: {
        readonly files: number;
        readonly additions: number;
        readonly deletions: number;
    };
}
interface WorkspaceScmCheckpointResult {
    readonly captured: boolean;
    readonly checkpoint: WorkspaceScmCheckpoint | null;
    readonly reason?: string;
}
interface WorkspaceScmCheckpointRestoreResult {
    readonly ok: boolean;
    readonly safety: WorkspaceScmCheckpoint | null;
    readonly restoredFiles: number;
    readonly message?: string;
}
interface WorkspaceFileBytes {
    readonly workspace: WorkspaceRef;
    readonly path: string;
    /** base64-encoded contents (empty when notFound). */
    readonly base64: string;
    readonly mime?: string;
    readonly sizeBytes: number;
    readonly notFound?: boolean;
    readonly truncated?: boolean;
}
interface WorkspaceDriver {
    listWorkspaces(): Promise<readonly WorkspaceRef[]>;
    getCurrentWorkspace?(): Promise<WorkspaceRef | null>;
    switchBranch?(workspace: WorkspaceRef, branch: string): Promise<WorkspaceRef>;
    getFileTree?(workspace: WorkspaceRef, options?: WorkspaceFileTreeOptions): Promise<WorkspaceFileTreeSnapshot>;
    readFile?(workspace: WorkspaceRef, path: string, options?: WorkspaceFileReadOptions): Promise<WorkspaceFileContent>;
    /** Register a new project by absolute folder path; the registry makes it active. */
    addWorkspace?(path: string): Promise<WorkspaceRef>;
    /** Mark an already-registered project active in the shared registry. */
    setActiveWorkspace?(workspace: WorkspaceRef): Promise<WorkspaceRef>;
    /** Forget a project; resolves to the remaining registered workspaces. */
    removeWorkspace?(workspace: WorkspaceRef): Promise<readonly WorkspaceRef[]>;
    /** Move a project up/down in the host's persisted project order; resolves to
     *  the full registered list in its new order. `anchorWorkspaceId` names the
     *  project the target should land immediately before (up) / after (down) — a
     *  surface that renders only SOME projects MUST pass the neighbour it shows,
     *  or the move trades places with an invisible one and looks like a no-op. */
    reorderWorkspace?(workspace: WorkspaceRef, direction: "up" | "down", anchorWorkspaceId?: string): Promise<readonly WorkspaceRef[]>;
    ensureManagedWorkspace?(input: EnsureManagedWorkspaceInput): Promise<WorkspaceRef>;
    /** Wipe a managed workspace folder and re-provision it from the seed
     *  (engine-guarded: vault-marker folders only). Dev/test reset door. */
    resetManagedWorkspace?(input: EnsureManagedWorkspaceInput): Promise<WorkspaceRef>;
    /** Create a dedicated git worktree on its own branch for a NEW session; the
     *  returned ref's path is the worktree checkout (becomes the session cwd). */
    createSessionWorktree?(workspace: WorkspaceRef): Promise<WorkspaceRef>;
    /** Promote a LIVE session's checkout into a worktree, carrying uncommitted
     *  work; returns the worktree ref to re-point the session at. */
    promoteSessionWorktree?(workspace: WorkspaceRef, mode?: WorktreePromoteMode): Promise<WorkspaceRef>;
    /** Move a session into a worktree: create it and carry uncommitted work, OR
     *  join the one that already holds the branch. The engine relocates the LIVE
     *  session in place when its sidecar owns it (SessionManager.moveTo), else the
     *  closed session file — callers need no close/reopen dance either way.
     *  Resolves with WHICH worktree and whether it was created or joined; a caller
     *  that reports both the same way is lying about one of them. */
    relocateSessionToWorktree?(workspace: WorkspaceRef, sessionId: string, mode?: WorktreePromoteMode, 
    /**
     * Repo-relative paths THIS session owns — its share of a checkout it may
     * not have to itself.
     *
     * The main checkout is shared by every session that has not moved to a
     * worktree, so its dirty state is everyone's. Given this list the engine's
     * carry takes only these paths and leaves every other session's work where
     * it is. Omit it only when the caller genuinely cannot attribute: the
     * engine then carries the lot, which is recoverable but not polite.
     *
     * Source it from `SessionScmLedger.touchedPaths` — the journal-derived
     * attribution — never from the checkout-wide snapshot, which is the very
     * thing that cannot tell whose work is whose.
     */
    ownedPaths?: readonly string[]): Promise<WorktreeRelocateResult>;
    /** Attach a session to a checkout of this repo that ALREADY EXISTS — the main
     *  checkout, another session's worktree, or one made by hand. Identify it by
     *  `path` (what `listAllWorktrees` returns) or by `branch`.
     *
     *  This is NOT `relocateSessionToWorktree`. That one CREATES, keyed on a name it
     *  turns into an `inso/<type>/<slug>` branch, so it cannot join a checkout whose
     *  branch does not follow that convention and refuses once the session is already
     *  in a worktree. Attach creates nothing, carries nothing, and lands nothing: it
     *  only moves this session's cwd, and it works FROM anywhere — between two
     *  worktrees, or back to main without finishing. `finish` remains the only verb
     *  that merges or removes a worktree.
     *
     *  `path` WINS when both are supplied. `reused` in the result is always true by
     *  construction — an attach cannot create. */
    attachSessionToWorktree?(workspace: WorkspaceRef, sessionId: string, 
    /** At least one selector is required — an empty object is a statically
     *  preventable round trip, so the union forbids it. */
    target: WorktreeAttachTarget): Promise<WorktreeRelocateResult>;
    /** Land a session worktree's work: merge to main / push for a PR / leave. */
    mergeSessionWorktree?(workspace: WorkspaceRef, mode: WorktreeMergeMode): Promise<WorktreeMergeResult>;
    /** Finish a worktree session: land its work (merge|pr|leave), move the session
     *  back to the main checkout, remove the worktree. `workspace` is the session's
     *  worktree workspace (path = the worktree checkout). A merge conflict returns
     *  `{ ok: false, conflict: true }` with the worktree kept intact. */
    finishSessionWorktree?(workspace: WorkspaceRef, sessionId: string, merge?: WorktreeMergeMode): Promise<WorktreeFinishResult>;
    /** Pull the main checkout's branch into a worktree session's branch (mid-life
     *  drift relief). `workspace` is the session's worktree workspace. A conflict
     *  returns `{ conflict: true }` with the merge aborted, worktree intact. */
    syncSessionWorktree?(workspace: WorkspaceRef): Promise<WorktreeSyncResult>;
    /** Every checkout of the repo owning `workspace.path` — main, session, foreign. */
    listAllWorktrees?(workspace: WorkspaceRef): Promise<readonly RepoWorktree[]>;
    /** Remove a session worktree (e.g. on session delete). */
    removeSessionWorktree?(workspace: WorkspaceRef, options?: {
        readonly deleteBranch?: boolean;
    }): Promise<void>;
    /** Sweep stale session worktrees; `liveWorktreePaths` are never pruned. */
    pruneSessionWorktrees?(liveWorktreePaths?: readonly string[]): Promise<WorktreePruneResult>;
    /** One-round-trip snapshot: branch, staged/unstaged/conflict entries, totals. */
    scmSnapshot?(workspace: WorkspaceRef): Promise<WorkspaceScmSnapshot>;
    /** Superproject + submodules: branch, ahead/behind, dirty, pin ancestry. */
    scmRepoTree?(workspace: WorkspaceRef): Promise<WorkspaceRepoTree>;
    /** Unified diff for one path on the staged or unstaged side of the index. */
    scmDiffFile?(workspace: WorkspaceRef, path: string, area: WorkspaceScmArea): Promise<string>;
    scmStage?(workspace: WorkspaceRef, paths: readonly string[]): Promise<WorkspaceScmActionResult>;
    scmUnstage?(workspace: WorkspaceRef, paths: readonly string[]): Promise<WorkspaceScmActionResult>;
    scmDiscard?(workspace: WorkspaceRef, paths: readonly string[]): Promise<WorkspaceScmActionResult>;
    scmCommit?(workspace: WorkspaceRef, message: string): Promise<WorkspaceScmCommitResult>;
    scmBranchList?(workspace: WorkspaceRef): Promise<readonly WorkspaceScmBranch[]>;
    scmCheckout?(workspace: WorkspaceRef, branch: string): Promise<WorkspaceScmActionResult>;
    scmCreateBranch?(workspace: WorkspaceRef, name: string): Promise<WorkspaceScmActionResult>;
    scmPull?(workspace: WorkspaceRef): Promise<WorkspaceScmActionResult>;
    /** `sessionId` (optional) lets the backing engine re-reconcile THAT session's
     *  SCM attribution the moment a push actually sends commits — without it the
     *  session's unpushed count stays stale until its next tool call, so the card
     *  reports a successful push and still says the work is local-only. */
    scmPush?(workspace: WorkspaceRef, sessionId?: string): Promise<WorkspaceScmActionResult>;
    /** Which of `sessionId`'s commits `git push` would send RIGHT NOW. The engine
     *  computes this live from git on every call — the journal only records WHICH
     *  shas the session made, never whether they are still pushable, because that
     *  answer changes under a rebase, an amend, or a squash-merge. */
    scmSessionPushable?(workspace: WorkspaceRef, sessionId: string): Promise<WorkspaceScmSessionPushable>;
    scmCreatePr?(workspace: WorkspaceRef, options?: {
        readonly title?: string;
        readonly body?: string;
    }): Promise<WorkspaceScmPullRequestResult>;
    /** Snapshot the working tree to a hidden ref for this session; skipped when
     *  nothing changed since the last checkpoint. */
    scmCheckpointCapture?(workspace: WorkspaceRef, sessionId: string, label: string): Promise<WorkspaceScmCheckpointResult>;
    /** Every checkpoint for the session, newest-first. */
    scmCheckpointList?(workspace: WorkspaceRef, sessionId: string): Promise<readonly WorkspaceScmCheckpoint[]>;
    /** Files changed by the turn that produced a checkpoint (vs the previous one). */
    scmCheckpointDiff?(workspace: WorkspaceRef, ref: string): Promise<readonly WorkspaceScmChangeEntry[]>;
    /** Rewrite the working tree to a checkpoint (auto-captures a safety snapshot first). */
    scmCheckpointRestore?(workspace: WorkspaceRef, sessionId: string, ref: string): Promise<WorkspaceScmCheckpointRestoreResult>;
    /** Raw file bytes (base64) for image / PDF / binary previews. */
    readFileBytes?(workspace: WorkspaceRef, path: string): Promise<WorkspaceFileBytes>;
}

/** A model profile's role → model map; a role holds ONE pattern or an ordered
 *  fallback STACK (both accepted by the engine's resolveConfiguredModelPatterns). */
type ProfileRoleMap = Record<string, string | readonly string[]>;
type ModelProfiles = Record<string, ProfileRoleMap>;

/**
 * The shared activity-state vocabulary. Every activity surface derives one of
 * these from its own data and renders it via {@link ActivityDot}. Colors and
 * motion live in the spec below, never at the call site — a consumer speaks in
 * states, so the rail, the fleet, and the run views can never disagree.
 *
 * MEANING + DETECTION per state (this is the canon):
 *
 * - `working` — a foreground agent turn is running RIGHT NOW.
 *     DETECTION: session `status === "running"`; a live in-flight loop tick.
 * - `needs-you` — parked awaiting YOUR decision. The one attention-grade state,
 *     and the only one that leaves the (user-recolorable) accent family for a
 *     FIXED violet, so "you are needed" always reads the same and never blurs
 *     into working's soft pulse.
 *     DETECTION: a loop with an OPEN ask (`ask.verdict === undefined`) or a run
 *     whose outcome is `awaiting-approval`. (Sessions: see the seam in
 *     `sessionDot`/`sessionLifecycle` — catalog-level blocked-on-input is not on
 *     the wire yet.)
 * - `background` — work continues in the background while the foreground idles;
 *     ALSO a loop that needs ATTENTION (failing / quarantined) — the amber signal.
 *     DETECTION: session `hasBackgroundWork`; a loop `quarantined` / failing.
 * - `attached` — loaded into memory but idle: warm and ready, not working.
 *     DETECTION: session `loaded` and not running.
 * - `failed` — the last run ended in failure.
 *     DETECTION: session `status === "failed"`.
 * - `ok` — healthy: on, not failing, not awaiting; a run that completed.
 *     DETECTION: a loop that is on and clear.
 * - `off` — deliberately switched off (a paused loop), or a loop whose live
 *     health is unknown — a neutral, quiet muted dot.
 *     DETECTION: a loop `paused`; a rail loop row with no pulse reading.
 * - `idle` — a settled session at rest; nothing live to signal. A quiet muted
 *     grey dot (the historic settled-session dot), sharing `off`'s look — the two
 *     differ in MEANING (idle = a session at rest, off = a deliberately-off loop),
 *     not in pixels.
 *     DETECTION: a settled session with no other signal.
 */
type ActivityState = "working" | "needs-you" | "background" | "attached" | "failed" | "ok" | "off" | "idle";

/** A loop-tick session from the catalog, as the runs sidebar consumes it. */
interface LoopSidebarSession {
    readonly ref: SessionRef;
    readonly firedAt: number;
    readonly preview?: string;
    /** The session is running RIGHT NOW (catalog `status === "running"`) — session
     *  truth for the live row, independent of the pulse's in-flight reading. */
    readonly running?: boolean;
}

interface ModelsHomeProps {
    readonly models: readonly EngineModelRecord[];
    readonly providers?: readonly EngineProviderRecord[];
    readonly profiles: ModelProfiles;
    readonly defaultRoles?: Readonly<Record<string, string>>;
    /** The engine's `modelRoles.*` spine — ordered role ids with their display
     *  names, straight from the running fork's catalog. Same set and order as
     *  `defaultRoles` (both are built from the same records); it adds the labels
     *  that map does not carry. Absent on an engine exposing no role records, in
     *  which case the surface falls back to the `defaultRoles` keys. */
    readonly engineRoles?: readonly {
        readonly id: string;
        readonly name: string;
    }[];
    readonly engineDefault?: {
        readonly provider: string;
        readonly modelId: string;
    };
    readonly insights?: Readonly<Record<string, EngineModelInsight>>;
    readonly insightsMeta?: {
        readonly fetchedAt?: number;
        readonly sources?: readonly string[];
        readonly stale?: boolean;
    };
    readonly onDefaultRoleChange: (role: string, pattern: string | undefined) => void;
    readonly onSave: (name: string, roles: Record<string, readonly string[]>) => void;
    readonly onDelete?: (name: string) => void;
    readonly onModelAgent?: (seed: string) => void;
    readonly className?: string;
}

/**
 * How ONE item draws itself in a switcher (doc 44 §4.5, owner ruling
 * 2026-08-08). Discord and Slack are the reference: the leftmost column is a
 * wall of marks, and the mark IS the thing's identity.
 *
 * Generic on purpose — a mark belongs to a `SwitcherItem`, not to a space.
 *
 * Four ways to draw one, in falling precedence; the FIRST present wins:
 *
 * 1. `fill` — an implementation id resolved through the registry to a real
 *    component. The owner's case verbatim: *"somebody can choose to write a
 *    very nice animated orb"*. A mark is the smallest possible slot, which is
 *    exactly why it is worth having: an item can be alive in the switcher
 *    without the host knowing anything about what it draws.
 * 2. `thumbnail` — an image URL (a data-URI, or a path the client composes
 *    against its asset route).
 * 3. `icon` — a shipped glyph, overriding the item's own `icon`.
 * 4. nothing — the host falls back to `SwitcherItem.icon`, which is required.
 *    So there is never a blank cell.
 */
interface Mark {
    readonly fill?: ImplId;
    readonly thumbnail?: string;
    readonly icon?: IconName;
    /** Any CSS colour. Tints the active ring and the hover glow — never a
     *  background fill, which would fight the host's mat (DESIGN.md). */
    readonly accent?: string;
    /** Overrides the tooltip/aria label. Defaults to `SwitcherItem.label`. */
    readonly label?: string;
}

/**
 * A pinned widget's STATION in the workspace pane — one of its four corners.
 *
 * Lives in the shell rather than in the designer because a SPACE has to be
 * able to express it: a pin the designer can draw but a space cannot declare
 * is a pin that never survives the save. The app's environment card sits at
 * top-right; the post is the user's call.
 */
type PinPost = "top-left" | "top-right" | "bottom-left" | "bottom-right";
/**
 * Who names the top bar while a surface is mounted.
 *
 * - `session` — the session breadcrumb and branch chip (the shipped default,
 *   and correct for a surface that IS a session).
 * - `surface` — the mounted surface names itself. The shell renders no
 *   session breadcrumb and no branch chip; the filling supplies the title
 *   through the frame's title slot, so an artifact surface reads as the
 *   artifact rather than as whatever session happens to be active.
 * - `none` — no identity at all; a whole-page takeover.
 *
 * A surface that says nothing gets `session`, which is what every space got
 * before this existed.
 */
type SurfaceChrome = "session" | "surface" | "none";
/**
 * Which sessions belong to a space.
 *
 * - `{ workspace }` — a fixed workspace id. The space is a ROOM, not a view
 *   over the user's projects: its rail lists that workspace's sessions and
 *   nothing else.
 * - `{ agent }` — sessions created as one General Agent, wherever they live.
 *   The space is a BENCH: a kind of work, spread across projects.
 *
 * Omitted ⇒ the cross-project catalog minus every session another space has
 * claimed by agent. That residual is what a general coding space wants, and
 * it is derived rather than declared so adding a bench never strands its runs
 * in two lists at once.
 */
type SpaceScope = {
    readonly workspace: string;
} | {
    readonly agent: string;
};

/** The UI SLICE of a space definition — what the SHELL needs to assemble a
 *  space: identity, slot implementations, rail actions, and the workspace
 *  surface registry (doc 44 §11.1's shell-facing subset).
 *
 *  The full `SpaceDef` (General Agents · `requires` · pipelines) is the Space
 *  SDK's contract and EXTENDS this type in `@inso/space-sdk` — Fraym cannot
 *  typecheck agents or capabilities and never imports the SDK (doc 44 §13,
 *  repo homes; dependency strictly downward). */
interface SpaceUiDef {
    readonly specVersion: 1;
    /** Stable wire id; built-in and contributed boxes use the same open set. */
    readonly id: SpaceId;
    readonly label: string;
    readonly icon: IconName;
    /** Ascending switcher order. Omitted definitions sort at 100. */
    readonly order?: number;
    /** A quiet one-line descriptor ("the build cockpit"). Feeds
     *  `SwitcherItem.hint`, which the dropdown shows as a second line and a dense
     *  rail folds into its tooltip. Added because the field existed on the
     *  generic side with nothing upstream to fill it — a decorative declaration
     *  (doc 44 §13) either gets a source or gets deleted. */
    readonly hint?: string;
    /** How this space draws itself wherever it is listed. Absent ⇒ the shell
     *  falls back to `icon`, so every space has a mark and no cell is blank.
     *
     *  The type is the GENERIC `Mark` — the space is one kind of thing a
     *  switcher can list, not the reason the type exists. */
    readonly mark?: Mark;
    /** Which filling mounts in each UI kind slot (defaults: the classic set —
     *  `DEFAULT_SPACE_IMPLS`). User/deployment overrides layer per doc 44 §10.
     *
     *  `switcher` is the app's chrome, not this space's — a space only ever
     *  PREFERS one, exactly as `rail.preferredMode` prefers a rail width, and
     *  the shell restores the user's choice on exit. Without that rule two
     *  spaces would fight over a control that outlives both of them. */
    readonly implementations?: {
        readonly rail?: ImplId;
        readonly dock?: ImplId;
        readonly workspace?: ImplId;
        readonly switcher?: ImplId;
    };
    /** Widgets this space mounts, by how they OCCUPY it (doc 56 §3.2).
     *
     *  `pinned` sit at a fixed post in the workspace chrome — chrome citizens,
     *  never slot cargo. `placed` occupy the start surface's widget region.
     *
     *  FLOATING widgets are deliberately NOT expressible yet: doc 56 §3.2
     *  records that they still need their own contract, and a field with no
     *  contract behind it is the costume doc 44 §2.1 rule 1 bans. */
    readonly widgets?: {
        readonly pinned?: readonly {
            readonly fill: string;
            readonly post: PinPost;
        }[];
        readonly placed?: readonly string[];
    };
    readonly rail: {
        /** Optional box preference applied on entry; the shell restores the
         * prior mode on exit. Omit to leave the user's rail unchanged. */
        readonly preferredMode?: "expanded" | "compact" | "hidden";
        /** Ordered action bindings (doc 44 §11.2) — the content-layer registry;
         *  the persona preset and the user's rail-customize ticks arrange it
         *  downstream (availability vs arrangement, doc 37 §7). */
        readonly actions: readonly RailActionDef[];
    };
    readonly workspace: {
        /** This space's surface registry — a `mount` intent naming a surface
         *  outside it is refused by the executor. */
        readonly surfaces: readonly SurfaceId[];
        /** Surface id → namespaced workspace-fill id. The host resolves these
         * against installed first-party or bridged marketplace fill code. */
        readonly fills?: Readonly<Record<SurfaceId, string>>;
        /** The `sessionMode:"none"` start surface. The shipped shell derives
         *  start-vs-session from session presence on ONE surface id. */
        readonly start: SurfaceId;
        readonly session: SurfaceId;
        readonly switchPolicy?: "remember" | "resetToStart";
        /**
         * Top-bar affordance ids this space publishes — `jobs` · `refresh` ·
         * `environment` · `dock`. Absent ⇒ the deployment's allowlist decides
         * alone, so a space that says nothing keeps whatever the product ships.
         *
         * A space declares these because they are NOT universal: the environment
         * card reports a repo, a worktree and a push, which is the point of Code
         * and noise in a space that watches an artifact. Before this existed the
         * shell hardcoded `appMode === "code"` for exactly that reason — and the
         * card itself carried no matching gate, so every other wide-screen space
         * rendered an environment overlay with no button able to close it.
         *
         * An id this build does not ship is IGNORED, never refused: action ids are
         * the shell's vocabulary, and a space naming a newer one must degrade to
         * absence rather than fail to install.
         */
        readonly actions?: readonly string[];
        /**
         * Which sessions this space claims (see {@link SpaceScope}).
         *
         * Declared because it was hardcoded three ways before this existed: the
         * chat bench pinned to the vault workspace, the design bench filtered on
         * a literal profile id, and everything else took the remainder — all in
         * one `if/else` chain that a contributed space could not enter. A space
         * that ships in a plugin had no way to say "my sessions are mine", so it
         * inherited the user's whole project list and the top bar named a repo
         * the space has nothing to do with.
         */
        readonly scope?: SpaceScope;
        /**
         * Surface id → who names the top bar while it is mounted
         * (see {@link SurfaceChrome}). Mirrors `fills`: a partial map, keyed by
         * surface, defaulting per-surface rather than per-space — because one
         * space legitimately holds both a session surface and an artifact
         * surface, and they do not want the same chrome.
         */
        readonly chrome?: Readonly<Record<SurfaceId, SurfaceChrome>>;
    };
}

type VibrMode = "" | "search" | "read" | "run" | "edit" | "skill" | "mcp" | "think";
type VibrState = "idle" | "thinking" | "typing";

/** A space's id on the wire. The built-in three ride the FROZEN values
 *  `chat` (Aether) · `studio` · `code`; step ④ of the Space System opened the
 *  set to community ids (a contributed space's `SpaceUiDef.id`), so the type
 *  is the open census (`SpaceId` aliases it in `shell/space/space-state`). */
type AppMode = string;
type MenuType = "palette" | "model" | "perm" | "context" | "mcp" | "user" | "filter" | "dock" | null;
interface MenuState {
    readonly type: MenuType;
    readonly rect: DOMRect | null;
    /** Set when the filter menu is opened from a project group's gear: scopes the
     *  menu (and the session list) to this project and hides the project/group-by rows. */
    readonly project?: string;
}

type RailActionTarget = "new-session" | "settings" | "plugins" | "loops" | "models" | "memory" | "lab" | "surface";
/** A rail action button. `target` is the page/handler it opens. A deployment
 *  profile supplies its own ordered list (default `DEFAULT_RAIL_ACTIONS`) to add,
 *  remove, or reroute rail actions — no hardcoded conditions in the component. */
interface RailActionDef {
    readonly id: string;
    readonly label: string;
    readonly icon: IconName;
    readonly target: RailActionTarget;
    /** Surface id mounted by `target: "surface"`. */
    readonly surface?: string;
    readonly kbd?: string;
    readonly primary?: boolean;
    readonly disabled?: boolean;
}

/** A space's id. Step ① rides the frozen wire noun (`appMode`: chat/studio/code);
 *  step ③'s SpaceRegistry makes the set open (doc 44 §11). User nouns: Aether ·
 *  Studio · Code. */
type SpaceId = AppMode;
/** A workspace-surface id — what the workspace host mounts (doc 44 §4.2).
 *  Namespaced `<plugin>.<name>` for community surfaces; built-ins own the bare
 *  namespace (doc 44 §4.1). */
type SurfaceId = string;
/** A kind implementation's id — which filling of a UI kind slot to mount
 *  (doc 44 §4: "five rails"). Namespaced `<plugin>.<name>` for community
 *  implementations; built-ins own the bare namespace. */
type ImplId = string;
/** Route takeovers that are NOT surfaces: settings and plugins are DOORS to the
 *  settings route (doc 44 §11.2 — "Capabilities" is a door, never a surface).
 *  "app" = the workspace shows the active space's surface. */
type ShellRoute = "app" | "settings" | "plugins";
/** Built-in surface ids, step ① scope. "session" = the workspace's session/start
 *  pair (State A/B — surface selection between start and session stays derived
 *  from session presence exactly as shipped; splitting it is step ② work).
 *  Machinery (autonomy · models) = shared workspace surfaces mountable from any
 *  space (doc 44 §11.2). Rooms are per-space. */
declare const SURFACE: {
    readonly session: "session";
    readonly autonomy: "autonomy";
    readonly models: "models";
    /** Aether rooms (canon: workspace surfaces reachable from the rail). */
    readonly palace: "palace";
    readonly lab: "lab";
    /** Studio pipeline navigation. */
    readonly library: "library";
    readonly pipelines: "pipelines";
};

interface WorkspaceAnalyticsState {
    readonly available: boolean;
    readonly loading: boolean;
    readonly refreshing: boolean;
    readonly range: AnalyticsRange;
    readonly setRange: (range: AnalyticsRange) => void;
    readonly snapshot: AnalyticsSnapshot | null;
    readonly error: string | null;
    readonly refresh: () => Promise<void>;
    /** "Test my models now" — a real, billed API call per model, only ever fired by an explicit click. */
    readonly runBenchmark: (models: readonly string[], runs?: number) => Promise<readonly AnalyticsBenchResult[]>;
}

/** A compose-time image pasted/dropped into the composer, pending send. `data` is RAW
 *  base64 (no `data:` prefix); preview + transport both derive from it. */
interface ComposerImageAttachment {
    readonly id: string;
    readonly name: string;
    readonly mimeType: string;
    readonly data: string;
    /** Char offset of this image's zero-width pill, persisted with unsent session drafts. */
    readonly inlineOffset?: number;
}

/** Result of a composer submit: `false` keeps the draft, anything else clears it. */
type ComposerSubmitResult = boolean | Promise<boolean>;
/** Submit handler signature shared by every composer surface. */
type ComposerSubmit = (value: string, attachments?: readonly ComposerImageAttachment[]) => ComposerSubmitResult;

interface DockTab {
    readonly id: string;
    readonly label: string;
    readonly icon: IconName;
    readonly badge?: number;
}

interface SessionItem {
    readonly id: string;
    readonly sessionRef?: SessionRef;
    readonly title: string;
    /** A pulse-confirmed autonomy tick with no catalog session yet. Renders as a
     * non-clickable locating shimmer until the relist replaces it with a real row. */
    readonly locating?: boolean;
    /** Session origin, from the driver `SessionSnapshot.source`. `"autonomy"` marks a
     *  Loop tick; the Loops grouping classifies runs by this (with the `loop: ` title
     *  prefix as display + pre-source fallback). Absent → a human session. */
    readonly source?: "user" | "autonomy";
    /** Leading glyph shown in place of the status dot. Absent → the normal status
     *  dot / presence avatar renders. */
    readonly icon?: IconName;
    /** Explicit activity STATE for this row's dot — wins over `icon` and the
     *  derived `status` dot. The Loops rows set it to say live status (see
     *  {@link ActivityState}): `needs-you` = a run awaits you (violet + ping),
     *  `background` = attention, `off` = paused/unknown, `ok` = healthy. */
    readonly dotState?: ActivityState;
    readonly time: string;
    /** The rail dot's semantic state, derived once in `sessionDot` from the
     *  session's lifecycle. Renders via the shared {@link ActivityDot}. */
    readonly status: ActivityState;
    readonly active?: boolean;
    readonly preview?: string;
    readonly updatedAt?: string;
    /** Session creation time (ISO), derived from the UUIDv7 session id. Powers the
     *  Created sort and the stable "oldest live work on top" order in Activity sort. */
    readonly createdAt?: string;
    /** Pinned to the top of its group/list, above every sort mode. Engine-backed (`pinnedAt`). */
    readonly pinned?: boolean;
    readonly archived?: boolean;
    /** This session runs in its OWN git worktree (its workspace is a linked
     *  worktree); drives the per-row worktree icon. */
    readonly worktree?: boolean;
    /** The worktree's branch (e.g. `fraym/feature/rail-badge`), shown on row hover. */
    readonly worktreeBranch?: string;
    /** The session workspace's current branch (worktree or not) — metadata line fuel. */
    readonly branch?: string;
    /** Pre-composed second metadata line (branch · worktree). Present ONLY when the
     *  Filters "Metadata" density is Show — the rail renders it verbatim. */
    readonly meta?: string;
    /** Absolute filesystem path of the session's workspace root — powers "Reveal
     *  in Finder/Explorer" on the session context menu (desktop only). */
    readonly workspacePath?: string;
    /**
     * Display label of the BODY this session runs on — doc 48 §6, the rail badge.
     *
     * Set this ONLY when the session runs on a body OTHER than the host's default;
     * the rail renders it unconditionally. A badge on every row is noise, while one
     * that appears exactly when the body is unusual is signal — and it keeps the
     * rail free of any notion of which body is "the" default, which is the host's
     * business, not a list row's.
     */
    readonly harnessLabel?: string;
    /**
     * The body's own mark, as an image source — the vendor logo its pack ships.
     *
     * Absent means the pack ships no logo (or none could be resolved), and the badge
     * falls back to the generic glyph. Never a bare glyph name: the resolver hands
     * over a usable `<img src>` or nothing, so this can never render broken.
     */
    readonly harnessIcon?: string;
}
interface RepoGroup {
    readonly repo: string;
    readonly branch: string;
    /** True when this group's checkout is a linked git worktree (not the main one);
     *  drives the rail's worktree badge. Sourced from `workspace.git.worktree`. */
    readonly worktree?: boolean;
    readonly dot: string;
    /** Header glyph for the group (default `folder`). The lifted Autonomy group sets
     *  `orbit` so loop ticks read as a distinct, non-project surface. */
    readonly icon?: IconName;
    /** Per-project rail image src (data:/http URL). Wins over `icon`/`emoji`. */
    readonly image?: string;
    /** Per-project emoji marker (rendered as text). Wins over `icon`, loses to `image`. */
    readonly emoji?: string;
    readonly items: readonly SessionItem[];
    /** Per-group "Show more" limit (from the effective per-project/global filter). Falls back
     *  to the rail's `collapseAfter` prop when unset. */
    readonly collapseAfter?: number;
    /** The workspace this group belongs to — powers the per-group "new session here" action. */
    readonly workspace?: WorkspaceRef;
}

/** A lab in the rail — one per organ. Unbuilt labs render disabled, never faked. */
interface LabItem {
    readonly id: string;
    readonly label: string;
    readonly icon?: IconName;
    /** Tooltip line describing the organ (e.g. "memory — vault · reflex · cortex"). */
    readonly organ?: string;
    /** Live labs are selectable; everything else renders dimmed with a "soon" meta. */
    readonly live?: boolean;
}
/** A collapsible group of labs in the rail (e.g. "Aether" → its organs). Data-driven. */
interface LabGroup {
    readonly label: string;
    readonly icon?: IconName;
    readonly items: readonly LabItem[];
}
interface LabBenchmarkItem {
    readonly id: string;
    readonly label: string;
    readonly icon?: IconName;
    /** Right-aligned meta — a pass rate, "soon", a run count. */
    readonly meta?: string;
    /** Planned/unbuilt — rendered dimmed but still selectable (blueprint pages). */
    readonly planned?: boolean;
}
interface LabBenchmarkGroup {
    readonly label: string;
    readonly icon?: IconName;
    readonly items: readonly LabBenchmarkItem[];
}
/** Host-injected Lab configuration — fraym stays engine- and product-agnostic. */
interface FraymLabConfig {
    /** Lab registry shown at the top of the rail. */
    readonly labs?: readonly LabItem[];
    /** Agent groups (e.g. "Aether" → its organs) — the Agents section, rendered from data. */
    readonly agentGroups?: readonly LabGroup[];
    /** Stage renderer for a selected live lab id — the host owns the instrument. */
    readonly renderLab?: (labId: string) => ReactNode;
    /** Benchmark registry shown below the labs in the rail. */
    readonly benchmarkGroups?: readonly LabBenchmarkGroup[];
    /** Stage renderer for a selected benchmark id; `openBenchmark` lets in-page links re-route while keeping the rail selection in sync. */
    readonly renderBenchmark?: (benchmarkId: string, openBenchmark: (benchmarkId: string) => void) => ReactNode;
}

type AethrState = "idle" | "nudge" | "dreaming" | "uncertain" | "satisfied";

/**
 * The Machinist surface registry — one row per config surface that can summon
 * the Machinist (the workbench-configuration agent; design canon doc 43).
 *
 * A new config surface joins the Machinist by adding a row here and wiring its
 * page's "ask the agent" door through the shared `onOpenMachinist` chain — the
 * agent, its hidden workspace, and the dock chrome are shared by every row.
 * Seeds stay PAGE-owned (they are view-aware, e.g. the Models dossier seed);
 * the registry owns the surface's identity and dock label.
 */
type MachinistSurfaceId = "autonomy" | "models" | "plugins" | "pipelines";

/** A workspace-surface implementation registered by an installed space box.
 *
 * Fraym owns the host and passes the host contract down; the box owns the
 * component. Keeping this type generic makes the registry a dependency leaf —
 * it never imports the shell's large host-props interface. */
type WorkspaceSurfaceFill<Props> = ComponentType<Props>;
/** Namespaced fill id → implementation. Hosts merge their registered fills over
 * Fraym's defaults; a missing id must render an honest notice, never vanish. */
type WorkspaceSurfaceFills<Props> = Readonly<Record<string, WorkspaceSurfaceFill<Props>>>;

/** The ONLY mutation of shell navigation state: a typed message an
 *  implementation emits; the executor is the single writer (doc 44 §5).
 *
 *  Step ① carries exactly the intents with a live consumer — the navigation
 *  subset collapsing the retired View/PersonalView/StudioView enums. Later
 *  steps extend the union as each new intent gains a real consumer
 *  (dock.open/close · split · pane.*): the
 *  decorative-declaration law (doc 44 §13) forbids declaring them early. */
type ShellIntent = 
/** Mint the space's unit of work (session · run · thread) and land on the
 *  session surface. Session creation itself is the caller's side effect —
 *  the executor owns only where navigation goes. */
{
    readonly t: "create";
}
/** Mount a workspace surface — the rail's surface actions, room entries,
 *  machinery pages. Refused (state returned unchanged) when the active
 *  space's registry does not list the surface. */
 | {
    readonly t: "mount";
    readonly surface: SurfaceId;
}
/** A context-carrying jump that is NOT a surface: the settings/plugins
 *  route takeover (doc 44 §11.2 — Capabilities is a door). `pane` is the
 *  door's carried context (which settings pane to land on); the executor
 *  ignores it — the wiring site applies it as a side effect. */
 | {
    readonly t: "door";
    readonly route: Exclude<ShellRoute, "app">;
    readonly pane?: string;
}
/** Leave a door's route takeover back to the workspace. */
 | {
    readonly t: "door.close";
}
/** The space switcher. Per-space activeSurface is remembered
 *  (switchPolicy "remember", doc 44 §11.1). */
 | {
    readonly t: "space.switch";
    readonly space: SpaceId;
}
/** Open the Machinist dock seeded to author or edit one of a config
 *  surface's records — the door Autonomy, Models, Plugins and Pipelines all
 *  share. `seed` is the composer's opening prompt, written by the surface
 *  because that wording is its product copy, not the shell's. */
 | {
    readonly t: "machinist.open";
    readonly surface: MachinistSurfaceId;
    readonly seed: string;
};

/** The dock slot contract's props (doc 44 §4.3) — Data in · Intents out.
 *
 *  specVersion 1 hands the implementation the whole workspace frame: the
 *  shipped dock reads its tabs, the Machinist page-dock state, drivers, and
 *  resize plumbing straight off it. Narrowing this to a dock-only projection
 *  is step-③/④ work — the contract records today's true surface, it does not
 *  invent a smaller one (doc 44 §13, decorative-declaration law).
 *
 *  Duties:
 *  - Render the space's dock tabs; honor `dockOpen`/`dockTab`.
 *  - Surface the Machinist page dock when its state is engaged.
 *  - Never own navigation truth — tab changes ride the frame's callbacks. */
interface DockSlotProps {
    readonly frame: FraymFrameWorkspaceProps;
}
/** One installable filling of the dock slot. */
interface DockImplementation {
    readonly specVersion: 1;
    readonly id: ImplId;
    readonly slot: "dock";
    readonly component: ComponentType<DockSlotProps>;
}

/** The workspace-host slot contract's props (doc 44 §4.2) — Data in · Intents out.
 *
 *  The workspace host is the pane container that mounts the active surface:
 *  splits/tiling, the chat presence backdrop, and the room overlays ride here.
 *  specVersion 1 carries the shipped pane surface verbatim (frame + tiling +
 *  presence state); a surface-registry projection is step-③ work.
 *
 *  Duties:
 *  - Mount exactly the active space's `activeSurface`; a mount REPLACES it.
 *  - Keep tiling/splits per space; never leak panes across a space switch.
 *  - Never own navigation truth — emit ShellIntents through the frame. */
type WorkspaceHostSlotProps = WorkspaceMainPaneProps;
/** One installable filling of the workspace-host slot. */
interface WorkspaceHostImplementation {
    readonly specVersion: 1;
    readonly id: ImplId;
    readonly slot: "workspace";
    readonly component: ComponentType<WorkspaceHostSlotProps>;
}

type PresenceState = ComponentProps<typeof Presence>["state"];
interface WorkspaceSplitRef {
    readonly sessionRef: SessionRef;
    readonly title: string;
}
interface WorkspacePanePopoutDragState {
    readonly title: string;
    readonly x: number;
    readonly y: number;
    readonly armed: boolean;
}
interface WorkspaceTilingState {
    readonly panes: readonly WorkspaceSplitRef[];
    readonly widths: readonly number[];
    readonly dragOver: boolean;
    readonly dropIndex: number | null;
    readonly dropPreviewCount: number;
    readonly popoutDrag: WorkspacePanePopoutDragState | null;
    readonly dropTargetRef: RefObject<HTMLDivElement | null>;
    readonly activePaneKey: string | null;
    readonly onDragOver: (event: DragEvent<HTMLDivElement>) => void;
    readonly onDragLeave: (event: DragEvent<HTMLDivElement>) => void;
    readonly onDrop: (event: DragEvent<HTMLDivElement>) => void;
    readonly onPaneFocus: (pane: WorkspaceSplitRef, selectSession: boolean) => void;
    readonly onPaneClose: (pane: WorkspaceSplitRef) => void;
    readonly onPanePopoutDragStart: (pane: WorkspaceSplitRef, title: string, event: PointerEvent<HTMLElement>) => void;
    readonly onSplitResizeStart: (index: number, event: PointerEvent<HTMLButtonElement>) => void;
}
interface ThreadStageProps {
    readonly isOpeningSession: boolean;
    readonly awaitingTranscript: boolean;
    readonly enteredFromStartSurface: boolean;
    readonly title: string;
    readonly repo: string;
    readonly avatar: AvatarId;
    readonly vibrState: PresenceState;
    readonly vibrMode: string;
    readonly energy: number;
    readonly verb: string;
    readonly showPresence: boolean;
    readonly showAvatar: boolean;
    readonly agentMeta: string;
}
type WorkspaceSurfaceSubmit = (value: string, attachments?: readonly ComposerImageAttachment[], expansion?: string) => ComposerSubmitResult;
interface WorkspaceSurfaceHostProps {
    readonly appMode: AppMode;
    /** The active space's mounted workspace surface (doc 44 §5). */
    readonly activeSurface: SurfaceId;
    /** ONE navigation channel: workspace affordances emit typed ShellIntents. */
    readonly onIntent: (intent: ShellIntent) => void;
    /** Active definition + code registry are separate by design: manifest data
     * chooses a fill id; the installed box supplies its React implementation. */
    readonly activeSpace: SpaceUiDef;
    readonly surfaceFills: WorkspaceSurfaceFills<WorkspaceSurfaceHostProps>;
    readonly enginePipelines?: readonly EnginePipelineRecord[];
    readonly enginePipelineIssues?: readonly EnginePipelineIssue[];
    /** Start a run from the Pipelines surface. Absent → it browses only. */
    readonly engineDesignSystems?: readonly EngineDesignSystemRecord[];
    /** Plugin-contributed artifactory packs + the host's bundle-URL composer. */
    readonly engineArtifactories?: readonly EngineArtifactoryRecord[];
    readonly resolvePluginAssetUrl?: (pluginId: string, path: string) => string | null;
    /** The bundled team (jury) + evaluator catalogs — resolve a pipeline stage's
     *  critic team + evaluators to human labels in the pipeline detail view. */
    readonly engineTeams?: readonly EngineTeamRecord[];
    readonly engineEvaluators?: readonly EngineEvaluatorRecord[];
    readonly showStartSurface: boolean;
    /** The transcript is UNKNOWN, not empty — the engine's replay has not landed yet.
     *  A surface that renders its own thread should keep a loading face rather than
     *  conclude the session is empty. See `SessionState.journalHydrated`. */
    readonly awaitingTranscript: boolean;
    /** The thread is mounting because the user just SENT from the start surface, so
     *  their message is a genuine arrival and animates in. */
    readonly enteredFromStartSurface: boolean;
    readonly isOpeningSession: boolean;
    /** A brand-new session is being created (as opposed to an existing one being
     *  opened). Both fold into `isOpeningSession` — so a fill that renders its own
     *  session view needs this to tell them apart, or it keeps the PREVIOUS session's
     *  content on screen for the whole create round-trip. Measured 2026-07-29 in Studio: ~1.15s of
     *  the last run's stage after pressing New session, then a rearrange. A fill
     *  should show its start surface as soon as this is true.
     *
     *  Holds true through the created session's OPEN as well — the raw flag drops
     *  at the create RPC, but until the minted session's transcript hydrates a
     *  fill would route to its stage over a known-empty session: a blank flash
     *  between start surface and start surface (measured 2026-07-31 in Studio).
     *  The model latches this prop until the created session can answer. */
    readonly isCreatingSession?: boolean;
    /** The composer cannot ADDRESS a session at all (no ref and no create-draft lane),
     *  as opposed to merely waiting on one to open. This — never `isOpeningSession` —
     *  is what disables a composer: the hero composer is live for the whole create. */
    readonly composerBlocked: boolean;
    readonly threadRef: RefObject<HTMLDivElement | null>;
    readonly sessionRef: SessionRef | null | undefined;
    readonly sessionCatalog: readonly SessionSnapshot[];
    readonly sessionDriver: SessionDriver | null | undefined;
    /** LEGACY. Read only by the built-in Aether palace overlay, which is deleted
     *  with that space. A space BOX must not reach for this: a vault, an OKF note
     *  and a quarantine taint are the product's domain, and a face kit's generic
     *  host props are the wrong place to carry them (doc 44 §13 — could a
     *  different face kit render it?). A fill takes its drivers as ARGUMENTS to
     *  its factory, the way `createStudioSurfaceFills` and `createLabSurfaceFills`
     *  do; the app holds them already. Nothing new may be added beside this. */
    readonly memoryDriver?: MemoryDriver | null;
    readonly workspace: WorkspaceRef | null | undefined;
    readonly workspaceDriver: WorkspaceDriver | null | undefined;
    readonly workspaces: readonly WorkspaceRef[];
    readonly sessions: readonly SessionSnapshot[];
    readonly analytics: WorkspaceAnalyticsState;
    readonly composer: string;
    readonly streaming?: boolean;
    readonly placeholder: string;
    readonly leftSlot: ReactNode;
    readonly rightSlot: ReactNode;
    readonly renderRightSlot: () => ReactNode;
    readonly title: string;
    readonly repo: string;
    readonly avatar: AvatarId;
    readonly vibrState: ThreadStageProps["vibrState"];
    readonly vibrMode: string;
    readonly energy: number;
    readonly tailVerb: string;
    readonly showTailPresence: boolean;
    readonly showAvatars: boolean;
    readonly agentMeta: string;
    readonly onComposerChange: (value: string) => void;
    readonly onSubmit: WorkspaceSurfaceSubmit;
    readonly onSendDetached?: ComposerSubmit;
    readonly onSlash: () => void;
    readonly onStop: () => void;
    readonly onSessionSelect?: (sessionRef: SessionRef) => void;
    readonly onSessionPopout?: (sessionRef: SessionRef, title?: string, workspace?: WorkspaceRef) => void | Promise<void>;
    readonly onWorkspaceSelect?: (workspace: WorkspaceRef) => void;
    readonly onAddProject?: () => void;
    /** When set, the workspace stage renders the Loops home instead of the start surface /
     *  thread (the ∞ Loops rail action, mounted in-pane rather than as a full-screen overlay). */
    readonly loopsActive?: boolean;
    /** When set, the workspace stage renders the Models home instead of the start surface /
     *  thread (the Models rail action, mounted in-pane like Loops). */
    readonly modelsActive?: boolean;
    readonly modelsHomeProps?: ModelsHomeProps;
    readonly onModelAgent?: (seed: string) => void;
    readonly pulseUrl?: string;
    readonly onPulseEndpointStale?: () => void;
    /** Open the loop-agent dock chat seeded for creation (New Loop consistency with Edit Loop). */
    readonly onNewLoopAgent?: (seed: string, workspace: WorkspaceRef) => void;
    readonly onEditLoop?: (loopName: string, workspace: WorkspaceRef) => Promise<SessionRef | null>;
    readonly machinistSession?: SessionRef | null;
    /** Which config surface last summoned the Machinist — drives the dock header's registry label. */
    readonly machinistSurface?: MachinistSurfaceId | null;
    readonly machinistPending?: boolean;
    readonly machinistError?: string | null;
    /** Mirror of the frame's dock-open chrome state — the machinist dock is only
     *  VISIBLE when this is true (closeDock keeps the session for reopen). */
    readonly dockOpen?: boolean;
    readonly onSelectLoopRun?: (sessionRef: SessionRef) => void;
    /** Nudge the session catalog to re-poll — the fleet's fire-follow uses it to
     *  surface a freshly-fired tick session, same as the run view's followBaseline. */
    readonly onRefreshSessions?: (workspaceId?: string) => void;
    readonly loopRunName?: string | null;
    readonly loopRunSessions?: readonly LoopSidebarSession[];
    readonly onPromoteRun?: (sessionRef: SessionRef, title: string) => void;
    /** Return to the Loops fleet page (the runs sidebar's back affordance). */
    readonly onOpenLoops?: () => void;
    /** Open the active loop's detail page (band 'Open loop'): setView('loops') + open detail. */
    readonly onOpenLoop?: () => void;
    /** When set, LoopsHome opens this loop's detail page (the band's 'Open loop' target). */
    readonly openLoopName?: string | null;
    /** LoopsHome calls this once it consumes openLoopName, so the shell clears the one-shot. */
    readonly onLoopOpened?: () => void;
    readonly tiling: WorkspaceTilingState;
}
interface FraymFrameWorkspaceProps {
    readonly className?: string;
    readonly rail: ReactNode;
    /** The resolved `placement: "column"` switcher and its declared width, or
     *  null/undefined when a `rail-head` switcher took the job. Forwarded
     *  straight to `AppShell` — the workspace does not resolve it (the frame
     *  does, once, so the two placements can never both render). */
    readonly primaryRail?: ReactNode;
    readonly primaryRailWidth?: number;
    readonly workspaceOnly?: boolean;
    /** Host-CONTROLLED environment-card visibility (embed/showcase lane): while
     *  defined it overrides the local open state; omit for the shipped behavior
     *  (open on wide viewports, toggled from the top bar). */
    readonly environmentOpen?: boolean;
    readonly appMode: AppMode;
    readonly dockOpen: boolean;
    readonly railMode: RailMode;
    /** Toggles the session rail between expanded and compact. Lives on the
     *  workspace TOP BAR (not the rail) so the control survives every rail state —
     *  a compact/hidden rail can't offer its own reopen affordance. */
    readonly onToggleRail?: () => void;
    readonly dockWidth: number;
    readonly dockTab: string;
    readonly dockTabs: readonly DockTab[];
    readonly topBarActions?: readonly string[];
    readonly topBarExtras?: ReactNode;
    readonly activeInsight: string | null;
    readonly sessionRef: SessionRef | null | undefined;
    readonly sessionCatalog: readonly SessionSnapshot[];
    readonly sessionDriver: SessionDriver | null | undefined;
    readonly repo: string;
    readonly title: string;
    /** The session checkout's git branch — the top-bar chip. UNDEFINED when there
     *  is no branch to state (non-git workspace, detached HEAD, a worktree whose
     *  checkout went missing). Absent means the chip does not render; it MUST NOT
     *  be back-filled with a workspace id or any other identifier that is not a
     *  branch (that shipped once — the chip read as a hex `workspaceId`). */
    readonly branch?: string;
    readonly threadRef: RefObject<HTMLDivElement | null>;
    readonly showStartSurface: boolean;
    /** The transcript has not arrived yet, so an empty thread is UNKNOWN rather than
     *  empty. Keeps the opening skeleton on screen across the gap between
     *  `sessionOpened` and the engine's replay, which is what makes a session switch
     *  one transition instead of three. See `SessionState.journalHydrated`. */
    readonly awaitingTranscript: boolean;
    /** The thread is mounting because the user just SENT from the start surface. */
    readonly enteredFromStartSurface: boolean;
    readonly isOpeningSession: boolean;
    /** See {@link WorkspaceSurfaceHostProps.isCreatingSession} — a fill needs the
     *  create case distinguishable from an open, or it shows the previous session. */
    readonly isCreatingSession?: boolean;
    /**
     * The composer cannot ADDRESS a session, so typing has nowhere to go.
     *
     * Deliberately NARROWER than {@link isOpeningSession}: a session that is
     * merely opening still accepts input. `sendUserMessage` awaits
     * `#ensureSessionLoaded` and coalesces with the in-flight load, and the
     * optimistic user row is dispatched synchronously — so the send is accepted
     * at 0 ms and runs the moment the sidecar is ready (doc 49 §5.2; the
     * capability is documented at `session-provider-actions.ts:189`). Blocking on
     * `isOpening` disabled the field for a measured 0.6–2.2 s over an ALREADY
     * PAINTED transcript, which is the whole friction.
     */
    readonly composerBlocked: boolean;
    readonly workspace: WorkspaceRef | null | undefined;
    readonly workspaceDriver: WorkspaceDriver | null | undefined;
    readonly terminalDriver: TerminalDriver | null | undefined;
    readonly memoryDriver?: MemoryDriver | null;
    readonly memoryRefreshSignal?: number;
    readonly lab?: FraymLabConfig | null;
    readonly activeSpace: SpaceUiDef;
    readonly surfaceFills: WorkspaceSurfaceFills<WorkspaceSurfaceHostProps>;
    /** The active space's mounted workspace surface (doc 44 §5). */
    readonly activeSurface: SurfaceId;
    /** ONE navigation channel: workspace affordances emit typed ShellIntents. */
    readonly onIntent: (intent: ShellIntent) => void;
    /** The engine's live Pipeline catalog (resource snapshot); absent falls back
     *  to the bundled starter catalog. */
    readonly enginePipelines?: readonly EnginePipelineRecord[];
    readonly enginePipelineIssues?: readonly EnginePipelineIssue[];
    /** Start a run from the Pipelines surface. Absent → it browses only. */
    readonly engineDesignSystems?: readonly EngineDesignSystemRecord[];
    readonly engineArtifactories?: readonly EngineArtifactoryRecord[];
    readonly resolvePluginAssetUrl?: (pluginId: string, path: string) => string | null;
    readonly engineTeams?: readonly EngineTeamRecord[];
    readonly engineEvaluators?: readonly EngineEvaluatorRecord[];
    readonly workspaces: readonly WorkspaceRef[];
    readonly sessions: readonly SessionSnapshot[];
    readonly analytics: WorkspaceAnalyticsState;
    readonly composer: string;
    readonly streaming?: boolean;
    readonly leftSlot: ReactNode;
    readonly rightSlot: ReactNode;
    readonly renderRightSlot: () => ReactNode;
    readonly placeholder: string;
    readonly avatar: AvatarId;
    readonly vibrState: ThreadStageProps["vibrState"];
    readonly vibrMode: string;
    readonly energy: number;
    readonly tailVerb: string;
    readonly showTailPresence: boolean;
    readonly showAvatars: boolean;
    readonly agentMeta: string;
    readonly tree: SessionTreeSnapshot | null;
    readonly tasks: readonly {
        readonly name: string;
        readonly status: string;
    }[];
    readonly runningTools: number;
    readonly onRefreshSessions?: (workspaceId?: string) => void;
    readonly onToggleDock: () => void;
    readonly onOpenDockMenu: (event: MouseEvent<HTMLButtonElement>) => void;
    readonly onDockTabChange: (tab: string) => void;
    readonly onCloseDock: () => void;
    readonly onDockResizeStart: (event: PointerEvent<HTMLButtonElement>) => void;
    readonly onComposerChange: (value: string) => void;
    readonly onSubmit: ComposerSubmit;
    /** Send a message that never lived in the composer (a parked stash entry): skips
     *  the composer clear, which would otherwise drop live text AND attachments. */
    readonly onSendDetached?: ComposerSubmit;
    readonly onSlash: () => void;
    readonly onStop: () => void;
    readonly onSessionSelect?: (sessionRef: SessionRef) => void;
    readonly onSessionPopout?: (sessionRef: SessionRef, title?: string, workspace?: WorkspaceRef) => void | Promise<void>;
    readonly onWorkspaceSelect?: (workspace: WorkspaceRef) => void;
    readonly onAddProject?: () => void;
    readonly onNewWorktree?: () => void;
    readonly onMoveToWorktree?: (sessionRef: SessionRef) => void;
    readonly loopsActive?: boolean;
    readonly modelsActive?: boolean;
    readonly modelsHomeProps?: ModelsHomeProps;
    readonly onModelAgent?: (seed: string) => void;
    readonly machinistSession?: SessionRef | null;
    readonly machinistSurface?: MachinistSurfaceId | null;
    readonly machinistPending?: boolean;
    /** Machinist dock error: `onOpenMachinist` rejected or resolved with no session. The dock
     *  swaps its pending skeleton for this message + a retry that re-invokes the flow. */
    readonly machinistError?: string | null;
    readonly onRetryMachinist?: () => void;
    /** Replace the remembered Machinist dock session with one selected from its history. */
    readonly onResumeMachinistSession?: (sessionRef: SessionRef) => void;
    /** Forget the dock session so its next page-dock open mints a clean Machinist thread. */
    readonly onNewMachinistThread?: () => void;
    readonly pulseUrl?: string;
    readonly onPulseEndpointStale?: () => void;
    readonly onNewLoopAgent?: (seed: string, workspace: WorkspaceRef) => void;
    readonly onEditLoop?: (loopName: string, workspace: WorkspaceRef) => Promise<SessionRef | null>;
    readonly onSelectLoopRun?: (sessionRef: SessionRef) => void;
    /** Set while the OPEN session is a loop tick run — mounts the runs sidebar
     *  beside the thread (the Aether-bench inner-rail pattern). */
    readonly loopRunName?: string | null;
    /** The active loop's tick sessions from the catalog, for the sidebar's history. */
    readonly loopRunSessions?: readonly LoopSidebarSession[];
    /** Promote the active run to an ordinary workspace session (rename drops the stamp). */
    readonly onPromoteRun?: (sessionRef: SessionRef, title: string) => void;
    /** Return to the Loops fleet page (the runs sidebar's back affordance). */
    readonly onOpenLoops?: () => void;
    /** Open the active loop's detail page from the run report band. */
    readonly onOpenLoop?: () => void;
    /** One-shot: LoopsHome opens this loop's detail once, then calls onLoopOpened. */
    readonly openLoopName?: string | null;
    readonly onLoopOpened?: () => void;
}
interface WorkspaceMainPaneProps {
    readonly frame: FraymFrameWorkspaceProps;
    readonly isChat: boolean;
    readonly aethrState: AethrState;
    readonly recede: number;
    readonly onAethrStateChange: (state: AethrState) => void;
    readonly tiling: WorkspaceTilingState;
}

/** The live presence a rail row needs: the same triple the foreground session feeds RailPresence. */
interface RailVibr {
    readonly state: VibrState;
    readonly mode: VibrMode;
    readonly energy: number;
}

interface FraymFrameRailProps {
    readonly version: string;
    readonly appMode: AppMode;
    readonly railMode: RailMode;
    readonly onToggleCompact: () => void;
    /** The registered spaces, ordered and already deployment-filtered — the
     *  space switcher renders from it; the active def's `rail.actions` is the
     *  content-layer action registry (step ③: data replaced the arrays). */
    readonly spaces: readonly SpaceUiDef[];
    readonly projectLabel: string;
    readonly menu: MenuState;
    readonly sessionSearchOpen: boolean;
    readonly sessionSearch: string;
    readonly searchInputRef: RefObject<HTMLInputElement | null>;
    readonly groups: readonly RepoGroup[];
    readonly avatar: AvatarId;
    /** Live vibr of the active session — the rail mirrors it onto that session's dot. */
    readonly vibrState: AvatarState;
    readonly vibrMode: AvatarMode;
    readonly energy: number;
    /** Master rail-vibr toggle: when false, every row uses the colored radiating status dot. */
    readonly railVibr: boolean;
    /** Opt-in: animate the live vibr for EVERY running row, not just the active one. */
    readonly railVibrAllSessions: boolean;
    /** Live vibr of background running sessions, keyed by `SessionItem.id`. Empty unless opted in. */
    readonly sessionVibrs: ReadonlyMap<string, RailVibr>;
    readonly userName: string;
    readonly userAvatarUrl?: string;
    readonly planLabel: string;
    readonly productLabel: string;
    readonly onAppModeChange: (mode: AppMode) => void;
    /** The resolved `placement: "rail-head"` switcher, mounted in the rail's head
     *  where the strip and the menu used to be HARDCODED. Absent when a
     *  `placement: "column"` switcher took the job, or when there is only one
     *  item to switch between — both decided by the host, so the rail neither
     *  resolves it nor second-guesses it. */
    readonly switcher?: ReactNode;
    readonly onNewSession?: (workspace?: WorkspaceRef) => void;
    /** Discovered agents for the secret creation-time picker (ctrl/⌘-click New Session).
     *  Empty/absent → the gesture just creates a normal session. */
    readonly newSessionAgents?: readonly AgentChoice[];
    /** Create a new session AS a chosen agent (ctrl/⌘-click New Session → pick). */
    readonly onNewSessionAs?: (agentName: string, workspace?: WorkspaceRef) => void;
    /** Non-default BODIES the same gesture offers (doc 48 §6). Absent/empty → the
     *  picker shows agents only. */
    readonly newSessionBodies?: readonly HarnessChoice[];
    /** Create a new session ON a chosen body (ctrl/⌘-click New Session → pick). */
    readonly onNewSessionOn?: (harnessId: string, workspace?: WorkspaceRef) => void;
    /** ONE navigation channel: every rail action emits a typed ShellIntent
     *  (doc 44 §5); the shell's executor is the single writer. */
    readonly onIntent: (intent: ShellIntent) => void;
    /** The active space's mounted workspace surface — active-state highlighting
     *  is an identity match (doc 44 §12.2). */
    readonly activeSurface: SurfaceId;
    readonly onSessionSearchChange: (value: string) => void;
    readonly onSessionSearchOpenChange: (open: boolean | ((current: boolean) => boolean)) => void;
    readonly onOpenFilterMenu: (event: MouseEvent<HTMLButtonElement>) => void;
    readonly onOpenProjectFilter?: (project: string, event: MouseEvent<HTMLButtonElement>) => void;
    readonly onSessionSelect?: (item: SessionItem) => void;
    readonly onSessionContextMenu: (item: SessionItem, event: MouseEvent<HTMLButtonElement>) => void;
    readonly onSessionMultiContextMenu?: (item: SessionItem, selectedItems: readonly SessionItem[], event: MouseEvent<HTMLButtonElement>) => void;
    /** Right-click on a project group header — opens the project context menu. */
    readonly onGroupContextMenu?: (group: RepoGroup, event: MouseEvent<HTMLButtonElement>) => void;
    /** Move a project up/down in the persisted project order, landing beside the
     *  RENDERED neighbour named by `anchorWorkspaceId`. Absent → the group
     *  header's reorder arrows and menu rows stay off. */
    readonly onReorderProject?: (workspace: WorkspaceRef, direction: "up" | "down", anchorWorkspaceId?: string) => void;
    readonly onOpenUserMenu: (event: MouseEvent<HTMLButtonElement>) => void;
    readonly renamingItemId?: string | null;
    readonly onRenameItem?: (item: SessionItem, value: string | null) => void;
}

/** The rail slot contract's props (doc 44 §4.1) — Data in · Intents out.
 *
 *  specVersion 1 carries the shipped rail surface verbatim: session catalog
 *  groups, space switcher data, action definitions, identity/footer data, and
 *  host side-effect callbacks (session create/select, menus). Navigation flows
 *  ONLY through `onIntent`; active-state highlighting is an identity match on
 *  `activeSurface` (doc 44 §12.2).
 *
 *  Duties (the conformance suite asserts these):
 *  - Render every enabled action; emit the action's mapped ShellIntent on click.
 *  - Exactly one primary action (the space's create verb) — never droppable.
 *  - Mark a surface action active iff its surface === `activeSurface`. */
type RailSlotProps = FraymFrameRailProps;
/** One installable filling of the rail slot ("five rails" — doc 44 §4).
 *  Implementations are open; the slot set is closed. */
interface RailImplementation {
    readonly specVersion: 1;
    readonly id: ImplId;
    readonly slot: "rail";
    readonly component: ComponentType<RailSlotProps>;
}

type ClassValue = ClassArray | ClassDictionary | string | number | bigint | null | boolean | undefined;
type ClassDictionary = Record<string, any>;
type ClassArray = ClassValue[];

declare function cn(...inputs: ClassValue[]): string;

/** The closed, platform-owned set of shell slot contracts a space's layout
 *  exposes (NOMENCLATURE § Spaces And Rooms — "slot contract"). An
 *  implementation fills exactly ONE; the set never grows from a plugin (grid
 *  law). `workspace-surface` is the pane's inner region, guarded by its own
 *  crash boundary alongside the three top-level slots.
 *
 *  AMENDMENT 2026-08-08 (owner ruling, in conversation) — the set is SIX.
 *  `switcher` and `mark` join it, and they pass doc 44 §15's bar (both sides
 *  need it; no existing contract expresses it).
 *
 *  **Both are GENERIC, and that is the whole point of the ruling.** The owner
 *  asked for a Discord/Slack-style primary rail that loads a space, and the
 *  first draft of this amendment called the socket `space-switcher`. That was
 *  wrong, and wrong in the way this repo keeps getting caught: Fraym owns UI
 *  PHYSICS, "space" is a Dimension word, and a component named after one of its
 *  uses can only ever have that use. A column of marks that selects one of N
 *  things is the same component whether the things are Discord servers, Slack
 *  workspaces, VS Code activity views, or our spaces.
 *
 *  - `switcher` — single-select navigation over a set of items, with the
 *    presentation left open. It replaces three behaviours that were hardcoded
 *    inside the session rail and chosen by `items.length > 4`: nothing was
 *    nameable, nothing was replaceable, and a third presentation could not
 *    exist without editing the rail. Three built-ins fill it now
 *    (`switcher-rail` · `switcher-tabs` · `switcher-dropdown`) and a fourth
 *    needs no permission — law 3, applied literally.
 *  - `mark` — how ONE item draws itself inside a switcher. Owner: the entry may
 *    be an icon, a thumbnail, "or a component… somebody can choose to write a
 *    very nice animated orb". A component resolved by id is a registry lookup,
 *    which is precisely a slot.
 *
 *  Neither is a variant of an existing contract. `rail` is handed the sessions
 *  and projects WITHIN a space and structurally cannot see the item set;
 *  `workspace-surface` is inside the pane. A switcher mounts outside both and
 *  ranges over a set neither can name.
 *
 *  The litmus every identifier in these two contracts must pass: *could this
 *  switch between Discord servers with zero changes?* The shell's mapping from
 *  spaces to `SwitcherItem[]` is the only place the word "space" belongs. */
type SlotContract = "rail" | "dock" | "workspace" | "workspace-surface" | "switcher" | "mark";

/** The workspace-surface slot contract's props (doc 44 §4.2) — Data in ·
 *  Intents out. This is the pane's INNER region: what the workspace host
 *  mounts once it has decided the geometry, guarded by its own crash boundary
 *  alongside the three top-level slots.
 *
 *  specVersion 1 carries the shipped host surface verbatim; narrowing it to a
 *  per-surface projection is step-③ work — the contract records today's true
 *  surface rather than inventing a smaller one (doc 44 §13, the
 *  decorative-declaration law).
 *
 *  Duties:
 *  - Fill the pane; the host owns the geometry, the surface owns the content.
 *  - Name itself through the frame's title slot when the space's `chrome` for
 *    this surface is `"surface"`; never draw a session breadcrumb.
 *  - Never own navigation truth — emit ShellIntents through the frame. */
type WorkspaceSurfaceSlotProps = WorkspaceSurfaceHostProps;
/** One installable filling of the workspace-surface slot.
 *
 *  This slot's WORKING registry is `WorkspaceSurfaceFills` — a plain id →
 *  component map, already consumed by `packages/lab`, `packages/studio` and
 *  the pipelines fill. This record is the same fact stated as a slot
 *  implementation, so a surface can be declared, conformance-tested and
 *  published the way the other three slots' fillings are. `workspaceSurfaceFillsFrom`
 *  converts a list of these into the map; nothing has to choose one shape. */
interface WorkspaceSurfaceImplementation {
    readonly specVersion: 1;
    readonly id: ImplId;
    readonly slot: "workspace-surface";
    readonly component: ComponentType<WorkspaceSurfaceSlotProps>;
}

/** A mounted implementation under test (doc 44 §12.5). */
interface MountedForTest {
    /** The DOM container the implementation rendered into. */
    readonly container: HTMLElement;
    /** Every ShellIntent the implementation emitted, in order. */
    readonly intents: readonly ShellIntent[];
    readonly unmount: () => void;
}
/** Mount a kind implementation standalone — fixture data in, recorded intents
 *  out; no engine, no live shell (doc 44 §12.5: every implementation MUST be
 *  mountable this way; the per-kind conformance suites run on top of it).
 *
 *  The caller builds the slot props around the provided recorder, wiring it
 *  wherever the slot's contract carries `onIntent` (the rail takes it
 *  top-level; dock/host carry it on the frame):
 *
 *  ```ts
 *  const mounted = mountForTest(RAIL_CLASSIC, record => railFixture({ onIntent: record }));
 *  ```
 *
 *  Requires an ambient DOM (`document`) — install one in the test file (the
 *  suite convention uses linkedom; see test/rail-intent.test.tsx). */
declare function mountForTest<P extends object>(impl: {
    readonly component: ComponentType<P>;
}, makeProps: (recordIntent: (intent: ShellIntent) => void) => P): MountedForTest;

/**
 * Slot fixture props — the dummy data a slot implementation mounts on when
 * there is no engine behind it (doc 44 §12.5).
 *
 * Two consumers, one source:
 *
 * - the per-kind CONFORMANCE suites (`test/space-kind-duties.test.tsx`) mount
 *   every implementation on these props and assert its duties;
 * - the SPACE DESIGNER mounts the same implementations on the same props, so
 *   the component you drag onto the canvas is the shipped one, rendering real
 *   chrome over fixture data.
 *
 * That sharing is the point. A designer with its own hand-rolled props would
 * drift from what the conformance suite certifies; a fixture that lives in a
 * test file cannot be imported by a product surface. Here both mount the same
 * thing, so "the canvas shows the real component" stays true by construction.
 *
 * Data is deliberately fake and visibly plausible — a couple of repos, a few
 * sessions — because a hollow rail (zero groups) reads as broken, and a
 * designer is where you look at the component, not at your own engine state.
 */

/** The code space's shipped action set — what the rail duties assert against. */
declare const FIXTURE_RAIL_ACTIONS: readonly RailActionDef[];
/** Rail slot props on fixture data. `overrides` lets a test pin one field. */
declare function railSlotFixture(onIntent: (intent: ShellIntent) => void, overrides?: Partial<FraymFrameRailProps>): FraymFrameRailProps;

export { FIXTURE_RAIL_ACTIONS, Icon, SURFACE, cn, mountForTest, railSlotFixture };
export type { DockImplementation, DockSlotProps, IconName, ImplId, MountedForTest, PinPost, RailActionDef, RailImplementation, RailSlotProps, RepoGroup, SessionItem, ShellIntent, SlotContract, SpaceId, SpaceUiDef, SurfaceId, WorkspaceHostImplementation, WorkspaceHostSlotProps, WorkspaceSurfaceImplementation, WorkspaceSurfaceSlotProps };
