{
	"name": "@dimension/phone-switcher",
	"author": "Dimension",
	"version": "0.1.0",
	"description": "Phone Switcher — the space switcher as a rail-head menu, for layouts that cannot afford a column (a 390px phone).",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"scripts": {
		"build": "vite build"
	},
	"peerDependencies": {
		"react": ">=19"
	},
	"devDependencies": {
		"vite": "8.1.5"
	},
	"omp": {
		"name": "phone-switcher",
		"description": "The space switcher as a rail-head menu: re-exports SWITCHER_DROPDOWN from @fraym/ui. For layouts that cannot afford a 56px switcher column — the `phone` layout.",
		"defaultEnabled": true,
		"category": "component",
		"keywords": [
			"component",
			"switcher",
			"phone"
		]
	}
}
