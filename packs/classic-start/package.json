{
	"name": "@dimension/classic-start",
	"author": "Dimension",
	"version": "0.1.0",
	"description": "Classic Start — the session surface's front door (hero composer, Project · Work strip, popover menus), as a marketplace pack.",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"scripts": {
		"build": "vite build"
	},
	"peerDependencies": {
		"react": ">=19"
	},
	"devDependencies": {
		"vite": "8.1.5"
	},
	"dimension": {
		"name": "classic-start",
		"description": "The shipped START SURFACE as a pack — the session surface's front door: hero composer, Project · Work strip, popover menus. Fills the `start` contract (a SECTION like `thread`/`composer`, resolved inside the session surface). The hero sentence comes from the space's `workspace.startCopy`, never from the kit.",
		"defaultEnabled": true,
		"category": "component",
		"keywords": [
			"component",
			"start",
			"classic"
		]
	}
}
