{
	"name": "@dimension/session-board",
	"author": "Dimension",
	"version": "0.1.0",
	"description": "Sessions - a mission-control dock component: every session partitioned by urgency (needs you / working / idle), switchable in one click. Contributed through @dimension/sdk/component; import surface: react only.",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"scripts": {
		"build": "vite build",
		"test": "bun test"
	},
	"peerDependencies": {
		"react": ">=19"
	},
	"keywords": [
		"component",
		"dock",
		"sessions",
		"store",
		"assembly"
	],
	"dimension": {
		"name": "session-board",
		"description": "Sessions - mission control in the dock: every session partitioned by urgency (needs you / working / idle), one-click switching. The first contributed dock component (\"type\": \"component\", \"slot\": \"dock\"), at the zero-import floor.",
		"defaultEnabled": true,
		"category": "component",
		"keywords": [
			"component",
			"dock",
			"sessions",
			"store"
		]
	},
	"devDependencies": {
		"vite": "8.1.5"
	}
}
